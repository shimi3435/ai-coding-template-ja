---
phase: 01-stable-identity-and-migration
plan: 04
type: tdd
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
  - tests/test_handoff_migration.py
autonomous: true
requirements:
  - HND-01
must_haves:
  truths:
    - "Caller can preview an exact schema-1 to schema-2 migration without any filesystem mutation."
    - "Preview reports old/new byte hashes, stable identity allocation/update/tombstone evidence, exclusions, and a deterministic approval identity."
    - "Unknown schema, downgrade, malformed source, collision, or incomplete observation returns structured non-success before staging."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      provides: "Read-only migration preview and immutable approval-binding evidence"
    - path: tests/test_handoff_migration.py
      provides: "Isolated read-only preview, stale input, unknown schema, and no-mutation examples"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "complete inventory and reconciliation result"
      pattern: "read_source_inventory|reconcile_source_items"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
      via: "exact schema-2 candidate construction and serialization"
      pattern: "HandoffManifestV2|serialize_manifest_v2"
---

<objective>
Exact schema-1 bytesとcanonical source observationsから、副作用のない完全なschema-2 migration previewを構築する。

Purpose: operatorがstable ID割当、candidate bytes、除外理由、hash bindingをreviewしてからのみapplyへ進める境界を作る。
Output: immutable migration preview/result seamとisolated filesystem tests。
Source authority: canonical HARD-R1 migration scenarios at `2cbb127917feaa637ef5eac439478227ac5f717b`。
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stable-identity-and-migration/01-02-SUMMARY.md
@.planning/phases/01-stable-identity-and-migration/01-03-SUMMARY.md
@.planning/phases/01-stable-identity-and-migration/01-RESEARCH.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/proposal.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
@src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py
@tests/fixtures/openspec_gsd_handoff/manifest/expected-prepared.json

<interfaces>
Expose module-level `preview_manifest_migration(...)` returning an immutable `ManifestMigrationPreview` on `Success`. The preview owns exact observed schema-1 SHA-256, candidate schema-2 SHA-256/bytes/value, stable allocation/update/tombstone/exclusion evidence, and one deterministic `preview_sha256` covering the complete machine-readable preview. It performs no write, mkdir, staging, or replacement operation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a complete read-only migration preview from frozen observations</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/proposal.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py
    - tests/fixtures/openspec_gsd_handoff/manifest/expected-prepared.json
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py, tests/test_handoff_migration.py</files>
  <behavior>
    - RED: a copied exact schema-1 fixture plus isolated canonical source returns old/new SHA-256, schema-2 candidate bytes/value, source allocation/update/tombstone evidence, exclusions, and deterministic preview identity.
    - RED: preview leaves the target and repository tree byte-for-byte unchanged and creates no staging or `.planning` path.
    - RED: rerunning the same frozen inputs returns an equal preview; content edit with stable identity retains the ID and reports fingerprint update.
  </behavior>
  <action>Compose the existing bounded schema-1 reader, source inventory, reconciliation, exact schema-2 constructor, and serializer into one application operation. Preserve all schema-1 fields and populate source state while emitting only `mappings=[]`, `ownership={"owned":[],"referenced":[]}`, and `lifecycle={"checkpoints":[],"receipts":[],"archives":[]}`. Build the machine view in fixed field order from `v1_sha256`, `v2_sha256`, stable create/update/tombstone records, and exclusions; encode it as compact UTF-8 JSON with `ensure_ascii=False`, fixed insertion order, and one final newline, then SHA-256 those bytes into `preview_sha256`. This is approval evidence only and is not a manifest field or root CLI operation. Do not copy canonical requirement/scenario prose into evidence.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Preview is fully deterministic and contains both old and proposed byte identities.
    - No mutation method is called and the target bytes remain exact schema 1.
    - Empty mappings remain explicitly not operation-ready.
  </acceptance_criteria>
  <done>The caller can review a complete source-grounded schema-2 candidate and approval identity without side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refuse incomplete preview inputs before any staging boundary</name>
  <read_first>
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
    - tests/test_handoff_migration.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py
    - tests/test_handoff_identity.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py, tests/test_handoff_migration.py</files>
  <behavior>
    - RED: unknown schema, existing schema 2 requested as schema 1, requested version below disk, malformed source, collision, exhausted allocation, oversized target/source, and unreadable input return structured failure.
    - GREEN: failure identifies the blocking seam and preserves every observed byte; it never returns a partial candidate or approval identity.
  </behavior>
  <action>Add isolated fixed examples for preview preconditions. Route schema decisions through the versioned exact facade, not a second parser. Record only evidence that was fully observed. Do not retry, migrate the tracked historical handoff, infer a downgrade, create the Phase-2 policy record, or invoke OpenSpec/GSD/Node/Git history.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_migration.py tests/test_handoff_identity.py tests/test_handoff_manifest_v2.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All precondition failures occur before create/write/replace calls.
    - Unknown and downgrade cases preserve source bytes.
    - Normal tests remain tool-independent.
  </acceptance_criteria>
  <done>Migration preview fails closed on every incomplete or incompatible input and never fabricates usable evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py`
  - `MigrationCandidateChange(kind, source_id, category, source_path, previous_fingerprint, candidate_fingerprint, reason)`
  - `ManifestMigrationPreview(v1_sha256, v2_sha256, candidate_bytes, candidate_manifest, changes, exclusions, preview_sha256)`
  - `preview_manifest_migration(...)`
- `tests/test_handoff_migration.py`
  - isolated read-only preview, deterministic rerun, content-update, incompatible schema, downgrade, collision, and no-mutation examples

This plan adds no manifest field, CLI flag/subcommand, package-root export, policy reference record, or write operation. It does not migrate the real historical handoff.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema-1 bytes + canonical source → preview | Stale, malformed, or ambiguous inputs can be combined into a misleading candidate. |
| preview evidence → operator approval boundary | Incomplete machine output can cause approval to cover different bytes than reviewed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | migration preview composition | mitigate | Complete exact-parser results only, old/new hashes, stable allocation evidence, deterministic preview identity, and no partial success. |
| T-01-11 | Repudiation | approval evidence | mitigate | Immutable machine-readable preview with explicit input/candidate identities and exclusions. |
| T-01-12 | Elevation of Privilege | preview filesystem access | mitigate | Read-only operations only; mutation-counting adapter proves zero mkdir/stage/write/replace calls. |
| T-01-SC | Tampering | package supply chain | accept | No install or external runtime dependency. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_migration.py tests/test_handoff_identity.py tests/test_handoff_manifest_v2.py -q`
- Confirm the tracked `.planning/openspec/.../handoff.json` and handoff brief have no diff.
</verification>

<success_criteria>
- Preview is complete, deterministic, and side-effect free.
- Every incompatible or uncertain input stops before staging.
- Approval-relevant evidence is exact but no approval/application is performed in this plan.
</success_criteria>

<output>
Create `.planning/phases/01-stable-identity-and-migration/01-04-SUMMARY.md` with preview interfaces, RED/GREEN commits, no-mutation evidence, and tests.
</output>
