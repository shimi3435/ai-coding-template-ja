---
phase: 01-stable-identity-and-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
  - tests/test_handoff_identity.py
  - tests/fixtures/openspec_gsd_handoff/identity/canonical-spec.md
  - tests/fixtures/openspec_gsd_handoff/identity/duplicate-heading.md
  - tests/fixtures/openspec_gsd_handoff/identity/unclosed-fence.md
autonomous: true
requirements:
  - HND-01
must_haves:
  truths:
    - "Caller can inventory normalized requirement/scenario source observations from bounded canonical Markdown without losing raw heading evidence."
    - "Given the same resolved stable parent ID, equivalent LF/NFC/display-heading forms produce the same normalized identity and fingerprint."
    - "Ambiguous Markdown, unsafe paths, aliases, symlinks, invalid UTF-8, and incomplete bounded input return structured failure rather than partial inventory."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      provides: "Immutable source observations, strict bounded inventory, parent locator, and parent-ID-bound framed SHA-256 fingerprint seam"
    - path: tests/test_handoff_identity.py
      provides: "Public-seam fixed examples for source normalization and fail-closed input handling"
    - path: tests/fixtures/openspec_gsd_handoff/identity/canonical-spec.md
      provides: "Independent nested requirement/scenario and fenced-heading source example"
  key_links:
    - from: tests/test_handoff_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "public bounded source inventory operation"
      pattern: "read_source_inventory"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/models.py
      via: "existing immutable Success/Failure whole-operation result"
      pattern: "Success|Failure|ClassifiedIssue"
---

<objective>
Canonical source bytesからstable identityの入力となるsource observationを決定論的に得るpublic seamをTDDで作る。

Purpose: HND-01 / canonical HARD-R1のうちsource inventory・normalization・fingerprint境界を、後続allocatorやmigrationが再解釈せず利用できるようにする。
Output: `source_identity.py`のimmutable valuesとread-only inventory operation、独立fixture、固定例tests。
Source authority: `2cbb127917feaa637ef5eac439478227ac5f717b` のcanonical `design.md` Gate Bとspec delta `HARD-R1`。
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/agents/workflow.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stable-identity-and-migration/01-CONTEXT.md
@.planning/phases/01-stable-identity-and-migration/01-RESEARCH.md
@.planning/phases/01-stable-identity-and-migration/01-PATTERNS.md
@.planning/phases/01-stable-identity-and-migration/01-VALIDATION.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/proposal.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
@src/ai_coding_template_ja/openspec_gsd_handoff/models.py
@src/ai_coding_template_ja/openspec_gsd_handoff/reader.py
@tests/test_handoff_core.py

<interfaces>
Use the existing `Result[T] = Success[T] | Failure` seam from `models.py`. Create immutable source-domain values in `source_identity.py` and expose `read_source_inventory(repository_root, source_paths, *, limits=...)` plus `fingerprint_source_observation(observation, *, parent_id)`. Inventory retains a normalized parent-requirement locator; Plan 02 resolves that locator to the stable requirement ID before calculating a scenario fingerprint. A successful inventory is complete; a failure carries a stable machine code and no partial items.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize supported ATX source blocks and fingerprint exact framed bytes</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/proposal.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/models.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/reader.py
    - tests/test_handoff_core.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, tests/test_handoff_identity.py, tests/fixtures/openspec_gsd_handoff/identity/canonical-spec.md</files>
  <behavior>
    - RED: a nested `### Requirement:` / `#### Scenario:` fixture, CRLF/CR/LF variants, NFC-equivalent text, display-only horizontal whitespace, and heading-like text inside a closed fence yield independently calculated normalized observations and lowercase SHA-256 literals when the same explicit parent ID is supplied.
    - GREEN: raw headings preserve the decoded original line while normalized heading/block/path follow the pinned UTF-8, LF, NFC, ATX, fence, horizontal-whitespace, parent, and 8-byte big-endian length-framing rules.
    - The requirement block extends to the next fenced-code-external heading of the same or higher level; child scenario blocks remain included in the parent requirement block.
  </behavior>
  <action>Run a RED→GREEN cycle through `read_source_inventory` and `fingerprint_source_observation`. Build frozen category/observation/inventory values, a literal-`\n` scanner, exact horizontal-whitespace predicates, canonical POSIX-relative NFC path validation, normalized parent-requirement locators, and version-tagged framed fingerprinting. The fingerprint operation takes `parent_id=None` for requirements and an already resolved `REQ-NNNNNN` for scenarios; it does not allocate IDs. Read each source file once with limit+1 and derive content plus evidence from the same bytes. Do not use `splitlines`, broad whitespace stripping, heuristic Markdown recovery, optional OpenSpec tools, or private-helper tests.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_identity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The positive fixture has independently asserted raw headings, normalized observations, parent locators, normalized blocks, and parent-ID-bound fingerprint literals.
    - Reordering the `source_paths` input or returned observation tuple does not change the normalized-identity/fingerprint mapping; reordering content inside a normalized source block retains the normalized identity but updates that observation's fingerprint.
    - No application dependency or root package export is added.
  </acceptance_criteria>
  <done>The public source inventory seam returns complete immutable observations with exact canonical fingerprint evidence and all focused tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed for ambiguous, escaped, aliased, and bounded source inputs</name>
  <read_first>
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_identity.py
    - tests/fixtures/openspec_gsd_handoff/identity/canonical-spec.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/reader.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, tests/test_handoff_identity.py, tests/fixtures/openspec_gsd_handoff/identity/duplicate-heading.md, tests/fixtures/openspec_gsd_handoff/identity/unclosed-fence.md</files>
  <behavior>
    - RED: invalid UTF-8, unclosed fences, duplicate normalized identity, unsupported/ambiguous headings, empty/absolute/dot-dot/backslash/NUL paths, symlink escape, Unicode or platform-case alias collision, source-item overflow, and limit+1 bytes produce exact structured failure codes.
    - GREEN: any incomplete observation invalidates the whole inventory and no prefix or partial source list is returned.
  </behavior>
  <action>Add negative fixtures and isolated `tmp_path` examples, then implement the narrow fail-closed branches at the public seam. Validate lexical segments before resolution, reject existing symlink components with `lstat`, prove repository containment, canonicalize alias keys without following unsafe paths, and cap source items at 4096 plus the shared 8 MiB envelope. Keep fixed examples only; Phase 1 does not property-test the normalizer.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_identity.py tests/test_handoff_core.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Every negative example returns `Failure` with no partial inventory.
    - Path/symlink/alias tests cannot create or modify `.planning`.
    - Existing reader/core tests remain green.
  </acceptance_criteria>
  <done>The source inventory boundary is bounded and fail-closed for all Phase-1 normalization trust-boundary examples.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py`
  - `SourceCategory`: enum values `requirement`, `scenario`
  - `SourceIdentityLimits(max_items, bytes_per_file, bytes_total)`
  - `SourceObservation(category, source_path, raw_heading, normalized_heading, normalized_block, parent_locator)`
  - `SourceInventory(items)`
  - `read_source_inventory(repository_root, source_paths, *, limits=...)`
  - `fingerprint_source_observation(observation, *, parent_id)`
- `tests/test_handoff_identity.py`
- `tests/fixtures/openspec_gsd_handoff/identity/canonical-spec.md`
- `tests/fixtures/openspec_gsd_handoff/identity/duplicate-heading.md`
- `tests/fixtures/openspec_gsd_handoff/identity/unclosed-fence.md`

Stable ID allocation, manifest codec, and migration persistence are produced by subsequent Phase 1 plans. This phase adds no decorator, CLI flag/subcommand, package-root export, or manifest field.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository path → bounded source bytes | Untrusted path and filesystem metadata enter the inventory adapter. |
| Markdown bytes → source observations | Invalid encoding, ambiguous headings, Unicode aliases, and oversized blocks enter the deterministic core. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing/Tampering | canonical path identity | mitigate | NFC segment keys, platform-case alias detection, lexical rejection, `lstat`, and resolved repository containment before read. |
| T-01-02 | Tampering | ATX/fence scanner | mitigate | strict supported grammar, raw-heading retention, duplicate/ambiguous identity failure, and fixed adversarial fixtures. |
| T-01-03 | Denial of Service | bounded source inventory | mitigate | limit+1 reads, aggregate 8 MiB envelope, 4096 item cap, and no truncated success. |
| T-01-SC | Tampering | package supply chain | accept | No package-manager install or new dependency occurs in this plan. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_identity.py tests/test_handoff_core.py -q`
- Confirm `git diff -- .planning/openspec openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md` is empty.
</verification>

<success_criteria>
- Source observations are deterministic, immutable, bounded, and visible only through a complete public result.
- Fixed examples cover the Phase-1 normalizer seam without adding an unapproved property family.
- The tracked historical handoff manifest and brief remain byte-for-byte untouched.
</success_criteria>

<output>
Create `.planning/phases/01-stable-identity-and-migration/01-01-SUMMARY.md` with RED/GREEN commits, public interfaces, tests, and deviations.
</output>
