---
phase: 01-stable-identity-and-migration
plan: 03
type: tdd
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
  - src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py
  - tests/test_handoff_manifest_v2.py
  - tests/fixtures/openspec_gsd_handoff/manifest/expected-migrated-v2.json
autonomous: true
requirements:
  - HND-01
must_haves:
  truths:
    - "Caller can parse and serialize exact schema 2 without weakening the unchanged schema-1 parser."
    - "Every schema-2 object rejects unknown fields, malformed IDs/counters/parents, invalid bounds, and invalid empty Phase-1 shapes."
    - "Version dispatch selects one exact full parser from the same bounded bytes and rejects unknown schema or requested downgrade."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
      provides: "Frozen exact 11-field schema-2 aggregate and canonical codec"
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py
      provides: "Bounded schema-version dispatch facade preserving the v1 API"
    - path: tests/fixtures/openspec_gsd_handoff/manifest/expected-migrated-v2.json
      provides: "Independent reviewable canonical schema-2 bytes"
    - path: tests/test_handoff_manifest_v2.py
      provides: "Golden, rejection, dispatch, downgrade, and round-trip evidence"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "validated active/tombstone identity types"
      pattern: "SourceItem|SourceTombstone|SourceIdentityState"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest.py
      via: "schema 1 delegates to unchanged parse_manifest_bytes"
      pattern: "parse_manifest_bytes"
---

<objective>
MVP schema 1を互換島として維持しながら、exact schema 2 codecとbounded version dispatchをTDDで追加する。

Purpose: migration候補をstrictに表現し、unknown schema / downgradeをmutation前に拒否できる土台を作る。
Output: `manifest_v2.py`、`versioned_manifest.py`、独立golden fixture、manifest round-trip property。
Source authority: canonical Gate A/B and HARD-R1 at `2cbb127917feaa637ef5eac439478227ac5f717b`。
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stable-identity-and-migration/01-02-SUMMARY.md
@.planning/phases/01-stable-identity-and-migration/01-RESEARCH.md
@.planning/phases/01-stable-identity-and-migration/01-PATTERNS.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/proposal.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_manifest.py
@tests/fixtures/openspec_gsd_handoff/manifest/expected-prepared.json

<interfaces>
Keep `HandoffManifest`, `parse_manifest_bytes`, `serialize_manifest`, `ManifestRepository`, and package `__all__` unchanged. Add `HandoffManifestV2`, `parse_manifest_v2_bytes`, `serialize_manifest_v2`, `parse_versioned_manifest_bytes`, and `read_versioned_manifest_file` in new modules only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode and validate the exact schema-2 aggregate</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/proposal.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_manifest.py
    - tests/fixtures/openspec_gsd_handoff/manifest/expected-prepared.json
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py, tests/test_handoff_manifest_v2.py, tests/fixtures/openspec_gsd_handoff/manifest/expected-migrated-v2.json</files>
  <behavior>
    - RED: a hand-reviewed exact 11-root-field fixture parses to frozen values and serializes deterministically to identical UTF-8 bytes with one final newline.
    - RED: unknown fields at every object level, wrong primitive types, malformed IDs/counters/parents, active/tombstone duplicates, invalid ordering, collection counts over 4096, and file bytes over 8 MiB fail closed.
    - RED: schema-valid migration state requires exact empty `mappings`, exact empty owned/referenced arrays, and exact empty checkpoints/receipts/archives arrays while retaining all seven schema-1 fields unchanged in meaning.
  </behavior>
  <action>Create a separate schema-2 aggregate using frozen dataclasses/enums and exact-field validators patterned after `manifest.py`. The root fields are exactly `schema_version`, `change_id`, `handoff_state`, `artifacts`, `source_commit`, `progress`, `capabilities`, `source_items`, `mappings`, `ownership`, and `lifecycle`. Reuse the Plan-02 source state for exact `next_requirement_id`, `next_scenario_id`, `active`, and `tombstones`. Define mapping entries with `source_id`, `phase_id`, `phase_path`, `plan_paths`, `evidence_paths`, `policy_references`; ownership entries with `kind`, `path` under exact `owned`/`referenced`; and lifecycle references with `operation_id`, `path`, `sha256`, `state` under exact `checkpoints`/`receipts`/`archives`. Enforce the canonical owned/reference kind allowlists and `pending`/`completed`/`failed`/`unknown` reference states even though Phase 1 emits empty arrays; do not implement later-phase readiness behavior. Serialize with fixed field order, reparse the complete emitted bytes, and compare value equality. The expected fixture must be a literal reviewed artifact, not serializer output.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_v2.py tests/test_handoff_manifest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The golden file contains exact 11 root fields and the Phase-1-valid empty shapes.
    - Schema 1 fixture bytes and parser behavior remain unchanged.
    - Invalid objects never produce a partial schema-2 value.
  </acceptance_criteria>
  <done>The exact schema-2 value/codec round-trips the independent golden bytes and rejects malformed/bounded inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch exact schema versions and prove schema-2 round trip</name>
  <read_first>
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
    - tests/test_handoff_manifest_v2.py
    - tests/test_handoff_manifest.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py, tests/test_handoff_manifest_v2.py</files>
  <behavior>
    - RED: schema 1 delegates to the existing exact parser; schema 2 delegates to the new exact parser using the same bounded byte observation.
    - RED: malformed/missing/boolean/unknown schema versions and caller requests below the disk version return structured failure without mutation.
    - RED property: every bounded valid complete schema-2 value serializes, parses, and reserializes to the same value and bytes.
  </behavior>
  <action>Add a narrow versioned facade that performs one bounded read, inspects only the JSON schema discriminator, and then requires the selected exact full parser to succeed. Do not broaden `parse_manifest_bytes`, make the version probe a success value, add a CLI verb, or export migration operations from package root. Add the only approved Phase-1 manifest round-trip Hypothesis family with bounded valid complete values; keep source parsing and filesystem effects outside it.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_v2.py tests/test_handoff_manifest.py tests/test_handoff_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Exact schema dispatch is deterministic and bounded.
    - Unknown schema and downgrade failures occur before any write seam.
    - Existing exact three package operations and CLI subcommands remain unchanged.
  </acceptance_criteria>
  <done>Versioned reads preserve schema-1 compatibility, accept only exact schema 2, and fail closed on unknown/downgrade inputs.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py`
  - `ManifestMapping(source_id, phase_id, phase_path, plan_paths, evidence_paths, policy_references)`
  - `OwnershipEntry(kind, path)`
  - `ManifestOwnership(owned, referenced)`
  - `LifecycleRecordReference(operation_id, path, sha256, state)`
  - `ManifestLifecycle(checkpoints, receipts, archives)`
  - `HandoffManifestV2(schema_version, change_id, handoff_state, artifacts, source_commit, progress, capabilities, source_items, mappings, ownership, lifecycle)`
  - `parse_manifest_v2_bytes(data)`
  - `serialize_manifest_v2(manifest)`
- `src/ai_coding_template_ja/openspec_gsd_handoff/versioned_manifest.py`
  - `VersionedManifest = HandoffManifest | HandoffManifestV2`
  - `parse_versioned_manifest_bytes(data, *, requested_schema_version=None)`
  - `read_versioned_manifest_file(path, *, requested_schema_version=None, operations=None)`
- `tests/test_handoff_manifest_v2.py`
- `tests/fixtures/openspec_gsd_handoff/manifest/expected-migrated-v2.json`

This plan creates the canonical schema-2 root fields `source_items`, `mappings`, `ownership`, and `lifecycle` through `HandoffManifestV2`; it adds no optional schema-1 field, CLI flag/subcommand, or package-root export.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted JSON bytes → version selection | A malformed discriminator can cause schema confusion or partial validation. |
| schema-2 object graph → canonical bytes | Unknown fields, oversized collections, and invalid references can enter persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing/Tampering | schema dispatch | mitigate | Exact integer discriminator, same-byte full parser dispatch, and unknown/downgrade structured failure. |
| T-01-08 | Tampering | schema-2 nested objects | mitigate | Exact field sets, primitive type checks, identity/parent validation, deterministic ordering, and serialize-reparse equality. |
| T-01-09 | Denial of Service | manifest codec | mitigate | 8 MiB limit+1 envelope and 4096 cap on every bounded collection. |
| T-01-SC | Tampering | package supply chain | accept | No package installation or dependency update occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_manifest_v2.py tests/test_handoff_manifest.py tests/test_handoff_cli.py -q`
- Confirm `python -m ai_coding_template_ja.openspec_gsd_handoff --help` still shows exactly the MVP operations.
</verification>

<success_criteria>
- Exact v1 and exact schema 2 coexist without optional-field weakening.
- Round-trip evidence is limited to the approved manifest property seam.
- Mapping/ownership/lifecycle placeholders are schema-valid but do not claim later operation readiness.
</success_criteria>

<output>
Create `.planning/phases/01-stable-identity-and-migration/01-03-SUMMARY.md` with codec interfaces, golden review evidence, RED/GREEN commits, and regression results.
</output>
