---
phase: 02-approval-gated-skill-orchestration
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .agents/skills/skills.lock.json
  - .claude/skills/execute-openspec-change
  - .codex/skills/execute-openspec-change
  - tests/test_skills_lock.py
  - AGENTS.md
  - docs/agents/workflow.md
  - docs/optional/gsd.md
autonomous: true
requirements:
  - SKILL-01
must_haves:
  truths:
    - "The first-party execute-openspec-change skill has a local lock entry whose SHA-256 matches the final SKILL.md bytes."
    - "Claude and Codex resolve the same canonical .agents skill through the existing relative-symlink distribution path."
    - "Agent and operator guidance names the skill, preserves read-only preview and explicit approval, gives both GSD routes the exact parity payload, and warns that ambiguous/failing dispatch retains prepared."
    - "Guidance defines acceptance as structured completed-success plus the route-specific read-only probe/filesystem postcondition; prose markers alone never authorize started."
    - "Guidance documents the generic-agent workaround and its fail-closed boundary without presenting it as equivalent to typed dispatch."
    - "Guidance stops at GSD handoff start and does not claim lifecycle automation, final completion, retry/recovery, or automatic cleanup."
    - "Guidance states that normal CI verifies the static skill instruction contract and Phase 1 state seam only; actual host orchestration is unverified in Phase 2 and belongs to Phase 3 opt-in/manual evidence."
  artifacts:
    - path: .agents/skills/skills.lock.json
      provides: first-party provenance and SHA-256 integrity gate
    - path: .claude/skills/execute-openspec-change
      provides: Claude relative symlink to the canonical skill
    - path: .codex/skills/execute-openspec-change
      provides: Codex relative symlink to the canonical skill
    - path: tests/test_skills_lock.py
      provides: explicit first-party lock and dual-runtime distribution evidence
    - path: AGENTS.md
      provides: concise agent routing guidance
    - path: docs/agents/workflow.md
      provides: authoritative operator sequence and failure boundary
    - path: docs/optional/gsd.md
      provides: opt-in GSD user guidance and retention boundary
  key_links:
    - from: .agents/skills/skills.lock.json
      to: .agents/skills/execute-openspec-change/SKILL.md
      via: source_type local entry and exact SHA-256
      pattern: "execute-openspec-change"
    - from: .claude/skills/execute-openspec-change
      to: .agents/skills/execute-openspec-change
      via: existing ../../.agents/skills relative target
      pattern: "\.agents/skills/execute-openspec-change"
    - from: .codex/skills/execute-openspec-change
      to: .agents/skills/execute-openspec-change
      via: existing ../../.agents/skills relative target
      pattern: "\.agents/skills/execute-openspec-change"
    - from: docs/agents/workflow.md
      to: .agents/skills/execute-openspec-change/SKILL.md
      via: named entrypoint and canonical handoff boundary
      pattern: "execute-openspec-change"
---

<objective>
D-01 と D-11 に従い、Plan 02-01 で確定した skill bytes を既存 lock/hash と Claude/Codex relative-symlink distribution に載せ、agent/operator guidance を handoff-start boundary へ接続する。

Purpose: 両 runtime が同一の監査済み skill を利用し、利用者が approval、parity payload、observable acceptance、dispatch degradation、prepared retention、evidence limits、OpenSpec final authority を誤解しない導線を完成させるため。
Output: local lock entry、dual-runtime symlinks、distribution regression、agent/workflow/optional GSD guidance。
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-gated-skill-orchestration/02-CONTEXT.md
@.planning/phases/02-approval-gated-skill-orchestration/02-01-SUMMARY.md
@openspec/changes/automate-openspec-gsd-handoff/proposal.md
@openspec/changes/automate-openspec-gsd-handoff/design.md
@openspec/changes/automate-openspec-gsd-handoff/specs/openspec-gsd-handoff-automation/spec.md
@openspec/changes/automate-openspec-gsd-handoff/tasks.md
@.agents/skills/execute-openspec-change/SKILL.md
@.agents/skills/skills.lock.json
@scripts/setup-skills.sh
@tests/test_skills_lock.py
@tests/test_setup_skills.py
@docs/agents/workflow.md
@docs/optional/gsd.md
</context>

<interfaces>
`.agents/skills/<name>/` is the only skill implementation root. `scripts/setup-skills.sh` discovers every directory below that root, performs a complete non-symlink collision preflight, and then creates or repairs `.claude/skills/<name>` and `.codex/skills/<name>` as `../../.agents/skills/<name>` relative symlinks. This plan must invoke that script; it must not add another symlink implementation.

`tests/test_skills_lock.py` already enforces one-to-one allowed entries/directories, license existence, SKILL.md SHA-256, and both runtime symlinks. The new focused assertion should additionally pin `execute-openspec-change` as a first-party `source_type=local`, `source=local (first-party)`, `commit=local`, MIT/allowed entry.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lock/hash と dual-runtime distribution を RED→GREEN で接続する</name>
  <files>tests/test_skills_lock.py, .agents/skills/skills.lock.json, .claude/skills/execute-openspec-change, .codex/skills/execute-openspec-change</files>
  <read_first>
    - `.planning/phases/02-approval-gated-skill-orchestration/02-CONTEXT.md` の D-01、D-11
    - `.agents/skills/execute-openspec-change/SKILL.md` の final bytes from Plan 02-01
    - `.agents/skills/skills.lock.json` の existing first-party entries
    - `scripts/setup-skills.sh` の collision preflight と relative-target contract
    - `tests/test_skills_lock.py` と `tests/test_setup_skills.py`
  </read_first>
  <behavior>
    - RED: an explicit test requires the execute-openspec-change local provenance entry, exact digest, and both relative symlinks, and fails before the lock/distribution artifacts exist.
    - GREEN: the computed SKILL.md SHA-256 is recorded and the existing setup script creates the two canonical relative symlinks without changing the script.
    - REFACTOR: keep the assertion within existing helpers and rerun collision/idempotence regressions.
  </behavior>
  <action>Add one focused assertion to `tests/test_skills_lock.py` for the exact first-party metadata and relative-link targets of `execute-openspec-change`; run it before changing lock or links and commit the expected failure as `test(02-02): add failing execute skill distribution contract`. Compute SHA-256 from the final Plan 02-01 SKILL.md bytes, add one `allowed` local/MIT entry to `skills.lock.json`, then run `bash scripts/setup-skills.sh` so the existing all-conflicts-first implementation creates both symlinks. Verify each link remains a symlink whose literal target is `../../.agents/skills/execute-openspec-change` and resolves to the canonical directory. Run the focused and setup-script suites, then commit GREEN as `feat(02-02): distribute execute OpenSpec change skill`. Do not hand-create symlinks, change `scripts/setup-skills.sh`, copy SKILL.md into runtime roots, or alter any other lock entry.</action>
  <verify>
    <automated>uv run pytest tests/test_skills_lock.py tests/test_setup_skills.py -q</automated>
  </verify>
  <acceptance_criteria>
    - RED `test(02-02)` precedes GREEN `feat(02-02)` and fails for the missing new distribution contract.
    - Lock digest equals the final canonical SKILL.md digest and all existing lock entries remain byte-for-byte unchanged.
    - Both runtime paths are relative symlinks created by the existing setup script and resolve to the same canonical skill.
    - Collision, broken-link repair, and idempotence tests remain green.
  </acceptance_criteria>
  <done>The lock, SHA-256, Claude/Codex symlinks, and focused distribution tests are consistent, with no second distribution implementation.</done>
</task>

<task type="auto">
  <name>Task 2: agent/operator guidance を approval-gated handoff boundary に揃える</name>
  <files>AGENTS.md, docs/agents/workflow.md, docs/optional/gsd.md</files>
  <read_first>
    - `.planning/phases/02-approval-gated-skill-orchestration/02-CONTEXT.md` の D-04〜D-11 と Deferred Ideas
    - canonical `design.md` §5、§10 と `spec.md` preflight scenarios
    - `.agents/skills/execute-openspec-change/SKILL.md`
    - `AGENTS.md` Workflow and Skills sections
    - `docs/agents/workflow.md` 大規模 change の手動 handoff / Skills sections
    - `docs/optional/gsd.md` 大規模 change の手動 handoff / GSD unavailable sections
  </read_first>
  <action>Update only the existing handoff/skill guidance sections. In `AGENTS.md`, name `execute-openspec-change` as the optional automation entry after OpenSpec route approval and the source-pinned reviewable commit; state that the preview displays the deterministic `input_route` label/state but cannot claim a fallback cause, and that explicit approval precedes prepare/GSD dispatch while final completion remains OpenSpec-owned. In `docs/agents/workflow.md`, document the exact operator sequence and one parity payload: change ID, every canonical path, source commit, completed gates, unresolved items, one-phase/one-change, and specification nonduplication are passed via the uninitialized `$gsd-new-project --auto @<brief>` route or inline to the initialized change-specific `$gsd-phase` route. Define acceptance conservatively: host dispatch must return structured completed-success and then read-only evidence must pass. Uninitialized evidence reruns `init progress --raw`, proves fully initialized correct-root/agents state, requires PROJECT/REQUIREMENTS/ROADMAP/STATE, and checks that the produced artifact set collectively preserves the exact payload/brief reference. Initialized evidence compares pre/post snapshots, proves exactly one max+1 phase and directory were added, and requires the new ROADMAP section to preserve the exact inline payload. Prose markers are supplemental only; missing/ambiguous evidence or checkpoints retain prepared.

Document that generic-agent workaround may first use bridge inspect read-only to select the entrypoint, but before approval/prepare must resolve the installed local GSD 1.5.0 entrypoint workflow, every reachable actual spawn name to the active-config TOML and complete preamble, and isolation requirements; any unknown, typed-only, or incompatible isolation requirement fails closed. Document refusal/inspect failure as pre-prepare with no brief/dispatch/state mutation, and non-accepted dispatch as prepared retention with completed-step/manual-continuation reporting. After manifest success, require the report to show manifest path and source commit and instruct the operator to make a distinct later tracking commit; explicitly state that the skill never commits. In `docs/optional/gsd.md`, give the same concise user route, parity payload, observable acceptance, feature-branch retention/source distinction, generic degradation, and manual fallback. All three documents must disclose that normal CI checks static SKILL/fixture instructions and existing Phase 1 dynamic state seams, not actual host orchestration; Phase 3 owns opt-in/manual host evidence. Reference canonical OpenSpec paths/headings and do not promise post-handoff lifecycle, retry, route switching, finalize, cleanup, automatic Git commit, push, PR, or merge.</action>
  <verify>
    <automated>uv run pytest tests/test_execute_openspec_change_skill.py tests/test_skills_lock.py tests/test_setup_skills.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three documents name the same canonical skill and preserve OpenSpec specification/final authority.
    - Operator order is preview, explicit approval, prepared, one initialization-specific dispatch, acceptance, started.
    - Both routes carry the exact parity payload, and acceptance requires structured completion plus the documented route-specific postcondition.
    - Generic dispatch is labeled as a workaround and its fail-closed boundary is explicit.
    - Failure before prepare and non-accepted failure after prepare have distinct, accurate retained-state guidance.
    - Manifest-success guidance names the source pin and later tracking-commit boundary without authorizing an automatic commit.
    - Phase 2 evidence limitations and the Phase 3 opt-in/manual assignment are explicit.
    - No deferred lifecycle-hardening behavior is promised or implied.
  </acceptance_criteria>
  <done>Agent and user guidance consistently routes through the distributed skill and accurately describes approval, dispatch, retention, degradation, and final-authority boundaries.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS Level 1; block_on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical skill bytes -> lock entry | Integrity metadata must identify exactly the executable SKILL.md delivered in Plan 02-01. |
| .agents skill root -> runtime symlinks | Existing user files or wrong/broken links can cause runtime-specific code divergence. |
| executable behavior -> operator guidance | Documentation can accidentally broaden authority or misstate retained state after failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | skills.lock.json digest | mitigate | Compute SHA-256 from final canonical bytes and enforce it in the existing lock test. |
| T-02-07 | Spoofing / Tampering | Claude/Codex skill paths | mitigate | Use the existing collision-first setup script and assert literal relative targets plus resolved identity. |
| T-02-08 | Repudiation | operator guidance | mitigate | State exact approval/state boundaries, generic degradation, source commit, and OpenSpec final authority in all entry documents. |
| T-02-09 | Elevation of Privilege | misleading generic-agent guidance | mitigate | Explicitly deny typed equivalence and require fail-closed behavior where typed dispatch or isolation is necessary. |
| T-02-SC | Tampering | package installs | accept | No dependency or package-manager install exists; distribution is local symlink creation only. |
</threat_model>

<source_coverage_audit>

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Approval-gated skill orchestration on Phase 1 seam | 02-01, 02-02 | COVERED | Behavior then distribution/guidance. |
| REQ | SKILL-01 | Canonical tasks.md §3 pointer at source commit | 02-01, 02-02 | COVERED | Only proxy requirement in this phase. |
| RESEARCH | — | Research hook produced no output | — | EXCLUDED | Explicit onError:skip; no external dependency or novel library. |
| CONTEXT | D-01 | Canonical first-party skill and runtime distribution | 02-01, 02-02 | COVERED | Final bytes precede hash/symlinks. |
| CONTEXT | D-02〜D-10 | Preview label, approval, parity dispatch, observable acceptance, retention, generic workaround | 02-01, 02-02 | COVERED | Guidance mirrors the executable instruction contract without runtime-proof claims. |
| CONTEXT | D-11 | Lock/hash, Claude/Codex symlinks, guidance, contract tests | 02-02 | COVERED | Both tasks. |
| CONTEXT | Deferred | Lifecycle hardening, retry/recovery, finalize/cleanup and external Git actions | — | EXCLUDED | Explicitly excluded from guidance and implementation. |
</source_coverage_audit>

<verification>
1. `uv run pytest tests/test_execute_openspec_change_skill.py tests/test_skills_lock.py tests/test_setup_skills.py -q`
2. `bash scripts/setup-skills.sh` and confirm it reports no changes on the second run.
3. `uv run ruff check tests/test_execute_openspec_change_skill.py tests/test_skills_lock.py tests/test_setup_skills.py`
4. `uv run basedpyright tests/test_execute_openspec_change_skill.py tests/test_skills_lock.py tests/test_setup_skills.py`
5. `task check`
6. Inspect `git log --oneline --grep='(02-02)'` and confirm distribution RED precedes GREEN.
7. Record actual host skill execution, generic spawns, GSD route mutations, and postcondition checks as unverified in Phase 2 and assigned to Phase 3 opt-in/manual evidence.
</verification>

<success_criteria>
- D-11 is satisfied with one canonical skill, exact lock digest, dual relative symlinks, regression coverage, and aligned guidance.
- All Phase 02 static instruction/distribution tests and Phase 1 state-seam regressions pass without requiring GSD in normal CI.
- Guidance preserves deterministic route labels, explicit approval, exact route-parity payload, conservative observable acceptance, generic fail-closed behavior, prepared retention, and OpenSpec final authority.
- Guidance does not claim actual host orchestration was verified in Phase 2 and assigns that evidence to Phase 3.
- No Phase 1 Python/OpenSpec/planning state/roadmap changes or lifecycle hardening are introduced.
</success_criteria>

<output>
Create `.planning/phases/02-approval-gated-skill-orchestration/02-02-SUMMARY.md` when done.
</output>
