---
phase: 02-approval-gated-skill-orchestration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/execute-openspec-change/SKILL.md
  - tests/fixtures/openspec_gsd_handoff/skill/contract.json
  - tests/test_execute_openspec_change_skill.py
autonomous: true
requirements:
  - SKILL-01
must_haves:
  truths:
    - "D-01: execute-openspec-change remains a first-party skill whose canonical implementation is under .agents/skills."
    - "D-02: the skill owns preview, runtime-host inspection, explicit approval, and GSD dispatch while reusing Phase 1 public operations for bridge mechanics."
    - "D-03: skill branches consume structured Phase 1 values and classified codes, never human-readable prose or exit 0 alone."
    - "D-04: skill instruction contract requires one read-only preview of change ID, canonical paths, input_route label/state, source commit, manifest path, capabilities, repository policy, and resolved host dispatch evidence before mutation."
    - "D-05: only a fresh explicit user answer after the complete preview authorizes prepare; prior approval, defaults, and automatic modes do not substitute."
    - "D-06: inspect failure or refusal ends before prepare, brief creation, GSD dispatch, and mark-started and reports classified gaps plus manual handoff guidance."
    - "D-07: the exact frozen preview tuple is replayed to prepare, and no GSD action occurs before structured prepared success."
    - "D-08: both GSD routes receive one parity payload containing change ID, all canonical paths, source commit, completed gates, unresolved items, one-phase/one-change, and specification-nonduplication evidence."
    - "D-09: GSD acceptance requires structured completed-success plus a conservative route-specific read-only probe/filesystem postcondition; failure, checkpoint, missing, or ambiguous evidence retains prepared and never calls mark-started."
    - "D-10: generic dispatch is preflighted against the selected local GSD 1.5.0 workflow, every reachable spawned agent's active-config TOML complete preamble, and isolation requirements before approval/prepare; unknown evidence fails closed."
    - "After manifest success the report names manifest path and source commit and instructs the operator to create a distinct later tracking commit, but the skill never commits automatically."
    - "Phase 2 evidence is explicitly limited to static SKILL/fixture instruction-contract tests plus existing Phase 1 dynamic state-seam tests; actual host orchestration remains unverified until Phase 3 opt-in/manual evidence."
  artifacts:
    - path: .agents/skills/execute-openspec-change/SKILL.md
      provides: first-party approval-gated orchestration instruction contract
    - path: tests/fixtures/openspec_gsd_handoff/skill/contract.json
      provides: ordered stages, parity payload, conservative acceptance, and reporting matrix
    - path: tests/test_execute_openspec_change_skill.py
      provides: static executable-instruction contract checks
  key_links:
    - from: .agents/skills/execute-openspec-change/SKILL.md
      to: src/ai_coding_template_ja/openspec_gsd_handoff/__init__.py
      via: exact inspect_handoff, prepare_handoff, and mark_handoff_started public operations
      pattern: "inspect_handoff|prepare_handoff|mark_handoff_started"
    - from: .agents/skills/execute-openspec-change/SKILL.md
      to: tests/fixtures/openspec_gsd_handoff/gsd/contract.json
      via: local GSD 1.5.0 initialization route and probe contract
      pattern: "init progress|gsd-new-project|gsd-phase"
    - from: tests/test_execute_openspec_change_skill.py
      to: tests/fixtures/openspec_gsd_handoff/skill/contract.json
      via: static ordered-stage and outcome assertions
      pattern: "contract\.json"
---

<objective>
D-01〜D-10 に従い、Phase 1 public seam を再実装せずに利用する first-party `execute-openspec-change` skill の instruction contract を、4つの縦切り RED→GREEN slice で実装する。

Purpose: approval、route parity payload、GSD acceptance、generic-agent degradation、manifest reporting を機械的に監査できる指示へ固定しつつ、prompt behavior を通常 CI で実行検証できるという過剰主張を避けるため。
Output: SKILL.md、local 1.5.0 skill contract fixture、static contract tests。実 host orchestration evidence は Phase 3 に残す。
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-gated-skill-orchestration/02-CONTEXT.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-02-SUMMARY.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-03-SUMMARY.md
@openspec/changes/automate-openspec-gsd-handoff/proposal.md
@openspec/changes/automate-openspec-gsd-handoff/design.md
@openspec/changes/automate-openspec-gsd-handoff/specs/openspec-gsd-handoff-automation/spec.md
@openspec/changes/automate-openspec-gsd-handoff/tasks.md
@src/ai_coding_template_ja/openspec_gsd_handoff/__init__.py
@src/ai_coding_template_ja/openspec_gsd_handoff/__main__.py
@src/ai_coding_template_ja/openspec_gsd_handoff/models.py
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest.py
@src/ai_coding_template_ja/openspec_gsd_handoff/preflight.py
@tests/fixtures/openspec_gsd_handoff/README.md
@tests/fixtures/openspec_gsd_handoff/gsd/contract.json
@tests/fixtures/openspec_gsd_handoff/gsd/handoff-brief.md
@tests/test_handoff_cli.py
@tests/test_handoff_preflight.py
@.agents/skills/tdd/SKILL.md
</context>

<interfaces>
Phase 1 exposes exactly `inspect_handoff`, `prepare_handoff`, and `mark_handoff_started`. `HandoffInspection.manifest` supplies sorted artifact kind/path/hash values, source commit, progress, OpenSpec `input_route`, GSD `project_initialized` and entrypoint, and validated host evidence. The public seam exposes only the deterministic route label/state (`json` or `markdown-fallback`); it does not retain a fallback cause, so the preview must not invent one.

`prepare_handoff` revalidates the same frozen repository/change/source/GSD-home/repository-policy/host tuple and persists only `prepared`. `mark_handoff_started(..., gsd_accepted=True)` is the sole prepared-to-started state transition. Existing Phase 1 tests dynamically verify these state seams; the new skill tests statically verify prompt instructions and fixture parity, not actual host calls.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read-only preview と fresh approval slice を RED→GREEN で固定する</name>
  <files>.agents/skills/execute-openspec-change/SKILL.md, tests/fixtures/openspec_gsd_handoff/skill/contract.json, tests/test_execute_openspec_change_skill.py</files>
  <behavior>
    - RED: static tests require ordered `capture-input`, `inspect-host`, `inspect-bridge`, `resolve-dispatch`, `preview`, and `approve` stages, complete preview fields, deterministic route label only, and zero mutable stages on failure/refusal.
    - GREEN: SKILL.md calls public inspect read-only, freezes the invocation/payload inputs, resolves dispatch evidence, displays preview, then requests a fresh explicit answer.
  </behavior>
  <action>Create the skill contract fixture and static test first. Require the preview to show change ID, every sorted canonical path, `input_route` as the exact deterministic label/state only, source commit, derived manifest path, OpenSpec/GSD capability and initialization values, repository-policy verdict, host schema/dispatch, and any generic degradation. Do not claim a fallback cause because Phase 1 does not expose one. Freeze repository real path, change ID, source commit, GSD home, repository policy, host evidence, completed gates, unresolved items, and canonical paths. Inspect/refusal/no-answer must end before prepare, brief creation, dispatch, or mark-started. Commit RED as `test(02-01): add failing preview approval contract`, implement the slice, then commit GREEN as `feat(02-01): add read-only preview approval gate`.</action>
  <verify><automated>uv run pytest tests/test_execute_openspec_change_skill.py -q -k "preview or approval"</automated></verify>
  <done>Static contract tests prove the instruction ordering and displayed fields; no test claims to execute the prompt in a real host.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prepared gate と route-parity payload slice を RED→GREEN で固定する</name>
  <files>.agents/skills/execute-openspec-change/SKILL.md, tests/fixtures/openspec_gsd_handoff/skill/contract.json, tests/test_execute_openspec_change_skill.py</files>
  <behavior>
    - RED: both route renderings must derive from one fixture payload and preserve every parity field; dispatch remains unreachable until structured prepared success.
    - GREEN: the frozen tuple is replayed to prepare, then uninitialized renders an @brief and initialized renders an inline phase payload with exact parity.
  </behavior>
  <action>Extend the fixture/test before the skill. Define one parity payload with change ID, all canonical paths, source commit, completed boundary gates, unresolved items, one-phase/one-change constraint, and specification-nonduplication evidence. Require public `prepare_handoff` success with state `prepared` before either route. For uninitialized GSD, render the complete parity payload deterministically into the idea document and pass `$gsd-new-project --auto @<brief>`. For initialized GSD, pass the same fields inline to a change-specific `$gsd-phase`; the test compares the inline payload to the common fixture rather than checking selected keywords. Partial initialization cannot reach dispatch. Commit RED as `test(02-01): add failing route parity contract` and GREEN as `feat(02-01): add prepared route parity dispatch`.</action>
  <verify><automated>uv run pytest tests/test_execute_openspec_change_skill.py -q -k "prepared or payload or route"</automated></verify>
  <done>Both route instructions preserve the exact common payload and no GSD action precedes prepared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: conservative acceptance と state transition slice を RED→GREEN で固定する</name>
  <files>.agents/skills/execute-openspec-change/SKILL.md, tests/fixtures/openspec_gsd_handoff/skill/contract.json, tests/test_execute_openspec_change_skill.py</files>
  <behavior>
    - RED: accepted requires structured completed-success and route-specific read-only postconditions; marker-only, checkpoint, empty, malformed, partial, and ambiguous rows retain prepared.
    - GREEN: only the conjunction of host result and postcondition produces `gsd_accepted=True`; all other rows report prepared without retry or route switch.
  </behavior>
  <action>Encode conservative local GSD 1.5.0 acceptance in the fixture and static instruction tests; do not use prose completion markers as sufficient evidence. For the uninitialized route, require host dispatch structured completed-success, then rerun read-only `init progress --raw` and require project/roadmap/state all true, correct repository root, agents installed, empty missing_agents, and existence of PROJECT.md, REQUIREMENTS.md, ROADMAP.md, and STATE.md. Their resulting evidence collectively must preserve the exact change ID, source commit, all canonical paths or the exact brief reference, completed gates, unresolved items, one-change constraint, and specification-nonduplication statement. For initialized route, capture a pre-dispatch read-only snapshot of the maximum integer phase, phase directories, and ROADMAP; after structured completed-success, require a read-only post-snapshot showing exactly one new max+1 phase and matching directory, with no other phase/directory change, and a new ROADMAP section containing the exact inline parity payload. Missing or ambiguous evidence, any checkpoint, or postcondition mismatch leaves prepared and does not call mark-started. Only accepted calls public `mark_handoff_started`. Commit RED as `test(02-01): add failing conservative acceptance contract` and GREEN as `feat(02-01): gate started on observable GSD acceptance`.</action>
  <verify><automated>uv run pytest tests/test_execute_openspec_change_skill.py tests/test_handoff_cli.py -q -k "accept or started or checkpoint or ambiguous"</automated></verify>
  <done>Static prompt tests cover the acceptance matrix and existing Phase 1 dynamic tests cover the actual prepared/started seam; real host orchestration remains explicitly unverified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: generic-agent preflight と manifest report slice を RED→GREEN で固定する</name>
  <files>.agents/skills/execute-openspec-change/SKILL.md, tests/fixtures/openspec_gsd_handoff/skill/contract.json, tests/test_execute_openspec_change_skill.py</files>
  <behavior>
    - RED: generic route requires complete workflow/agent-TOML/isolation evidence before approval; manifest-success report requires path/source/distinct-commit instruction and forbids automatic commit.
    - GREEN: SKILL.md resolves the selected workflow and all reachable agent roles or fails closed, labels the workaround, and emits the non-mutating tracking report.
  </behavior>
  <action>After read-only bridge inspect identifies the selected entrypoint, require generic preflight to resolve the installed local GSD 1.5.0 entrypoint SKILL, its concrete execution workflow under the frozen arguments/config, every reachable Task/Agent spawn name, the active Codex config root mapping for each `agents/<name>.toml`, each complete role preamble, and every isolation requirement. If reachability, TOML mapping/content, or isolation compatibility is unknown, stop before approval/prepare. Typed-only or worktree-isolated requirements also fail closed; otherwise label every result `generic-agent workaround`. Add the report contract: after manifest success, always show manifest path and source commit and instruct the operator to create a distinct later tracking commit; never execute a commit. On non-accepted dispatch also show completed operations, failure point, prepared state, and manual continuation evidence. Commit RED as `test(02-01): add failing generic report contract` and GREEN as `feat(02-01): add generic preflight and manifest report`.</action>
  <verify><automated>uv run pytest tests/test_execute_openspec_change_skill.py -q -k "generic or isolation or report or commit"</automated></verify>
  <done>Generic degradation and manifest tracking instructions are statically auditable, fail closed on unknowns, and add no automatic Git or lifecycle operation.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS Level 1; block_on: high.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing / Repudiation | approval | mitigate | Complete frozen preview plus fresh explicit answer before mutation. |
| T-02-02 | Tampering | route payload | mitigate | One parity fixture renders both @brief and inline phase payload. |
| T-02-03 | Elevation of Privilege | generic dispatch | mitigate | Resolve selected workflow, reachable agents, complete active TOMLs, and isolation before approval; unknown fails closed. |
| T-02-04 | Spoofing / Tampering | acceptance | mitigate | Structured completed-success AND route-specific read-only postcondition; prose is insufficient. |
| T-02-05 | Repudiation | manifest tracking | mitigate | Report path/source and distinct later commit instruction without committing automatically. |
| T-02-SC | Tampering | package installs | accept | No install task exists. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Approval-gated orchestration on Phase 1 seam | 02-01, 02-02 | COVERED |
| REQ | SKILL-01 | Canonical tasks.md §3 at source pin | 02-01, 02-02 | COVERED |
| RESEARCH | — | Interrupted research hook | — | EXCLUDED — explicit onError:skip |
| CONTEXT | D-01〜D-10 | Skill, preview, approval, routes, acceptance, generic workaround | 02-01 | COVERED |
| CONTEXT | D-11 | Distribution and guidance | 02-02 | COVERED |
| CONTEXT | Deferred | Lifecycle hardening/recovery/finalize/cleanup | — | EXCLUDED |
</source_coverage_audit>

<verification>
1. `uv run pytest tests/test_execute_openspec_change_skill.py tests/test_handoff_cli.py tests/test_handoff_preflight.py -q`
2. `uv run ruff check tests/test_execute_openspec_change_skill.py`
3. `uv run basedpyright tests/test_execute_openspec_change_skill.py`
4. Confirm four RED commits each precede its matching GREEN commit.
5. Record as unverified in the summary: actual host prompt execution, generic-agent spawning, real new-project/phase mutation, and route-specific postconditions; assign these to Phase 3 opt-in/manual evidence.
</verification>

<success_criteria>
- Four vertical RED→GREEN slices cover preview/approval, prepare/payload, acceptance/state, and generic/report behavior.
- Static tests prove instruction/fixture consistency; Phase 1 tests prove only the public dynamic state seam.
- No Phase 2 claim says normal CI executed the skill in a real host.
- Source pin, canonical OpenSpec authority, prepared retention, and lifecycle-hardening exclusion remain intact.
</success_criteria>

<output>Create `.planning/phases/02-approval-gated-skill-orchestration/02-01-SUMMARY.md` when done.</output>
