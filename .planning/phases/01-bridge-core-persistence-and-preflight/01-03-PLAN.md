---
phase: 01-bridge-core-persistence-and-preflight
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py
  - tests/test_handoff_discovery.py
  - tests/test_handoff_cli.py
autonomous: true
gap_closure: true
requirements:
  - BRIDGE-01
must_haves:
  truths:
    - "BRIDGE-01 の canonical discovery requirement と source commit に追跡できる CR-10 closure evidence がある。"
    - "OpenSpec JSON candidate に missingArtifacts field が存在するとき、空の string list でも JSON route の classified terminal failure になり Markdown fallback を開始しない。"
    - "ready / blocked の両 empty-list variants は public prepare_handoff で OpenSpec probe 以外の GSD/Git runner call と manifest replace/write を一度も行わず、`.planning/` を生成せず、handoff state を進めない。"
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py
      provides: field-presence-aware terminal candidate classification
    - path: tests/test_handoff_discovery.py
      provides: ready and blocked empty-list route/no-fallback regression evidence
    - path: tests/test_handoff_cli.py
      provides: parameterized ready/blocked public prepare evidence for JSON-route terminal failure, zero downstream preflight, zero manifest mutation, and no `.planning/` creation
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py
      to: tests/test_handoff_discovery.py
      via: candidate field-presence classification is exercised through the public discovery operation with an injected probe and fallback spy
      pattern: "missingArtifacts|openspec-unprepared"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/__init__.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py
      via: prepare_handoff returns the terminal discovery failure before GSD/Git preflight or ManifestRepository persistence
      pattern: "discover_openspec_artifacts"
---

<objective>
Close the single root gap recorded by Phase 1 verification and CR-10 while preserving the source-pinned OpenSpec/GSD boundary.

Purpose: make field presence independent from list truthiness so the canonical OpenSpec terminal signal cannot be hidden by a Markdown fallback or a prepared-manifest write.
Output: a narrow discovery fix plus direct route/no-fallback and public prepare no-mutation regression evidence.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-CONTEXT.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-RESEARCH.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-PATTERNS.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-01-SUMMARY.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-02-SUMMARY.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-REVIEW.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-VERIFICATION.md
@openspec/changes/automate-openspec-gsd-handoff/design.md
@openspec/changes/automate-openspec-gsd-handoff/specs/openspec-gsd-handoff-automation/spec.md
@openspec/changes/automate-openspec-gsd-handoff/tasks.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: missingArtifacts field-presence terminal gate を RED-GREEN で閉じる</name>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py, tests/test_handoff_discovery.py, tests/test_handoff_cli.py</files>
  <read_first>
    - source commit `5a1f78b81f546c900745328fad24f9adb073e768` の canonical `design.md` §9
    - source-pinned canonical `spec.md` の `Requirement: canonical OpenSpec artifactsを正確に発見して読む` / `Scenario: CLIが準備不足を報告する`
    - canonical `tasks.md` § `## 2. Bridge MVP phase`
    - `.planning/phases/01-bridge-core-persistence-and-preflight/01-REVIEW.md` の CR-10
    - `.planning/phases/01-bridge-core-persistence-and-preflight/01-VERIFICATION.md` frontmatter `gaps` と `Gaps Summary`
    - `.planning/phases/01-bridge-core-persistence-and-preflight/01-PATTERNS.md` の adopt-or-discard discovery、fail-closed ordering、injected test seam
    - `src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py` の `_string_list`、`_candidate_shape`、`_candidate_discovery`、`discover_openspec_artifacts`
    - `tests/test_handoff_discovery.py` の injected `OpenSpecProbe` / fixture helpers
    - `tests/test_handoff_cli.py` の `_DispatchRunner`、`_CountingOperations`、`_setup_repository`、`prepare_handoff` public-seam tests
    - `.agents/skills/tdd/SKILL.md`
  </read_first>
  <behavior>
    - RED: parameterized `ready` and `blocked` candidates with `missingArtifacts: []` both return `Failure(code="openspec-unprepared", route=InputRoute.JSON)`; an injected fallback spy proves `_fallback` is never called.
    - RED: parameterize the existing public `prepare_handoff` seam over `ready` and `blocked` candidates with `missingArtifacts: []`; both cases return `Failure(code="openspec-unprepared", route=InputRoute.JSON)`, execute only the two OpenSpec read-only probes, make zero GSD/Git runner calls, make zero manifest replace/write calls, and leave `.planning/` absent.
    - GREEN: validate an empty `missingArtifacts` value as a valid string-list shape without relaxing the non-empty cardinality of proposal/design/tasks/specs, then classify terminal readiness from field presence rather than collection truthiness.
    - REFACTOR: keep candidate adopt-or-discard and route-local builders intact; do not move presentation, preflight, or persistence policy into discovery.
  </behavior>
  <action>Per D-02, D-09, and D-10, trace this closure to BRIDGE-01, source commit `5a1f78b81f546c900745328fad24f9adb073e768`, CR-10, and the single root gap in `01-VERIFICATION.md`; do not restate or broaden the canonical specification. First add the two discovery regressions and one `tests/test_handoff_cli.py` public prepare regression parameterized over both `ready` and `blocked` candidates, each with `missingArtifacts: []`; run the focused tests to capture RED, and commit only those failing tests as `test(01-03): add failing missingArtifacts presence regressions`. In each public case assert the JSON-route `openspec-unprepared` result, exactly the two OpenSpec read-only probe calls and no other runner calls (therefore zero GSD/Git preflight calls), zero `ManifestFileOperations.replace` or other manifest write calls, and no `.planning/` directory creation. `tests/test_handoff_cli.py` is included solely because its existing `_DispatchRunner`, `_CountingOperations`, and public `prepare_handoff` seam are the closest evidence that terminal discovery prevents downstream preflight and persistence; do not add another production seam for this assertion. In `discovery.py`, separate string-list shape from non-empty cardinality with an explicit helper option or equally narrow typed representation: contextFiles proposal/design/tasks/specs keep their existing non-empty rules, while the optional terminal field accepts an empty list only when every present element is a string. Preserve the field-presence fact through candidate validation, and use presence—not list truthiness—to produce the existing `openspec-unprepared` classified JSON-route failure. Keep the supported candidate validation, canonical Markdown read/parity checks, fresh fallback behavior for unsupported candidates, and all unrelated issue codes unchanged. Run focused and Phase 1 suites, then commit the production fix and green regressions as `fix(01-03): honor missingArtifacts field presence`. Do not add dependencies, fixture schema changes, lifecycle hardening, retry, resume, rollback, finalize, cleanup automation, GSD invocation, push, PR, or merge behavior.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_discovery.py tests/test_handoff_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both `ready + []` and `blocked + []` preserve `InputRoute.JSON` and return `openspec-unprepared` without calling the Markdown fallback seam.
    - Empty `missingArtifacts` is accepted only as the optional field's string-list shape; canonical contextFiles arrays remain non-empty and existing contract-table routes remain unchanged.
    - The parameterized public prepare regression proves for both `ready + []` and `blocked + []` that the result is `openspec-unprepared` on `InputRoute.JSON`, the runner records exactly the two OpenSpec probes and zero GSD/Git commands, `ManifestFileOperations.replace` and all other manifest writes remain at zero, and `.planning/` is not created.
    - RED commit precedes GREEN commit, and both focused and complete Phase 1 tests pass before `task check` is accepted.
  </acceptance_criteria>
  <done>CR-10 and the Phase 1 verification root gap are closed by field-presence-aware terminal classification, with both ready/blocked empty-list variants proven through public `prepare_handoff` to preserve JSON-route `openspec-unprepared`, call only the two OpenSpec probes, skip GSD/Git preflight and manifest replace/write, and leave `.planning/` absent.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | In-scope item | Coverage |
|--------|---------------|----------|
| GOAL | `01-VERIFICATION.md` の単一 root gap | Task 1 の RED/GREEN と no-fallback/no-mutation evidence で closure する。 |
| REQ | BRIDGE-01 -> canonical `tasks.md` § `## 2. Bridge MVP phase` | Plan frontmatter、objective、Task 1 trace で source commit と heading を保持する。 |
| RESEARCH | adopt-or-discard discovery、fail-closed ordering、no new dependency | Field presence fix を discovery に限定し、既存 route-local builder と stdlib stack を維持する。 |
| CONTEXT | D-02、D-09、D-10 | Boundary adapter の責務分離、classified result、machine-consumable surface を Task 1 で維持する。 |

CONTEXT のその他の locked decisions は既存 Plan 01-01 / 01-02 で実装済みであり、この gap closure はそれらの production surface、persistence contract、host boundary、atomic transitionを変更しない。Deferred Ideas はすべて除外する。

</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenSpec apply JSON -> candidate validator | An external structured response can encode a terminal field with an empty collection whose presence must not be lost during normalization. |
| validated JSON route -> Markdown fallback | An incorrect truthiness check can downgrade a terminal stop into a fresh route and expose later preflight/persistence. |
| discovery failure -> public prepare operation | The classified stop must propagate before GSD/Git probes and filesystem mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering / Elevation of Privilege | `discovery.py` terminal field handling | mitigate | Preserve key presence independently from its collection contents and assert the existing JSON-route stop code for both candidate states. |
| T-01-11 | Tampering | JSON-to-fallback route boundary | mitigate | Inject a fallback spy and require zero fallback calls for field-present candidates. |
| T-01-12 | Tampering / Repudiation | `prepare_handoff` downstream boundary | mitigate | Exercise the existing public seam with counted commands and filesystem replacement to prove no downstream state change. |
| T-01-SC | Tampering | package installs | accept | This plan adds no dependency or install action and uses only the existing project test toolchain. |
</threat_model>

<verification>
1. `uv run pytest tests/test_handoff_discovery.py tests/test_handoff_cli.py -q`
2. `uv run pytest tests/test_handoff_core.py tests/test_handoff_discovery.py tests/test_handoff_manifest.py tests/test_handoff_preflight.py tests/test_handoff_cli.py -q`
3. `uv run pytest tests/test_handoff_cli.py -q -k 'missing_artifacts and prepare'`
4. `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py tests/test_handoff_discovery.py tests/test_handoff_cli.py`
5. `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/discovery.py tests/test_handoff_discovery.py tests/test_handoff_cli.py`
6. `task check`
</verification>

<success_criteria>
- The single actionable gap from `01-VERIFICATION.md` is covered without changing canonical OpenSpec artifacts or introducing a second requirement definition.
- `ready + []` and `blocked + []` are terminal JSON failures, and unsupported/malformed candidate cases still use their prior clean fallback routes.
- Parameterized public prepare evidence proves for both `ready + []` and `blocked + []` the JSON-route `openspec-unprepared` result, exactly two OpenSpec probes, zero GSD/Git runner calls, zero manifest replace/write calls, and no `.planning/` creation.
- No new dependency or lifecycle hardening/retry/resume/finalize behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-bridge-core-persistence-and-preflight/01-03-SUMMARY.md` when done.
</output>
