---
phase: 03-deterministic-verification-and-acceptance-evidence
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_handoff_discovery.py
  - scripts/validate-handoff-acceptance-evidence.py
  - tests/test_handoff_acceptance_evidence.py
  - docs/optional/gsd.md
  - .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "D-02/D-03: the existing JSON/fallback integration test is extended only with the missing explicit sorted identity/hash/bytes/progress projection; no duplicate route matrix or production behavior is added."
    - "D-04〜D-06: operator guidance names the exact opt-in task, required inputs, success/failure stream contract, read-only command boundary, and normal-CI isolation."
    - "D-07: actual host prompt, generic-agent spawn, real GSD mutation, and route-specific postconditions each have a separate reasoned-unverified row; none is inferred from the real-tool smoke."
    - "D-08: every canonical requirement, scenario, and all 60 spec-hole taxonomy rows map to production, a named test, real smoke evidence, an explicit non-applicable contract, or a reasoned-unverified item."
    - "D-08: a normal-CI validator derives the exact requirement/scenario and spec-hole coordinate order from canonical OpenSpec, then fails closed on missing, duplicate, reordered, empty, unknown, unpinned, leaked, raw-body, or bare-covered evidence."
    - "D-08: validator authority is the evidence header's validated fixed 40-hex source commit and bounded proposal/design/spec blobs read from that commit; worktree spec/design drift cannot redefine coordinates or leakage checks."
    - "D-08: the phase evidence says GSD Phase 3 completion is not OpenSpec final completion and leaves canonical tasks 5.1/5.2 to the main/orchestrator boundary."
  artifacts:
    - path: docs/optional/gsd.md
      provides: explicit read-only smoke operator contract and evidence limitations
    - path: tests/test_handoff_discovery.py
      provides: explicit sorted artifact identity/hash/canonical-byte/progress parity projection over both discovery routes
    - path: scripts/validate-handoff-acceptance-evidence.py
      provides: canonical-derived fail-closed acceptance-evidence validator and CLI
    - path: tests/test_handoff_acceptance_evidence.py
      provides: positive matrix validation and one negative mutation for every validator failure class
    - path: .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md
      provides: source-pinned requirement/scenario/spec-hole implementation and verification traceability
  key_links:
    - from: docs/optional/gsd.md
      to: Taskfile.yml
      via: exact openspec:gsd-handoff:smoke invocation with CHANGE_ID and GSD_HOME
      pattern: "openspec:gsd-handoff:smoke"
    - from: .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md
      to: openspec/changes/automate-openspec-gsd-handoff/specs/openspec-gsd-handoff-automation/spec.md
      via: document-local Rn/Snn coordinates mapped to canonical requirement/scenario order and source commit
      pattern: "R[1-5]-S[0-9]{2}"
    - from: .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md
      to: openspec/changes/automate-openspec-gsd-handoff/design.md
      via: document-local Rn-Hnn coordinates cover every 12-row spec-hole table without creating stable requirement IDs
      pattern: "R[1-5]-H(0[1-9]|1[0-2])"
    - from: scripts/validate-handoff-acceptance-evidence.py
      to: openspec/changes/automate-openspec-gsd-handoff/specs/openspec-gsd-handoff-automation/spec.md
      via: fixed-argv bounded git blob reads at the validated source commit generate the only accepted requirement/scenario coordinate sequence
      pattern: "Requirement:|Scenario:"
---

<objective>
Per D-02、D-03、D-07、D-08, turn existing deterministic tests and the new opt-in smoke into complete, source-pinned acceptance evidence without redefining OpenSpec.

Purpose: Give the final OpenSpec boundary a mechanically countable map of what is verified, observed by real read-only tools, non-applicable, or still unverified for a concrete safety reason.
Output: operator documentation and Phase 3 acceptance-evidence matrix with the actual opt-in result.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-deterministic-verification-and-acceptance-evidence/03-CONTEXT.md
@.planning/phases/03-deterministic-verification-and-acceptance-evidence/03-01-SUMMARY.md
@.planning/phases/01-bridge-core-persistence-and-preflight/01-VERIFICATION.md
@.planning/phases/02-approval-gated-skill-orchestration/02-VERIFICATION.md
@.planning/phases/02-approval-gated-skill-orchestration/02-SECURITY.md
@openspec/changes/automate-openspec-gsd-handoff/proposal.md
@openspec/changes/automate-openspec-gsd-handoff/design.md
@openspec/changes/automate-openspec-gsd-handoff/specs/openspec-gsd-handoff-automation/spec.md
@openspec/changes/automate-openspec-gsd-handoff/tasks.md
@tests/fixtures/openspec_gsd_handoff/README.md
@tests/fixtures/openspec_gsd_handoff/openspec/contract.json
@tests/fixtures/openspec_gsd_handoff/gsd/contract.json
@tests/fixtures/openspec_gsd_handoff/manifest/expected-prepared.json
@tests/fixtures/openspec_gsd_handoff/skill/contract.json
@tests/test_handoff_core.py
@tests/test_handoff_discovery.py
@tests/test_handoff_manifest.py
@tests/test_handoff_preflight.py
@tests/test_handoff_cli.py
@tests/test_execute_openspec_change_skill.py
@tests/test_handoff_smoke.py
@docs/optional/gsd.md
@.agents/skills/spec-holes/SKILL.md
@.agents/skills/verify-change/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canonical-derived acceptance evidence validatorをRED→GREEN実装する</name>
  <files>scripts/validate-handoff-acceptance-evidence.py, tests/test_handoff_acceptance_evidence.py</files>
  <read_first>
    - source-pinned canonical specification requirement/scenario heading hierarchy and canonical design R1〜R5 12-row spec-hole tables
    - source commit `5a1f78b81f546c900745328fad24f9adb073e768` and all four canonical artifact paths in PROJECT/CONTEXT
    - `scripts/openspec-validate-gate.py` thin validation CLI pattern and `tests/test_smoke.py` importlib/negative gate test pattern
    - `.agents/skills/tdd/SKILL.md`; agreed seam is validator input bytes plus canonical source files to one structured verdict
  </read_first>
  <behavior>
    - RED: after validating one lowercase 40-hex source commit, bounded fixed-argv Git blob reads supply proposal/design/spec bytes at that commit; their headings derive exactly ordered `R1..R5`, 26 `Rn-Snn`, and unique `R1-H01..R5-H12` coordinates.
    - RED: valid evidence requires the exact source commit and fixed canonical proposal/design/spec/tasks paths, one row per pinned-derived coordinate in exact order, an allowed nonempty evidence/disposition kind and locator/reason, and exactly ordered HOST-UNVERIFIED-1..4 rows with no-safe-dry-run reasons.
    - RED: worktree design/spec drift does not alter pinned coordinates or raw-body leakage baselines; evidence written against drift instead of pinned blobs fails.
    - RED: SHA/path injection, unknown SHA, missing/non-zero/oversized/non-UTF-8/malformed pinned blobs, missing/duplicate/reordered/unknown rows, empty evidence, unsupported dispositions, absolute home path, pinned raw canonical body, fenced raw output, and bare `covered` each return one non-zero fail-closed code.
    - GREEN: one injected bounded Git-blob seam, stdlib parser/validator, and thin CLI implement those contracts without changing canonical OpenSpec or inventing stable requirement IDs.
  </behavior>
  <action>Write table-driven negative tests before the validator. Parse the evidence metadata first and require the source commit to match lowercase `[0-9a-f]{40}` and then equal the fixed authority SHA `5a1f78b81f546c900745328fad24f9adb073e768`; return `source-pin-mismatch` for any other well-shaped SHA. Require proposal/design/spec/tasks paths to equal four implementation constants and do not interpolate an evidence-supplied path into Git argv. Through an injected runner using `shell=False`, repository cwd, timeout, separate stderr, and fixed argv `git show &lt;validated-sha&gt;:&lt;fixed-path&gt;`, read only the pinned proposal, design, and single capability spec blobs. Bound each stdout to 1 MiB plus one detection byte and all three to the canonical 4 MiB aggregate, reject truncation, and require UTF-8; command non-zero (including when the otherwise-correct fixed SHA is unknown in a test repository), missing blob, oversized output, decoding error, or structurally malformed pinned Markdown fails before evidence-row acceptance. These are canonical artifact input bounds and remain separate from Plan 03-01's repo-wide streaming snapshot resource bounds. Do not require worktree equality and do not use worktree proposal/design/spec files as authority. The tasks path remains required in the evidence header, but do not compare worktree `tasks.md` with its pinned blob because progress checkboxes are intentionally updated during implementation.

Parse the pinned specification blob's heading hierarchy to derive requirement coordinates `R1` through `R5` and per-requirement scenario coordinates in source order; require exactly 5 requirements and 26 scenarios while using that parsed sequence as the comparison source. Parse the pinned design blob's `### Rn:` spec-hole tables and require row numbers exactly 1 through 12 in order, yielding the unique full sequence `R1-H01` through `R5-H12`; use pinned proposal/design/spec non-heading body lines for leakage detection. Define a simple evidence row schema with coordinate, allowed evidence kind, nonempty locator, and nonempty reason/disposition where applicable. Allow only explicit kinds such as `production-test`, `fixture-test`, `property-test`, `real-smoke`, `reasoned-unverified`, and `canonical-non-applicable`; reject unknown/empty values and the bare word `covered` case-insensitively. Require exactly four ordered `HOST-UNVERIFIED-1..4` rows for actual host prompt, generic-agent spawn, real GSD mutation, and the route-postcondition family, each with a no-safe-dry-run reason. Reject POSIX `/home/`, macOS `/Users/`, Windows user-profile absolute paths, fenced raw command/probe output, and pinned canonical raw body leakage by comparing normalized evidence lines/cells against pinned non-heading body lines of 32 or more characters while allowing headings/paths as references. Return stable structured codes for source-commit-invalid, source-pin-mismatch, canonical-paths-mismatch, pinned-blob-command-failed, pinned-blob-output-limit, pinned-blob-utf8-invalid, canonical-source-invalid, coordinate-missing/duplicate/order/unknown, evidence-kind-invalid, evidence-empty, host-unverified-invalid, absolute-path-leak, raw-output-forbidden, canonical-body-leak, and bare-covered. The CLI prints one constant-bounded sorted-key JSON verdict and exits non-zero on any failure.

Tests start from valid pinned blob fixtures plus one valid minimal evidence matrix, then mutate one condition per failure class. Include explicit tests that modify worktree `design.md` and `spec.md` while the injected/pinned Git blobs remain fixed: pinned-correct evidence stays valid and drift-authored coordinates/body evidence fails. Add pre-run injection assertions proving an invalid/mixed-case/option-like/extra-argument SHA or any noncanonical evidence path reaches zero Git calls, plus unknown SHA, one missing blob, non-zero result, limit-plus-one stdout, non-UTF-8, and malformed pinned heading/table negatives.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_acceptance_evidence.py -q</automated>
  </verify>
  <done>Normal pytest proves canonical-derived exact ordering/counts and every fail-closed metadata, row, evidence, unverified, leakage, raw-body, and bare-covered condition before the final matrix is authored.</done>
</task>

<task type="auto">
  <name>Task 2: opt-in smokeのoperator contractと証拠限界を文書化する</name>
  <files>docs/optional/gsd.md</files>
  <action>Per D-04〜D-06, add one focused subsection near the existing handoff/GSD compatibility guidance. Document exact invocation `task openspec:gsd-handoff:smoke CHANGE_ID=automate-openspec-gsd-handoff GSD_HOME=<active-config-root>`, required exact versions, stdout one-object JSON/stderr human-summary contract, and the non-zero result for absent/mismatched tools or signals. Enumerate the allowed read-only OpenSpec/GSD probes and the ignored-inclusive repo-wide before/after snapshot that excludes only `.git`; explicitly say the task does not approve, prepare, persist a manifest, create a brief, dispatch either GSD entrypoint, or mark started. State that `task check` runs fake/fixture tests only and does not depend on this task. Per D-07, distinguish installed-tool compatibility from actual host prompt, generic spawning, real mutation, and route-postcondition evidence. Use placeholders rather than a user home or repository absolute path, and do not copy canonical requirements or Markdown content.</action>
  <verify>
    <automated>rg -q 'openspec:gsd-handoff:smoke' docs/optional/gsd.md &amp;&amp; rg -q 'task check' docs/optional/gsd.md &amp;&amp; rg -q '未検証' docs/optional/gsd.md</automated>
  </verify>
  <done>Operator documentation gives one exact safe invocation, observable output/exit semantics, normal-CI separation, prohibited mutations, and honest host-level evidence limits.</done>
</task>

<task type="auto">
  <name>Task 3: canonical acceptanceとspec-holes Phase 2の全対応表を作成し実smokeを記録する</name>
  <files>tests/test_handoff_discovery.py, .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md</files>
  <read_first>
    - canonical specification: 5 requirement headings and 26 scenario headings in source order
    - canonical design `Spec holes Phase 1`: R1〜R5 each with rows 01〜12, including explicit non-applicable dispositions
    - all Phase 1/2 verification reports and named tests before deciding that a new integration/property test is needed
    - `tests/test_handoff_discovery.py::test_positive_json_and_fallback_share_values_but_keep_distinct_routes`: `Discovery` equality already includes artifact content, content_bytes, SHA-256, and normalized Progress; route is stored separately, but a named sorted identity/hash projection is not explicit yet
    - `.agents/skills/spec-holes/SKILL.md` Phase 2 output discipline and `.agents/skills/verify-change/SKILL.md`
  </read_first>
  <action>Per D-02 and D-03, first run the focused Phase 1/2/new-smoke tests and mechanically inventory canonical headings. Extend the existing positive JSON/fallback integration test rather than adding another fixture matrix: derive an independently sorted tuple of `(kind, path, sha256, content_bytes)` for each route, assert those tuples and normalized progress are identical, and retain the distinct route assertion. This is evidence-only test work; if the assertion is already green, record the expected-green audit result and do not modify production code. Create the source-pinned evidence artifact in the exact schema owned by Task 1's validator; its `R1`〜`R5`, `Snn`, and `Hnn` labels are document-local coordinates only, not stable IDs or replacement specifications. Populate exactly the validator-derived 5 requirement rows, 26 scenario rows, and 60 spec-hole rows in canonical order. Every row points to production plus a named test/fixture, the actual real-tool smoke observation, an explicit canonical non-applicable disposition, or a precise reasoned-unverified item; never use a bare `covered` claim. Run `task check:without-gsd` to obtain actual normal-gate evidence under isolated temporary HOME/CODEX_HOME/GSD_HOME and curated PATH with node/OpenSpec/GSD unavailable, then run the explicit smoke against this change and the active GSD root. Record only exit status, exact versions, redacted command templates, `json` route, relative artifact identities/hashes/progress, GSD initialized/signals, bounded snapshot counts/digest, and mutation verdict; copy no absolute path, raw probe JSON, or canonical Markdown body. A failed opt-in is not rewritten as success: retain its classified code and stop the plan for verifier review. Per D-07, add exactly four reasoned-unverified rows for actual host prompt, generic-agent spawn, real GSD mutation, and the route-specific postcondition family (covering both route variants) because no read-only/dry-run host seam exists. Per D-08, finish with an authority statement that this evidence enables but does not perform OpenSpec tasks 5.1/5.2 and that GSD phase completion is not OpenSpec final completion. Run the validator CLI last; any non-zero verdict blocks completion.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_discovery.py tests/test_handoff_acceptance_evidence.py -q &amp;&amp; uv run --no-sync python scripts/validate-handoff-acceptance-evidence.py --repository . --evidence .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md</automated>
  </verify>
  <done>All canonical requirement/scenario/spec-hole coordinates have concrete evidence or an explicit disposition, the actual read-only smoke result is recorded without local-path/content leakage, and unsafe host observations remain visibly unverified.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS Level 1; block_on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source-pinned Git blobs -> evidence matrix | Worktree drift, malicious commit/path metadata, missing blobs, or unbounded Git output may omit or reinterpret authoritative requirements. |
| Test/smoke output -> tracked documentation | Raw local paths, tool output, or Markdown bodies may disclose environment data. |
| Phase evidence -> final-completion decision | GSD evidence can be mistaken for OpenSpec final authority or unsafe host E2E proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering / Repudiation | acceptance mapping | mitigate | Validate lowercase 40-hex SHA/fixed paths, read bounded proposal/design/spec blobs with fixed argv and no shell, then derive exact unique 5/26/60 ordering from pinned bytes despite worktree drift. |
| T-03-08 | Information Disclosure | smoke evidence capture | mitigate | Validator rejects absolute user paths, raw/fenced output, and copied canonical body; evidence retains redacted commands and relative identities/digests only. |
| T-03-09 | Spoofing | host-level acceptance claims | mitigate | Validator requires four separate reasoned-unverified rows and no-safe-dry-run reasons; read-only tool compatibility cannot prove host behavior. |
| T-03-10 | Elevation of Privilege | final authority / scope | mitigate | State that OpenSpec tasks 5.1/5.2 and final completion remain outside GSD phase authority; exclude hardening and lifecycle operations. |
| T-03-SC | Tampering | package installs | accept | No package installation or dependency update exists in this plan. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | acceptance-ready deterministic/real/unverified evidence | 03-01, 03-02 | COVERED |
| REQ | VERIFY-01 | canonical tasks.md §4 at source pin | 03-01, 03-02 | COVERED |
| RESEARCH | — | Explicitly skipped: no external choice/dependency; repository contracts are pinned | — | EXCLUDED |
| CONTEXT | D-01 | normal CI remains optional-tool independent | 03-01, 03-02 | COVERED |
| CONTEXT | D-02 | audit and fill only actual gaps | 03-02 | COVERED |
| CONTEXT | D-03 | route-only parity distinction over exact values | 03-02 | COVERED |
| CONTEXT | D-04 | dedicated opt-in exact-tool task | 03-01, 03-02 | COVERED |
| CONTEXT | D-05 | probe scope and mutable entrypoint prohibition | 03-01, 03-02 | COVERED |
| CONTEXT | D-06 | machine/human summary and redaction | 03-01, 03-02 | COVERED |
| CONTEXT | D-07 | unsafe host evidence reasoned unverified | 03-01, 03-02 | COVERED |
| CONTEXT | D-08 | full canonical mapping, OpenSpec final authority | 03-02 | COVERED |
| CONTEXT | Deferred | destructive E2E, hardening, lifecycle, push/PR/merge | — | EXCLUDED |
</source_coverage_audit>

<verification>
1. `uv run pytest tests/test_handoff_core.py tests/test_handoff_discovery.py tests/test_handoff_manifest.py tests/test_handoff_preflight.py tests/test_handoff_cli.py tests/test_execute_openspec_change_skill.py tests/test_handoff_smoke.py tests/test_taskfile.py -q`
2. `task check:without-gsd` and retain its successful isolated-subprocess verdict.
3. `task openspec:gsd-handoff:smoke CHANGE_ID=automate-openspec-gsd-handoff GSD_HOME="$HOME/.codex"` and inspect stdout/stderr for exact versions, `json` route, relative-only artifact/progress evidence, initialized GSD signal, and no mutation.
4. `uv run --no-sync python scripts/validate-handoff-acceptance-evidence.py --repository . --evidence .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md`.
5. `! rg -n -F '/home/' .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md &amp;&amp; ! rg -n -F '/Users/' .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md &amp;&amp; ! rg -n -F ':\Users\' .planning/phases/03-deterministic-verification-and-acceptance-evidence/03-ACCEPTANCE-EVIDENCE.md` and rely on the validator for raw-body/bare-covered/coordinate failures.
6. `git diff --check` and confirm the evidence artifact does not modify canonical OpenSpec files or mark tasks 5.1/5.2.
</verification>

<success_criteria>
- Existing deterministic tests are reused; the only added parity assertion is the explicit sorted identity/hash/bytes/progress projection missing from D-03 evidence.
- Real read-only smoke passes exact OpenSpec/GSD compatibility and mutation checks, with redacted structured/human evidence.
- The fail-closed normal-CI validator derives and enforces all 5 requirements, 26 scenarios, and 60 spec-hole rows; four unsafe host observations are separately unverified for the no-safe-dry-run reason.
- The phase claims only GSD implementation/verification completion and leaves OpenSpec acceptance/final completion to the main boundary.
- No hardening, retry/resume/rollback/finalize/cleanup, push, PR, merge, or real GSD mutation is performed.
</success_criteria>

<output>Create `.planning/phases/03-deterministic-verification-and-acceptance-evidence/03-02-SUMMARY.md` when done.</output>
