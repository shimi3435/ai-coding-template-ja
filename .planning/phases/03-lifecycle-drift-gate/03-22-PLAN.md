---
phase: 03-lifecycle-drift-gate
plan: 22
type: tdd
wave: 16
depends_on:
  - 03-21
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
  - tests/test_handoff_migration.py
  - tests/test_handoff_manifest_refresh.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: migration and refresh serialize each change's manifest writers with one change-directory lock held from final validation through rename and parent durability."
    - "A target update injected after the locked internal target validation but before rename is preserved byte-for-byte, and apply_manifest_refresh returns structured state-guard non-success."
    - "Lock contention, lock-integrity failure, and target mismatch never install the candidate or retry; valid exact approval, no-op, staging, and persistence-failure behavior remain unchanged."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      provides: "Shared change-directory writer-lock token and lock-required conditional replace_at contract"
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
      provides: "Refresh lock lifetime and state-guard classification for contention or replacement-boundary mismatch"
    - path: tests/test_handoff_manifest_refresh.py
      provides: "Fixed post-validation/pre-rename mutation and lock-contention integration regressions"
    - path: tests/test_handoff_migration.py
      provides: "Migration participation and contention coverage for the shared writer lock"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      via: "apply_manifest_refresh acquires the shared target-parent writer lock before its final guard, passes the live lock token plus old_target_sha256 into replace_at, and releases only after rename durability or classified failure"
      pattern: "acquire_writer_lock_at|replace_at|old_target_sha256|refresh-state-changed-at-replace"
---

<objective>
Close the final-guard-to-replace lost-update race for HND-03 / HARD-R2.

Purpose: Serialize every in-scope writer across the final-validation-to-rename critical section and preserve target bytes changed inside that boundary.
Output: One change-scoped writer-lock/conditional-replacement contract shared by refresh and migration, plus fixed integration evidence.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-17-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-21-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
@tests/test_handoff_migration.py
@tests/test_handoff_manifest_refresh.py

<interfaces>
Preserve `preview_manifest_refresh`, `apply_manifest_refresh`, `apply_manifest_migration`, exact preview approval, staging identity validation, no-follow bounded reads, valid no-op/apply results, and existing failure enums. Extend `ManifestMigrationFileOperations` as the sole shared persistence authority with change-directory writer-lock acquisition/release, a live lock token, and lock-required expected-target-hash `replace_at` outcomes; do not add another persistence or writer-lock authority.
</interfaces>
</context>

## Artifacts this phase produces

- No new module, package export, dependency, schema, evidence format, tracked lock file, retry, or recovery authority.
- New private lock token/outcome types in `manifest_migration.py`; the lock is an exclusive non-blocking `flock` on a duplicated descriptor for the already anchored `.planning/openspec/<change_id>` target directory, so its inode is the repository/change-scoped synchronization identity.
- Modified operations contracts: acquire/release the writer-lock token, require that live token plus `expected_target_sha256` in `replace_at`, and expose one test seam after the first locked target validation but before rename.
- New stable issue-code values: `refresh-writer-lock-unavailable`, `migration-writer-lock-unavailable`, and `refresh-state-changed-at-replace`; retain existing `migration-state-changed-at-replace`.
- New fixed test nodes:
  - `tests/test_handoff_manifest_refresh.py::test_apply_preserves_target_mutated_after_locked_validation_before_rename`
  - `tests/test_handoff_manifest_refresh.py::test_refresh_and_migration_writers_contend_on_one_change_directory_lock`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Later operations rely on fail-closed approval-bound persistence. | 03-22 | COVERED | Concurrent target state cannot be silently lost. |
| REQ | HND-03 / HARD-R2 | Approval-relevant evidence remains bound through the effect. | 03-22 | COVERED | Fixed integration evidence covers the write boundary. |
| RESEARCH | — | Preserve exact approval, bounded no-follow persistence, and fixed I/O-race evidence. | 03-22 | COVERED | No property family or dependency is added. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Operator approved planning without it. |
| VERIFICATION | gap 2 / CR-02 | Target change after final guard is overwritten with Success. | 03-22 | COVERED | One change-directory lock spans final validation through rename; fixed integration injection proves fail-closed preservation inside that boundary. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Protect final validation through rename with one shared writer lock</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py (`_MigrationDirectoryAnchor`, `ManifestMigrationFileOperations`, `before_replace_at`, `replace_at`, `_apply_anchored_manifest_migration`, `apply_manifest_migration`)
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py (`apply_manifest_refresh`, `_failure_after_staging`, final state guard and replace call)
    - tests/test_handoff_migration.py (all writer-lock/`replace_at` operation adapters and migration replace/failure tests)
    - tests/test_handoff_manifest_refresh.py (`MutationRecordingRefreshOperations`, `FaultInjectingRefreshOperations`, `DriftAtReplaceRefreshOperations`, apply race/failure tests)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py, src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py, tests/test_handoff_migration.py, tests/test_handoff_manifest_refresh.py</files>
  <behavior>
    - RED: a fixed refresh operations adapter reaches a hook after the first internal target validation while the shared writer lock is held, proves an independent non-blocking lock attempt is rejected, raw-writes concurrent bytes before rename, and receives `refresh-state-changed-at-replace` with exact concurrent bytes preserved and validated staging removed.
    - RED: a separately held change-directory lock makes refresh and migration return their exact writer-lock-unavailable state-guard failures without `os.replace`, retry, candidate installation, or target-byte change.
    - GREEN: valid exact refresh and migration applies acquire the same directory-inode lock in the declared order, retain it through atomic rename and parent-directory durability, and release it in `finally`; existing no-op and replace-failure classifications remain unchanged.
  </behavior>
  <action>RED: add a fixed `AfterLockedValidationTargetMutationOperations` adapter at the shared operations seam. Its post-validation/pre-rename hook must (1) attempt a second exclusive non-blocking `flock` on a separately opened descriptor for the same target-parent directory and record contention, proving the first lock is live; (2) bypass the cooperative writer contract only for the test by raw-writing a distinct valid concurrent byte sequence to the target; and (3) return to the real `replace_at`. Add `test_apply_preserves_target_mutated_after_locked_validation_before_rename` and assert exact `ManifestRefreshFailure`, code `refresh-state-changed-at-replace`, `RefreshFailurePoint.STATE_GUARD`, target state UNKNOWN, validated staging cleanup REMOVED, no candidate installation, byte-for-byte concurrent target preservation, no staging residue, and no rename event. This is a fixed filesystem integration test, not a Hypothesis property. Run it and require current Success/lost-update behavior; commit `test(03-22): expose mutation after locked target validation`.

Also RED-test cooperative contention with a real separately held directory lock. Open the same anchored `.planning/openspec/<change_id>` target directory twice, hold the first exclusive lock, and drive refresh and migration through their public apply seams with the second descriptor. Require exact `refresh-writer-lock-unavailable` / `migration-writer-lock-unavailable`, STATE_GUARD, no rename, exact original target bytes, classified staging cleanup, no retry, and lock release after the holder closes. This test proves both writers use the same repository/change-scoped lock identity rather than per-instance or per-process mutexes.

GREEN: add a private lock token whose ownership is one duplicated target-parent directory descriptor plus its device/inode. `acquire_writer_lock_at` must duplicate the already no-follow-anchored target-parent descriptor, verify the duplicate is the same directory identity, and take `fcntl.flock(..., LOCK_EX | LOCK_NB)`; contention, unsupported/failed locking, token-integrity failure, or parent-identity mismatch is structured non-success, never an unlocked fallback. `release_writer_lock` must unlock and close exactly once in `finally`; the directory entry itself is not created, deleted, or treated as evidence.

Use this exact acquisition/lifetime order in both apply paths: approval and repository/target-parent anchoring; candidate staging creation/write/reread/validation; acquire the one change-directory writer lock; while holding it rerun the existing final current-preview/source/target/parent checks; call `replace_at` with the live token and `preview.old_target_sha256` or `preview.v1_sha256`; perform atomic rename and existing parent-directory durability; then release in `finally` on success or every failure. No-op returns before staging/locking because it performs no write. Never hold two writer locks, acquire a writer lock before repository/target anchoring, or call a second migration/refresh writer while holding one.

Make `replace_at` reject a missing, released, wrong-directory, or mismatched lock token before effect. Under the live lock, retain staging descriptor identity checks; no-follow open and bounded-read the target; require its exact expected SHA-256; invoke the post-validation/pre-rename test seam; then revalidate the same target entry/hash before `os.replace`. The second check covers the deliberately non-cooperative raw mutation injected by the fixed test; the held directory lock closes the real migration/refresh writer window across both checks and rename. Return a private outcome distinguishing REPLACED, TARGET_CHANGED, and LOCK_UNAVAILABLE; actual filesystem exceptions remain in the existing REPLACE failure branch. Refresh maps TARGET_CHANGED to new `refresh-state-changed-at-replace`; migration maps it to existing `migration-state-changed-at-replace`; each maps lock failure to its exact writer-lock-unavailable state-guard code and cleans validated staging without overwriting target bytes.

Update every operations adapter in both test files to forward the lock token, expected hash, acquisition/release calls, outcome, and hook without bypassing the shared authority. Run the named mutation/lock-contention nodes, exact apply/no-op/fault matrices, full refresh/migration files, Ruff, and BasedPyright; commit `feat(03-22): serialize handoff writers through rename`.

REFACTOR only if demonstrated; otherwise record no REFACTOR commit. Keep filesystem/I/O evidence fixed and add no Hypothesis family, package, schema, retry, repair, rollback, or route switch.</action>
  <acceptance_criteria>
    - The fixed adapter mutates the target after the first locked internal target validation and before rename, proves the shared lock is held at injection time, and the candidate is never installed.
    - Concurrent bytes remain byte-for-byte exact; result is state-guard non-success with UNKNOWN target state, classified validated-staging cleanup, no rename, and no residue.
    - A real separately held change-directory lock blocks both refresh and migration with their exact structured lock-unavailable results; neither path retries or writes.
    - The lock scope is the anchored `.planning/openspec/<change_id>` directory inode; ownership is the private duplicated-descriptor token; acquisition order and `finally` lifetime are identical in both writers and cover final validation, conditional replace, rename, and parent durability.
    - Refresh and migration both pass a live token and previewed target hash into one `replace_at` authority; all adapter signatures and fault matrices remain green.
    - Valid exact apply/no-op, preview identity, protected subtrees, public manifest schema, and approval hash contract remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_refresh.py::test_apply_preserves_target_mutated_after_locked_validation_before_rename tests/test_handoff_manifest_refresh.py::test_refresh_and_migration_writers_contend_on_one_change_directory_lock tests/test_handoff_manifest_refresh.py::test_apply_exact_preview_stages_validates_and_atomically_replaces tests/test_handoff_migration.py -q</automated>
  </verify>
  <done>Every migration/refresh writer shares one change-directory critical section from final validation through durable rename, and a mutation injected after internal validation is preserved and classified instead of overwritten.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| final target observation → replacement effect | Mutable target state may change after the caller's final guard unless all in-scope writers share one lock lifetime. |
| staging entry → target path | Only validated staging and the expected target may be renamed. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-22-RACE | Tampering/Denial of Service | high | writer-lock token, both apply paths, `replace_at` | mitigate | Lock the anchored change-directory inode before final validation through rename/durability in every migration/refresh writer; fixed post-validation/pre-rename injection and contention tests require exact preservation and state-guard failure. |
| T-03-22-LOCK | Spoofing/Elevation of Privilege | high | lock acquisition/token validation | mitigate | Bind a private token to one duplicated anchored-directory descriptor/device/inode, reject wrong/released tokens and lock failures without fallback, never acquire multiple writer locks, and release exactly once in `finally`. |
| T-03-22-STAGING | Tampering | high | staging descriptor/rename | mitigate | Retain no-follow staging identity validation, bounded reads, cleanup classification, and existing fault matrices. |
| T-03-22-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_manifest_refresh.py::test_apply_preserves_target_mutated_after_locked_validation_before_rename tests/test_handoff_manifest_refresh.py::test_refresh_and_migration_writers_contend_on_one_change_directory_lock tests/test_handoff_manifest_refresh.py::test_apply_exact_preview_stages_validates_and_atomically_replaces -q`
- `uv run pytest tests/test_handoff_manifest_refresh.py tests/test_handoff_migration.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py tests/test_handoff_migration.py tests/test_handoff_manifest_refresh.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py tests/test_handoff_migration.py tests/test_handoff_manifest_refresh.py`
- `git diff --check`
</verification>

<success_criteria>
- Verification gap 2 and review CR-02 are closed exactly once.
- The reproduced post-validation/pre-rename lost-update case is structured non-success with concurrent bytes preserved byte-for-byte.
- Both public writers demonstrably contend on the same anchored change-directory lock and never fall back to an unlocked effect.
- Fixed I/O evidence remains fixed; no property family, dependency, schema, or recovery behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md` when done.
</output>
