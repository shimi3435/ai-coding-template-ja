---
phase: 03-lifecycle-drift-gate
plan: 05
type: tdd
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
requirements:
  - HND-03
gap_closure: true
must_haves:
  truths:
    - "HND-03 / HARD-R2: host capability evidence is complete only when host.inspected is exactly true; an uninspected host is never admitted."
    - "Every nested ArtifactLimits field is a positive exact integer before repository or boundary observation begins."
    - "Malformed and duplicate phase graph declarations become lifecycle-phase-observation-incomplete before normalization."
    - "Expected and observed phase dependency graphs must both be acyclic; any cycle is unknown and never admitted."
    - "Valid set-like phase ordering remains identity-invariant after raw validation succeeds."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      provides: "Fail-closed capability, nested-limit, raw phase-shape, uniqueness, and DAG validation"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Public-gate regressions for uninspected host, malformed limits, malformed/duplicate graphs, and cycles"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: LifecycleObservationBoundary
      via: "`_valid_limits` rejects malformed nested bounds before boundary calls"
      pattern: "_valid_limits|lifecycle-input-invalid"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: PhaseGraphObservation
      via: "raw graph validation precedes canonical ordering and DAG validation"
      pattern: "_validate_phase_nodes|_normalize_phase_nodes|lifecycle-phase-observation-incomplete"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: CapabilityObservation
      via: "host.inspected participates in completeness and drift comparison"
      pattern: "_validate_capabilities|_capability_changes"
---

<objective>
Make every lifecycle admission prerequisite structurally complete before normalization, comparison, or repository reads for HND-03 / HARD-R2.

Purpose: Close the fail-open and crash paths caused by uninspected capabilities, malformed nested bounds, malformed/duplicate phase evidence, and cyclic graphs.
Output: Hardened lifecycle gate validation and public-gate regressions for verification gaps 1, 2, 3, and 7.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-02-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve `LifecycleGateLimits`, `PhaseNodeObservation`, `PhaseGraphObservation`, `CapabilityObservation`, `observe_lifecycle_operation`, and `gate_lifecycle_operation`. Enforce the research order at the public gate: raw limits/capability/phase validation, then canonical graph normalization, then comparison and decision projection; identity binding remains downstream in Plan 03-06. Invalid caller limits return `lifecycle-input-invalid`; incomplete phase evidence returns `lifecycle-phase-observation-incomplete`; incomplete capability evidence returns `lifecycle-capability-observation-incomplete`. Unknown public decisions remain non-admitted and expose no decision identity or remediation evidence. `policy_reference.py::_valid_limits` and the existing phase helpers are closest analogs, not exhaustive validators; the no-exact-analog DAG case is authorized as one local bounded iterative helper.
</interfaces>
</context>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py`
  - modified public behavior: `observe_lifecycle_operation`
  - modified public behavior: `gate_lifecycle_operation`
  - hardened validation for `LifecycleGateLimits.artifact_limits`
  - hardened phase graph validation for `PhaseGraphObservation.expected_nodes` and `observed_nodes`
  - hardened capability completeness for `CapabilityObservation.capabilities.host.inspected`
- `tests/test_handoff_lifecycle_gate.py`
  - new public-gate fail-closed regression matrices for gaps 1, 2, 3, and 7
- `.planning/phases/03-lifecycle-drift-gate/03-05-SUMMARY.md`
  - RED/GREEN/REFACTOR evidence and validation-order proof

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | The shared lifecycle decision is fresh and fail-closed. | 03-05 | COVERED | All four prerequisite families stop before admission when incomplete. |
| REQ | HND-03 / HARD-R2 | Capability and phase-state evidence must be complete and bounded. | 03-05 | COVERED | Reuses the existing public gate and issue-code contract. |
| VERIFICATION | Gap 1 | `host.inspected=False` can be clean/admitted. | 03-05 | COVERED | Exact-true completeness plus drift comparison defense. |
| VERIFICATION | Gap 2 | Malformed/duplicate phase evidence can crash or normalize clean. | 03-05 | COVERED | Raw validation precedes normalization. |
| VERIFICATION | Gap 3 | Malformed nested ArtifactLimits can raise TypeError. | 03-05 | COVERED | Every nested integer is validated before reads. |
| VERIFICATION | Gap 7 | Cyclic phase graphs can be clean/admitted. | 03-05 | COVERED | Expected and observed graphs must be DAGs. |
| RESEARCH | G1, G3 | Exact-true host completeness and every outer/nested positive exact-integer bound are checked before observation. | 03-05 | COVERED | Reuses capability and `policy_reference.py::_valid_limits` patterns without coercion or exception recovery. |
| RESEARCH | G2, G7 | Raw phase shape, types, uniqueness, bounds, and DAG validity precede normalization. | 03-05 | COVERED | Uses one bounded iterative DAG helper because PATTERNS identifies no exact validator analog. |
| VALIDATION | 03-05-01, 03-05-02 | Nyquist requires the two named focused public-gate matrices and the valid-order backstop. | 03-05 | COVERED | Task commands match `03-VALIDATION.md` exactly. |
| PATTERNS | admission validation | Exact types, one shared validator, and unknown empty projection remain the established contracts. | 03-05 | COVERED | Partial analogs guide implementation order without limiting the required raw checks. |
| CONTEXT | — | No Phase 3 CONTEXT.md exists. | — | EXCLUDED | Continuation without context was explicitly approved. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed on uninspected capabilities and malformed nested limits</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - .planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_gate.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/reader.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - Current `host.inspected=False` returns unknown, not admitted, with `lifecycle-capability-observation-incomplete`.
    - A complete comparison where expected and observed `host.inspected` differ cannot remain clean; `host.inspected` is part of capability drift comparison.
    - Invalid `max_files`, `bytes_per_file`, `bytes_total`, or `change_id_bytes` values return `lifecycle-input-invalid` before any boundary call or artifact read.
  </behavior>
  <action>RED: add public `gate_lifecycle_operation` regressions for an uninspected host and a parameter table covering each nested `ArtifactLimits` field with wrong type, boolean, zero, and negative values; assert no exception and no admission. Include a complete comparison case proving `host.inspected` is represented by the explicit capability comparison when manifest and complete current evidence differ. GREEN: perform raw public-input validation before root resolution, manifest read, or boundary use. Require `capabilities.host.inspected is True` for current observation completeness, retain `host.inspected` in `_capability_changes` as defense-in-depth, and make `_valid_limits` validate `max_files`, `bytes_per_file`, `bytes_total`, and `change_id_bytes` with `type(value) is int and value > 0`. Follow `policy_reference.py::_valid_limits` for the exact-positive-integer pattern, while retaining every gate-specific outer and nested bound not present in that partial analog. REFACTOR: consolidate positive-integer checks without coercion and preserve existing issue codes. Do not turn incomplete capability evidence into ordinary drift and do not catch a downstream type error as the primary control path.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py -q -k "uninspected_host or malformed_nested_limits or host_inspected_drift"</automated>
  </verify>
  <acceptance_criteria>
    - `host.inspected=False` never returns clean or admitted.
    - All four nested fields reject wrong-type, boolean, zero, and negative inputs with `lifecycle-input-invalid`.
    - Boundary source, phase, and capability call counters remain zero for invalid limits.
    - A valid complete host-inspection mismatch is represented as capability drift rather than silently omitted.
  </acceptance_criteria>
  <done>Capability completeness and nested bounds are enforced before any admission-relevant observation proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate raw phase graphs and reject dependency cycles</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_gate.py
    - .planning/phases/03-lifecycle-drift-gate/03-02-SUMMARY.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - A `None` node, invalid node field type, non-tuple dependency collection, duplicate node ID, or duplicate dependency returns unknown without raising.
    - Duplicate dependencies are rejected before `_utf8_sorted` can deduplicate them.
    - Two-node and longer cycles in either expected or observed graph return `lifecycle-phase-observation-incomplete` and are never admitted.
    - Reordering valid nodes and dependency tuples remains semantically irrelevant and preserves decision identity.
  </behavior>
  <action>RED: add a public-gate malformed graph table for invalid node objects and duplicate dependencies, plus explicit two-node and longer-cycle cases applied independently to expected and observed graphs. Confirm current behavior either raises or admits. GREEN: preserve the ordered trust boundary: validate the raw `PhaseGraphObservation` tuple containers, every `PhaseNodeObservation`, every string field, dependency tuple and member, node/dependency uniqueness, edge count, aggregate byte count, self/unknown edges, and DAG acyclicity before calling `_normalize_phase_nodes`; only after validity is proven may comparison/projection consume the canonical order. Implement one bounded iterative Kahn-style helper using the already validated node IDs and edges. PATTERNS explicitly marks `_downstream_phases` as a reachability-only partial analog, so do not use its termination as acyclicity proof; the absence of an exact DAG helper is not an API-surface prohibition. Do not recurse without a depth bound and do not infer or drop edges. REFACTOR: keep one graph validator shared by expected and observed values and preserve `lifecycle-phase-observation-incomplete` for all malformed/cyclic graph cases.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py -q -k "malformed_phase_graph or duplicate_phase_edge or cyclic_phase_graph or irrelevant_phase_tuple_order"</automated>
  </verify>
  <acceptance_criteria>
    - Every malformed, duplicate, and cyclic graph case returns unknown, not admitted, with no reusable identity.
    - No malformed case raises `AttributeError`, `TypeError`, or recursion failure.
    - Both expected and observed graphs receive the same DAG and bounds validation.
    - The existing valid order-invariance identity test remains green.
  </acceptance_criteria>
  <done>Phase graph evidence is proven well-formed, unique, bounded, and acyclic before canonical ordering or admission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller limits → repository/boundary readers | Malformed nested bounds can trigger exceptions or disable resource limits. |
| host probe → capability completeness | A type-valid but unperformed host inspection can masquerade as complete evidence. |
| phase observation boundary → graph normalization | Malformed, duplicate, or cyclic declarations can be erased or bypass dependency ordering. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-05-01 | Spoofing/Elevation of Privilege/Denial of Service | high | host capability admission and nested `ArtifactLimits` | mitigate | Require `host.inspected is True`, include it in capability comparison, validate every nested field as a positive exact integer before repository resolution or boundary calls, and prove public non-admission with zero boundary calls. |
| T-03-05-02 | Tampering/Elevation of Privilege/Denial of Service | high | phase graph normalization and dependency validity | mitigate | Validate raw types, uniqueness, bounds, and both DAGs before ordering so malformed, duplicate, or cyclic evidence cannot normalize into clean. |
| T-03-05-SC | Tampering | low | package supply chain | accept | No package install or dependency change is in scope. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `git diff --check`
</verification>

<success_criteria>
- Verification gaps 1, 2, 3, and 7 are closed through `gate_lifecycle_operation`.
- Uninspected, malformed, duplicate, over-limit, and cyclic inputs never contribute green evidence.
- Valid phase ordering remains deterministic and identity-invariant.
- No canonical artifact, tracked handoff, mapping implementation, package export, or CLI surface is modified.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-05-SUMMARY.md` with RED/GREEN/REFACTOR evidence, exact issue codes, boundary call-count proof, and DAG regression results.
</output>
