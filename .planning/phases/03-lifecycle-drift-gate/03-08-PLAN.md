---
phase: 03-lifecycle-drift-gate
plan: 08
type: tdd
wave: 7
depends_on:
  - 03-06
  - 03-07
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
  - tests/test_handoff_execution_mapping.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: source and capability commit values are exact validated strings before regex use, and malformed values yield the dimension-specific unknown lifecycle decision."
    - "PlanningInventory outer scalars, collection containers, members, and nested fields are completely runtime-validated before byte accounting, cross-change checks, sorting, iteration, or attribute access."
    - "Mapping construction/readiness and the lifecycle phase boundary use one safe authoritative PlanningInventory validator."
    - "Every malformed boundary value returns admitted=false with empty partial evidence and decision_identity=null rather than terminating with an exception or reusing stale identity."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      provides: "One safe public PlanningInventory validator shared by all mapping consumers"
      exports:
        - validate_planning_inventory
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      provides: "Validate-before-dereference source, phase inventory, and capability boundary checks"
    - path: tests/test_handoff_execution_mapping.py
      provides: "Fixed authoritative-validator and mapping-readiness malformed-inventory regressions"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Fixed public-gate regressions for malformed boundary commits and inventory values"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      via: "_validate_phase_graph calls the authoritative PlanningInventory validator before reading change_id or phase members"
      pattern: "validate_planning_inventory"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      via: "read, build, and readiness paths all delegate to the same validated declaration seam"
      pattern: "validate_planning_inventory"
    - from: tests/test_handoff_lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "FakeBoundary injects malformed values and assertions consume only gate_lifecycle_operation"
      pattern: "gate_lifecycle_operation"
---

<objective>
Close the remaining lifecycle-boundary validation gap for HND-03 / HARD-R2 and finish Phase 3 verification.

Purpose: Make external observation results safe to consume by ensuring commits and the complete planning inventory are authoritatively validated before regex, byte accounting, iteration, sorting, attribute access, or admission identity generation.
Output: One shared PlanningInventory validation seam, hardened lifecycle boundary checks, fixed mapping/public-gate regressions, and full Phase 3/project verification.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-02-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-05-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-06-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-07-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
@src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_execution_mapping.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Add only `validate_planning_inventory(value: object) -> Result[PlanningInventory]` as the module-level authoritative validation seam in `execution_mapping.py`. It must validate the complete in-memory aggregate safely, return the original immutable value on success, and return a structured `Failure` on malformed input. `read_planning_inventory`, `build_manifest_mappings`, `validate_mapping_readiness`, and the lifecycle phase boundary must delegate to it rather than call an unsafe or duplicated subset. Preserve `gate_lifecycle_operation(...)` as the public admission test seam and preserve the existing dimension-specific lifecycle issue codes. Do not add a root-package export, serializer, CLI operation, dependency, evidence schema change, or property family.
</interfaces>
</context>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py`
  - public module-level `validate_planning_inventory`
  - safe validation order for every `PlanningInventory` collection and declaration family
  - shared use by inventory read, mapping construction, and mapping readiness
- `src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py`
  - exact-string commit checks before regex matching
  - authoritative inventory validation before phase-boundary dereference
  - unchanged dimension-specific unknown decision projection
- `tests/test_handoff_execution_mapping.py`
  - fixed outer/container/member/field regressions for the authoritative inventory seam and readiness
- `tests/test_handoff_lifecycle_gate.py`
  - fixed source/capability commit and planning-inventory public-gate regressions
- `.planning/phases/03-lifecycle-drift-gate/03-08-SUMMARY.md`
  - executor-produced RED/GREEN/REFACTOR evidence, focused/full lifecycle results, and `task check` result

<source_audit>

| SOURCE | ID | Feature / Requirement | Task / Must-have / Verify | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation consumes the same fresh fail-closed decision. | Tasks 1-2; truths 1-4; final lifecycle suites | COVERED | All boundary-returned values stop safely at the shared public gate. |
| REQ | HND-03 / HARD-R2 | Failed or incomplete inspection becomes unknown and stops. | Tasks 1-2; truths 1-4 | COVERED | No malformed boundary result admits or receives identity. |
| RESEARCH | — | Validate nested runtime values before dereference and avoid parallel admission/validation authorities. | Tasks 1-2 actions | COVERED | One inventory validator is consumed by mapping and lifecycle boundaries. |
| CONTEXT | — | No Phase 3 CONTEXT.md; continuation from research, requirements, and verification was approved. | — | EXCLUDED | No locked decisions or deferred ideas exist for this phase. |
| VERIFICATION | GAP-2.1 | Validate source and capability commits as exact strings before regex use. | Task 2 behavior/action/acceptance | COVERED | Both reproduced TypeError families receive public-gate regressions. |
| VERIFICATION | GAP-2.2 | Validate PlanningInventory outer scalars, exact tuple containers, member types, and nested fields before attribute access. | Task 1 behavior/action/acceptance | COVERED | Every declaration family and policy observation receives fixed cases. |
| VERIFICATION | GAP-2.3 | Use the same safe inventory validation from lifecycle phase boundary and mapping readiness. | Task 1 integration plus Task 2 key link | COVERED | Read/build/readiness/gate consumers share the named seam. |
| VERIFICATION | GAP-2.4 | Add public-gate regressions for non-tuple collections, None members, and malformed nested fields with dimension-specific unknown, admitted=false, and identity=null. | Task 2 behavior/verify/acceptance | COVERED | Fixed `FakeBoundary` rows cover source, phase, and capability dimensions. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish one safe authoritative PlanningInventory validator</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
    - tests/test_handoff_execution_mapping.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py, tests/test_handoff_execution_mapping.py</files>
  <behavior>
    - RED: `validate_planning_inventory` rejects a non-PlanningInventory outer value and malformed exact-string `version` or `change_id` fields without raising.
    - RED: each of `phases`, `assignments`, `plans`, `evidence`, and `policy_observations` rejects a non-tuple container, an over-limit tuple, and a `None` or wrong-class member before iteration or attribute access.
    - RED: fixed cases reject malformed fields for PhaseDeclaration, PhaseAssignment including its policy-reference tuple and members, PlanDeclaration, EvidenceDeclaration including optional owner fields, and PolicySectionObservation before regex, path, byte, sort, or cross-change operations.
    - RED: `build_manifest_mappings` and `validate_mapping_readiness` return structured mapping failure for the same malformed inventories and never expose partial mappings/readiness.
    - GREEN: valid parsed and in-memory inventories retain existing mapping construction and operation-readiness behavior.
    - REFACTOR: inventory read, build, and readiness paths share the named validator and no second partial declaration authority remains.
  </behavior>
  <action>Write fixed public-validator and mapping-readiness regressions first and commit RED as `test(03-08): expose unsafe planning-inventory validation`. Add `validate_planning_inventory(value: object) -> Result[PlanningInventory]` and reorganize the existing declaration checks into safe stages: require the outer class; validate exact scalar fields; require every collection to be an exact bounded tuple; require the correct member class for every collection; validate every nested scalar, optional scalar, and nested tuple/member type; only then perform byte accounting, regex/path checks, cross-change checks, uniqueness, sorting, and relationship invariants. Include `PolicySectionObservation` runtime field validation within the aggregate boundary before any mapping consumer can traverse it. Make `read_planning_inventory`, `build_manifest_mappings`, and `validate_mapping_readiness` call this single seam and preserve their established public failure vocabulary. Commit GREEN as `feat(03-08): centralize safe planning-inventory validation`. Review after GREEN for duplicate `_validate_declarations` call paths; if consolidation changes production code, commit it separately as `refactor(03-08): unify inventory validation consumers`, otherwise record that no refactor commit was warranted. Use fixed examples only and do not add Hypothesis coverage.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_execution_mapping.py -q -k "planning_inventory_runtime_validation or readiness_rejects_malformed_inventory"</automated>
  </verify>
  <acceptance_criteria>
    - The named validator returns `Failure`, never an exception, for every fixed outer/container/member/field family.
    - No collection member is dereferenced before its runtime class is validated, and no string operation is applied before exact string validation.
    - Inventory read, mapping construction, and mapping readiness all use the same authority.
    - Valid 49-item baseline, plan/execute/verify/finalize readiness, and existing mapping failure codes remain green.
  </acceptance_criteria>
  <done>All PlanningInventory consumers share one safe complete validation authority that runs before nested dereference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed on malformed lifecycle boundary commits and inventories</name>
  <read_first>
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_execution_mapping.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: non-string source-commit evidence returns `lifecycle-source-commit-observation-incomplete`, not `TypeError`.
    - RED: non-string capability commit evidence returns `lifecycle-capability-observation-incomplete`, not `TypeError`.
    - RED: a phase observation containing each malformed PlanningInventory outer/container/member/field family returns `lifecycle-phase-observation-incomplete`, including the reproduced `phases=(None,)` case.
    - RED: every malformed row returns `UNKNOWN`, `admitted=False`, empty artifact paths/source IDs/remediation targets/action codes, no progress candidate, and `decision_identity is None`.
    - GREEN: all validation completes before regex, inventory iteration, phase-path comparison, mapping readiness, sorting, or identity generation; valid complete boundary values retain existing clean/drifted/stale results.
    - REFACTOR: `gate_lifecycle_operation` remains the sole production admission authority.
  </behavior>
  <action>Add fixed `FakeBoundary` public-gate rows first and commit RED as `test(03-08): expose malformed lifecycle boundary values`. In `_validate_source_commit` and `_validate_capabilities`, require every outer identity field used by equality or regex, especially `source_commit`, to be an exact string before the operation. In `_validate_phase_graph`, validate the phase observation scalars and nodes, then call `validate_planning_inventory` before reading `planning_inventory.change_id`, iterating phases, comparing paths, or invoking mapping readiness. Preserve the existing dimension-specific issue-code projection and unknown constructor so rejected values are non-admitted with empty evidence and no identity. Commit GREEN as `feat(03-08): validate lifecycle boundary values before use`. Perform a final behavior-preserving review for duplicated inventory checks or broad exception handling; commit a separate refactor only if production code changes. Do not regenerate lifecycle evidence, change identity encoding, add retry/repair/route-switch behavior, or run the optional real-tool smoke.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py -q -k "malformed_boundary_commit or malformed_boundary_inventory"</automated>
  </verify>
  <acceptance_criteria>
    - The reproduced source commit, capability commit, and `phases=(None,)` counterexamples return their exact lifecycle observation-incomplete decisions without exceptions.
    - Non-tuple inventory collections, wrong members, and malformed nested fields produce the same fail-closed public shape.
    - No rejected row reaches mapping readiness or decision identity generation.
    - Existing operation matrix, drift remediation, progress projection, same-root identity, cross-root rejection, and stale-decision tests remain green.
  </acceptance_criteria>
  <done>Every malformed source, phase inventory, and capability boundary value stops through the public lifecycle gate without fail-open behavior or exception termination.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Risk threshold: any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| lifecycle observation boundary → gate validators | External inspection implementations can return malformed complete-looking dataclasses. |
| PlanningInventory → mapping readiness and phase comparison | Nested collections and declarations can violate annotations and must not be traversed before validation. |
| validated observation → lifecycle decision identity | Partial or stale evidence must not mint or reuse an admission identity. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-08-01 | Tampering | high | malformed boundary structured input | mitigate | Exact outer/container/member/field validation precedes regex, byte accounting, iteration, sorting, and attribute access. |
| T-03-08-02 | Elevation of Privilege | high | fail-open mapping/lifecycle readiness | mitigate | One authoritative inventory validator feeds mapping construction, readiness, and lifecycle phase admission; malformed results become dimension-specific unknown. |
| T-03-08-03 | Denial of Service | high | exception termination at boundary | mitigate | Fixed public regressions cover non-string commits, non-tuple collections, `None` members, and malformed nested fields without broad exception swallowing. |
| T-03-08-04 | Spoofing/Replay | high | stale identity reuse | mitigate | Malformed observations are non-admitted and receive no identity, while existing fresh recomputation and constant-time prior-identity comparison remain unchanged. |
| T-03-08-SC | Tampering | low | package supply chain | accept | No package install or dependency update occurs. |

</threat_model>

<verification>
- Focused authoritative inventory validation: `uv run pytest tests/test_handoff_execution_mapping.py -q -k "planning_inventory_runtime_validation or readiness_rejects_malformed_inventory"`
- Focused public lifecycle boundary validation: `uv run pytest tests/test_handoff_lifecycle_gate.py -q -k "malformed_boundary_commit or malformed_boundary_inventory"`
- All lifecycle suites: `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py tests/test_handoff_execution_mapping.py -q`
- Static analysis: `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py`
- Type checking: `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py`
- Full project gate: `task check`
- `git diff --check`
- Confirm no new `@given` family, dependency, root-package export, CLI operation, evidence JSON change, or optional real-tool smoke result was introduced.
</verification>

<success_criteria>
- All four GAP-2 missing bullets in `03-VERIFICATION.md` have fixed public-seam evidence.
- The two `gaps_remaining` families are covered across 03-07 and 03-08, and every malformed fixed row returns unknown, non-admitted, empty evidence, and no identity.
- Focused mapping and lifecycle regressions, all three lifecycle suites, static analysis, and `task check` pass.
- HND-03 / HARD-R2 is no longer blocked by incomplete nested canonical or boundary validation.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-08-SUMMARY.md` when done.
</output>
