---
phase: 03-lifecycle-drift-gate
plan: 24
type: tdd
wave: 17
depends_on:
  - 03-22
files_modified:
  - .planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
  - tests/test_handoff_manifest_refresh.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "preview_manifest_refresh returns refresh-input-invalid in IssueCategory.INPUT for None, int, and arbitrary object current_source_commit values before repository or filesystem work."
    - "The Phase 03 GSD record states that the 03-22 guarantee covers bridge-owned writers and cooperating writers using the same change-directory advisory lock protocol."
    - "The GSD record states that a hash recheck is defense-in-depth, not a guarantee against a non-cooperating writer that changes the target after final observation; Phase 03 adds no CAS-like persistence protocol."
    - "The existing issues_found review remains untouched, and existing Plan 03-23 resumes only after this gap closes so its fresh independent review can rejudge CR-01 using the clarified boundary."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
      provides: "Exact-string admission before regex evaluation and filesystem observation"
    - path: tests/test_handoff_manifest_refresh.py
      provides: "Fixed public-seam regressions for None, int, and arbitrary object source commits"
    - path: .planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md
      provides: "Append-only clarification of the completed writer-lock guarantee boundary"
  key_links:
    - from: tests/test_handoff_manifest_refresh.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
      via: "public preview_manifest_refresh calls with malformed current_source_commit values and filesystem-forbidden probes"
      pattern: "preview_manifest_refresh|refresh-input-invalid|IssueCategory.INPUT"
    - from: .planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md
      to: .planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md
      via: "the fresh independent review required by 03-23 consumes the preserved 03-22 history plus its explicit advisory-lock boundary clarification"
      pattern: "cooperating writers|advisory lock|final observation|fresh independent review"
---

<objective>
Close WR-01 at the public refresh-preview input boundary and record the exact 03-22 writer-lock guarantee boundary without changing canonical OpenSpec.

Purpose: Keep malformed caller values fail-closed before filesystem work while ensuring the next independent reviewer judges CR-01 against the canonical point-in-time/advisory-lock scope rather than an atomic persistence guarantee.
Output: One RED-to-GREEN public-seam regression slice, one exact-string guard, and one append-only GSD history clarification.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-22-PLAN.md
@.planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
@tests/test_handoff_manifest_refresh.py

<interfaces>
The confirmed public test seam is `preview_manifest_refresh`. Preserve its signature and `Result[ManifestRefreshPreview]` contract. Malformed `current_source_commit` values return `Failure(ClassifiedIssue(IssueCategory.INPUT, "refresh-input-invalid", KnownState.UNKNOWN))` before regex evaluation, repository resolution, Git calls, or `ManifestRefreshFileOperations` work. Preserve every valid-string preview behavior and all completed 03-22 migration/refresh writer-lock behavior.
</interfaces>
</context>

## Artifacts this plan produces

- No new public API, issue code, enum, package, schema, migration, persistence authority, lock protocol, retry, rollback, repair, or route switch.
- No canonical OpenSpec edit and no execution-time change to `03-REVIEW.md`, `03-23-PLAN.md`, ROADMAP, STATE, REQUIREMENTS, or the completed 03-22 implementation/tests; planning has already registered 03-24 ahead of 03-23.
- One fixed public regression node in `tests/test_handoff_manifest_refresh.py` covering three explicit runtime values: `None`, an integer, and a fresh arbitrary object.
- One append-only clarification in `03-22-SUMMARY.md`; prior accomplishments, decisions, commits, verification results, and TDD evidence remain unchanged.

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on the same fresh, fail-closed drift decision. | 03-24 | COVERED | Malformed refresh-preview caller input stops before observation work; review resumes only after closure. |
| REQ | HND-03 / HARD-R2 | Incomplete or malformed observation input cannot become reusable approval evidence. | 03-24 | COVERED | Exact structured input failure at the existing public preview seam. |
| RESEARCH | — | Validate untrusted structured input before attribute use, normalization, comparison, or filesystem work; use fixed public examples and no new dependency. | 03-24 | COVERED | Three fixed examples, one existing seam, no property family or dependency. |
| CONTEXT | UD-01 | Guaranteed refresh writers are bridge-owned writers and cooperating writers using the same change-directory advisory lock protocol. | 03-24 | COVERED | Append-only 03-22 boundary clarification. |
| CONTEXT | UD-02 | Non-cooperating writers changing the target after final observation are out of scope; hash recheck is defense-in-depth; no Phase 03 CAS-like persistence. | 03-24 | COVERED | Clarification only; no persistence code change. |
| CONTEXT | UD-03 | Canonical OpenSpec remains authoritative and unchanged. | 03-24 | COVERED | OpenSpec paths are read-only context and excluded from files_modified. |
| CONTEXT | UD-04 | CR-01 is an over-broad interpretation, not an implementation defect; preserve the existing review for fresh rejudgment. | 03-24 | COVERED | No REVIEW edit; 03-23 fresh review remains the decision gate. |
| CONTEXT | UD-05 | WR-01 requires exact-string-before-regex and structured refresh-input-invalid / INPUT behavior for None, int, and object before filesystem effects. | 03-24 | COVERED | Dedicated RED-to-GREEN public-seam task. |
| CONTEXT | UD-06 | Do not rebuild completed 03-22 implementation/tests and do not expand migration scope. | 03-24 | COVERED | Only preview admission code/new regression and summary clarification change. |
| REVIEW | WR-01 | Non-string current_source_commit leaks TypeError. | 03-24 | COVERED | Exact public return code/category and zero-work order are regression-tested. |
| REVIEW | CR-01 | Reviewer assumed guarantees for non-cooperating writers after final observation. | 03-24 | COVERED | Correct the GSD boundary record, retain the report, and require fresh independent rejudgment. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject non-string source commits before regex and filesystem work</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md (`WR-01` only; retain the file unchanged)
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py (`_HEX_40`, `_valid_limits`, `ManifestRefreshFileOperations`, `preview_manifest_refresh` admission block)
    - tests/test_handoff_manifest_refresh.py (`MutationRecordingRefreshOperations`, `_preview`, existing read-only preview/input-boundary tests)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py, tests/test_handoff_manifest_refresh.py</files>
  <behavior>
    - RED case None: public `preview_manifest_refresh` returns the exact structured input failure instead of raising `TypeError`, and no repository/filesystem probe is reached.
    - RED case int: the same public seam returns code `refresh-input-invalid`, category `IssueCategory.INPUT`, and known state `KnownState.UNKNOWN` before any observation work.
    - RED case object: a fresh arbitrary object receives the same exact result, with no implicit coercion, regex call, Git call, read, staging, mutation, or filesystem operation.
    - GREEN valid-control: a valid exact 40-character lowercase hexadecimal string continues through the existing preview path and retains existing behavior.
  </behavior>
  <action>RED: add one fixed parameterized public regression named `test_preview_rejects_non_string_source_commit_before_filesystem_work`, with explicit IDs for `None`, integer, and arbitrary-object cases. Call `preview_manifest_refresh` directly rather than a private helper or regex. Supply otherwise valid preview arguments, plus test probes that fail the test if repository resolution or any `ManifestRefreshFileOperations` filesystem boundary is reached. For every case assert an exact `Failure`, issue code `refresh-input-invalid`, `IssueCategory.INPUT`, and `KnownState.UNKNOWN`; assert the probe counters remain zero. Run only this node and require RED because the current regex call raises `TypeError` before producing the structured result. Commit `test(03-24): reject non-string refresh source commits`.

GREEN: in the initial `preview_manifest_refresh` admission condition, test `type(current_source_commit) is not str` before calling `_HEX_40.fullmatch`. Keep `_valid_limits` first and retain the existing code/category. Do not coerce values with `str`, broaden acceptance with `isinstance`, catch the regex exception later, or move repository/filesystem work ahead of admission. Do not change `_source_pin_matches`, `apply_manifest_refresh`, migration code, writer-lock code/tests, persistence, schema, or public signatures. Run the new node, the existing read-only preview/input-boundary tests, then the full refresh test file; commit `feat(03-24): validate refresh source commit type`.

REFACTOR only if a behavior-preserving cleanup is demonstrated and the same tests remain green; otherwise record that no REFACTOR commit was needed.</action>
  <acceptance_criteria>
    - `None`, one integer, and one fresh arbitrary object are separate fixed examples through public `preview_manifest_refresh`.
    - Each case returns exact `Failure` code `refresh-input-invalid`, category `IssueCategory.INPUT`, and known state `KnownState.UNKNOWN`; no exception escapes.
    - Exact type validation occurs before `_HEX_40.fullmatch`, repository resolution, Git/subprocess work, reads, staging, or mutation, and the test's filesystem-forbidden probes remain untouched.
    - Valid exact-string preview behavior and every completed 03-22 writer-lock/apply regression remain green.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_refresh.py::test_preview_rejects_non_string_source_commit_before_filesystem_work -q &amp;&amp; uv run pytest tests/test_handoff_manifest_refresh.py -q</automated>
  </verify>
  <done>The public preview seam fails closed with the exact structured INPUT result for all three wrong runtime types before filesystem work.</done>
</task>

<task type="auto">
  <name>Task 2: Append the 03-22 advisory-lock guarantee boundary and preserve review history</name>
  <read_first>
    - AGENTS.md (OpenSpec/GSD authority boundary)
    - .planning/phases/03-lifecycle-drift-gate/03-22-PLAN.md (completed writer-lock scope)
    - .planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md (append-only historical record)
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md (`CR-01`; retain unchanged)
    - .planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md (fresh independent review is the first exit task)
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md (`HARD-R2` final-observation scope around lines 164-173, 350-358, and 611-617; read-only authority)
  </read_first>
  <files>.planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md</files>
  <action>Before editing, require `03-22-SUMMARY.md` to be clean relative to HEAD, capture its exact tracked pre-execution bytes with `git show HEAD:.planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md`, and record the SHA-256 of that snapshot in the 03-24 execution evidence. Append a short dated `Post-review guarantee boundary clarification` to `03-22-SUMMARY.md`; do not rewrite existing sections or claims. After appending, require the current file bytes to be strictly longer and to start with the complete tracked snapshot byte-for-byte, so any replacement, deletion, or in-place rewrite fails even when all keywords remain. State exactly that the 03-22 guaranteed writer set is (a) bridge-owned migration/refresh writers and (b) cooperating writers that follow the same change-directory advisory lock protocol. State that the target-hash recheck is defense-in-depth for detectable interference, not an atomic guarantee against a non-cooperating writer that changes target bytes after final observation. State that such post-observation non-cooperating writes are outside the canonical HARD-R2/Phase 03 guarantee and that this phase introduces no CAS-like persistence protocol. Record that these lines clarify the scope of the completed implementation rather than change OpenSpec, its acceptance criteria, or 03-22 implementation evidence.

Preserve `03-REVIEW.md` byte-for-byte as historical issues_found evidence. Record in the clarification that CR-01 must be rejudged by the fresh independent reviewer required at the start of existing Plan 03-23, using canonical OpenSpec plus this boundary; do not hand-edit, delete, rename, suppress, or mark the current finding resolved. After Task 1 and this clarification are committed and verified, stop gap execution and hand control back to existing 03-23. Its Task 1 fresh clean independent review is required before Tasks 2-3, HND-03 completion, or Phase 4 unblocking.</action>
  <acceptance_criteria>
    - Existing 03-22 summary history is preserved as the exact complete prefix of the post-edit file, and the new section names both guaranteed writer classes and their shared advisory-lock protocol.
    - The clarification distinguishes defense-in-depth hash rechecking from a guarantee after final observation and explicitly records the non-cooperating-writer boundary and absence of CAS-like Phase 03 persistence.
    - Canonical OpenSpec, `03-REVIEW.md`, and `03-23-PLAN.md` are unchanged.
    - Existing 03-23 is not resumed until 03-24 passes; once resumed, its fresh independent review rejudges CR-01 before any security, traceability, verification, or Phase 4 completion step.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from pathlib import Path; import hashlib,subprocess; path='.planning/phases/03-lifecycle-drift-gate/03-22-SUMMARY.md'; old=subprocess.run(['git','show',f'HEAD:{path}'],check=True,stdout=subprocess.PIPE).stdout; current=Path(path).read_bytes(); assert len(current)&gt;len(old); assert current.startswith(old); required=(b'bridge-owned',b'cooperating writers',b'advisory lock protocol',b'defense-in-depth',b'final observation',b'non-cooperating',b'CAS-like',b'fresh independent review',b'03-23'); assert all(value in current[len(old):] for value in required); print(hashlib.sha256(old).hexdigest())" &amp;&amp; git diff --exit-code -- .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md .planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md &amp;&amp; task check &amp;&amp; git diff --check</automated>
  </verify>
  <done>The GSD history records the canonical advisory-lock guarantee boundary, preserves the current review, and routes rejudgment through the fresh independent 03-23 review gate.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| untyped caller → `preview_manifest_refresh` | JSON/CLI/direct callers can bypass Python annotations and supply hostile runtime types. |
| canonical OpenSpec → GSD review artifacts | GSD may clarify implementation scope but cannot redefine the canonical guarantee or acceptance criteria. |
| cooperating writers → advisory lock | Bridge-owned and cooperating writers share one change-directory synchronization identity. |
| non-cooperating writer → target bytes | Changes after final observation are not serialized by the advisory protocol and remain outside the Phase 03 guarantee. |
| completed gap → independent review | The executor cannot turn a historical finding clean by editing its report; a fresh role must rejudge it. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-24-TYPE | Tampering/Denial of Service | high | `preview_manifest_refresh.current_source_commit` | mitigate | Exact-string validation precedes regex and every repository/filesystem boundary; fixed None/int/object public tests require structured INPUT failure. |
| T-03-24-SCOPE | Spoofing/Tampering | high | 03-22 persistence guarantee record | mitigate | Append the two in-scope writer classes, advisory nature, defense-in-depth recheck, and final-observation exclusion without editing canonical OpenSpec. |
| T-03-24-REVIEW | Repudiation/Elevation of Privilege | high | Phase 03 exit review | mitigate | Preserve issues_found review unchanged and require a fresh independent 03-23 review before later exit tasks or Phase 4. |
| T-03-24-DATA | Tampering | medium | non-cooperating target writer | accept | Canonical HARD-R2 excludes post-final-observation non-cooperating changes; no CAS-like persistence is authorized in Phase 03. |
| T-03-24-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- RED evidence: the new public test node fails on the current `TypeError` path before production changes.
- GREEN evidence: `uv run pytest tests/test_handoff_manifest_refresh.py::test_preview_rejects_non_string_source_commit_before_filesystem_work -q` passes all three fixed cases.
- Regression evidence: `uv run pytest tests/test_handoff_manifest_refresh.py -q` and `task check` pass.
- Artifact integrity: the pre-execution `git show HEAD:` bytes for `03-22-SUMMARY.md` are a strict exact prefix of the post-edit file, the snapshot SHA-256 is recorded, `task check` passes, and `git diff --check` passes; diff contains only `03-22-SUMMARY.md`, `manifest_refresh.py`, and `test_handoff_manifest_refresh.py` during execution.
- Authority integrity: canonical OpenSpec, existing REVIEW, 03-23, ROADMAP, STATE, and REQUIREMENTS remain unchanged.
</verification>

<success_criteria>
- WR-01 is closed through RED then GREEN at the confirmed public seam with exact return code/category/state and no filesystem work.
- CR-01 is routed as an over-broad guarantee interpretation: history is preserved, the GSD boundary is clarified, and no persistence implementation is rebuilt or expanded.
- Completed 03-22 code/tests remain unchanged and green.
- Gap execution stops after 03-24; existing 03-23 resumes with a fresh independent review that must rejudge the clarified boundary and become clean before its later exit tasks run.
- HND-03 and Phase 3 remain incomplete until existing 03-23 completes; Phase 4 remains blocked.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-24-SUMMARY.md` when done, including RED/GREEN commit evidence, exact malformed-input results, the append-only guarantee clarification, and the explicit handoff back to existing 03-23 fresh review.
</output>
