---
phase: 03-lifecycle-drift-gate
plan: 17
type: tdd
wave: 13
depends_on:
  - 03-14
  - 03-16
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
  - tests/test_handoff_lifecycle_gate.py
  - tests/test_handoff_manifest_refresh.py
  - tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
  - tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
  - tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
  - .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
  - .planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json
  - .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: complete expected and observed phase graphs are validated independently, while only observed_nodes must exactly match the current PlanningInventory phase map."
    - "Complete phase addition, removal, path change, dependency change, and their simultaneous combinations return deterministic DRIFTED decisions with reusable identities instead of lifecycle-phase-observation-incomplete."
    - "Graph remediation uses the union of old and new dependency edges, retains every changed phase as a revalidation target, and limits replanning targets to phases still present in observed_nodes."
    - "Malformed, expected-only, observed-only, both-present, and neither-present target relations are classified before mapping readiness with the exact issue/action behavior pinned by HARD-R2."
    - "Valid clean decision bytes and lifecycle-gate-decision-v1 remain compatible; valid drift is order-invariant and stale replay is freshly rejected."
    - "The source-pinned handoff, tracked 54-item assignment, Plan A derived authorities, and GSD authority metadata are published from canonical commit 9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901 only after fresh approval of a preview that binds the exact candidate assignment hash."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      provides: "Complete phase-graph, target-relation, downstream-remediation, and compatible decision-identity behavior"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Fixed public graph/target/compatibility examples and the pure graph/remediation property family"
    - path: .planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json
      provides: "Immutable read-only refresh preview for the exact 9a7a313 source pin and 54-item candidate, including assignment_inventory_sha256"
    - path: .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
      provides: "Freshly approved started-v2 source pin with 54 active source items and mappings"
    - path: tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
      provides: "Complete 54-source phase assignment authority materialized only after exact-preview approval"
    - path: tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
      provides: "Independent expected refresh-preview fixture for the 54-item candidate"
    - path: tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
      provides: "Independent literal lifecycle evidence after graph-contract recovery"
    - path: .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json
      provides: "Deterministic tracked lifecycle evidence equal to the independent fixture"
    - path: .planning/PROJECT.md
      provides: "Post-approval current canonical source pin"
    - path: .planning/ROADMAP.md
      provides: "Post-approval 18-plan Phase 3 incomplete status and corrected property-seam criterion"
    - path: .planning/REQUIREMENTS.md
      provides: "Post-approval non-normative traceability authority pin"
    - path: .planning/STATE.md
      provides: "Post-approval Phase 3 execution/reverification position"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      via: "_validate_phase_graph validates PlanningInventory once and compares its phase map only with observed_nodes before mapping readiness"
      pattern: "_validate_phase_graph|validate_planning_inventory"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: LifecycleGateDecision
      via: "_phase_changes, target relation, and downstream union feed the final sorted projection before _decision_identity"
      pattern: "_phase_changes|_downstream_phases|_decision_identity"
    - from: .planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json
      to: .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
      via: "apply_manifest_refresh receives the exact approved preview_sha256 only after the candidate assignment bytes reproduce assignment_inventory_sha256"
      pattern: "preview_sha256|assignment_inventory_sha256|candidate_manifest"
    - from: tests/test_handoff_lifecycle_gate.py
      to: tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
      via: "the public-gate producer is byte-equal to an independent literal and the tracked evidence"
      pattern: "expected-lifecycle-evidence|03-LIFECYCLE-EVIDENCE"
---

<objective>
Close the complete phase-graph drift gap for HND-03 / HARD-R2 and repin every Plan A source-derived authority exactly once.

Purpose: Restore deterministic DRIFTED decisions and remediation for valid graph changes without weakening fail-closed validation or approval-bound manifest refresh.
Output: Correct public phase/target decisions, fixed and property evidence, one explicitly approved 54-item handoff refresh, synchronized derived fixtures/evidence, and synchronized post-approval GSD source/state metadata.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-10-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-14-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
@tests/test_handoff_lifecycle_gate.py
@tests/test_handoff_manifest_refresh.py

<interfaces>
Preserve `PhaseGraphObservation` fields, `LifecycleGateDecision` fields, `LifecycleObservationBoundary.observe_phase_graph(...)`, `gate_lifecycle_operation(...)`, the `lifecycle-gate-decision-v1` discriminator, manifest schema/version, and valid-clean identity bytes. Modify the existing `_validate_phase_graph`, `observe_lifecycle_operation`, `_phase_changes`, `_downstream_phases`, `_decision_from_observation`, `_unknown_decision`, and `gate_lifecycle_operation` paths; add at most one private `_classify_target_relation` helper so target membership is decided before `validate_mapping_readiness`. Reuse `preview_manifest_refresh`, `serialize_manifest_refresh_preview`, and `apply_manifest_refresh`; do not write a second refresh path or bypass its approval/current-snapshot guards.
</interfaces>
</context>

## Artifacts this phase produces

- New private symbol: `lifecycle_gate._classify_target_relation`.
- New next-action code values: `lifecycle-input-invalid`, `lifecycle-target-phase-removed`, `lifecycle-target-phase-unknown`.
- New file: `.planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json`.
- New fixed public test nodes for phase add, remove, path change, dependency change, simultaneous changes, empty/one/all-removed graphs, target relations, and clean/drift identity compatibility.
- New Hypothesis family for pure graph/remediation order invariance, old/new-edge downstream union, observed-set replanning intersection, set-like UTF-8 ordering, and same-input identity determinism.
- Modified artifacts after approval: the existing source-pinned handoff, 54-ID assignment/refresh fixtures, lifecycle golden/tracked evidence, their existing tests, and the four GSD authority/state metadata files.
- No new public field, CLI flag, module, package export, dependency, schema version, or mutation authority.

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation consumes the same fresh fail-closed drift decision. | 03-17 | COVERED | Complete graph changes reach the shared decision instead of becoming unknown. |
| REQ | HND-03 / HARD-R2 | Phase state drift stops the operation and projects deterministic recovery. | 03-17 | COVERED | Fixed and property evidence cover graph, target, identity, and stale replay. |
| RESEARCH | ordered graph trust boundary | Validate both raw graphs before normalization, comparison, projection, or identity. | 03-17 Task 1 | COVERED | Existing bounded validators remain the sole raw graph authority. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Continuation without context was explicitly approved. |
| VERIFICATION | gap 1 | Valid phase add/remove/path/dependency drift is unreachable behind one-inventory equality. | 03-17 Task 1 | COVERED | observed_nodes alone is checked against current inventory; expected_nodes remains source-pinned. |
| CANONICAL | A-P-GRAPH / A-E-GRAPH / A-E-TARGET / A-E-COMPAT | Complete graph, remediation, target, compatibility, and deterministic identity contract. | 03-17 Task 1 | COVERED | Exact public outcomes and one pure property family are required. |
| CANONICAL | A-E-REPIN | Repin 49→54 source items/mappings and 43→48 scenarios once in Plan A. | 03-17 Tasks 1-3 | COVERED | Task 1 produces only an isolated candidate; the preview binds `assignment_inventory_sha256`, and Task 3 owns all tracked publication. |
| CANONICAL | A-E-EXIT | Phase 4 remains blocked pending full check/review/verifier/security exit evidence. | 03-17 verification | COVERED | This plan does not claim Phase 3 complete. |
| METADATA | source/state synchronization | PROJECT, ROADMAP, REQUIREMENTS, and STATE consume the new pin only after approved apply and keep Phase 3 incomplete across 18 plans. | 03-17 Task 3 | COVERED | OpenSpec task 3.1 remains unchecked until every canonical exit gate passes. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recover complete graph drift, target, and remediation behavior</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-10-SUMMARY.md
    - .planning/phases/03-lifecycle-drift-gate/03-14-SUMMARY.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_gate.py
    - tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py, .planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json</files>
  <behavior>
    - RED: fixed public-gate cases for add, remove, path change, dependency change, same-phase path plus dependency change, simultaneous multi-phase changes, both-empty, one-sided-empty, one phase, and all removed fail with the current phase-incomplete short circuit.
    - RED: malformed targets are identity-free UNKNOWN with only `lifecycle-input-invalid` as next action; expected-only, observed-only, both, and neither target relations match the canonical issue/action table before mapping readiness.
    - RED: removed-phase and old-edge-only downstream evidence is computed over the expected/observed union, then replanning is intersected with observed phase IDs; an empty final set suppresses `replan-affected-phases`.
    - RED: valid drift gets a reusable deterministic identity, valid clean identity bytes remain unchanged, tuple/dependency order does not affect outputs, and stale replay remains freshly DRIFTED.
    - GREEN: `_validate_phase_graph` independently validates both node tuples and requires only `observed_nodes` to equal `planning_inventory.phases`; no expected inventory field is introduced.
  </behavior>
  <action>RED: add fixed public `gate_lifecycle_operation` tests named by the canonical A-E-GRAPH, A-E-TARGET, and A-E-COMPAT families, plus one bounded Hypothesis family that generates small valid phase graphs and input permutations at the already approved public seam. Commit the failing behavior as `test(03-17): expose incomplete phase graph drift contract`. GREEN: in `_validate_phase_graph`, retain exact runtime/UTF-8/count/aggregate/duplicate/edge/DAG validation for expected and observed independently, but compare only the observed ID/path map with the validated current PlanningInventory map. In `_phase_changes`, use independent path and dependency comparisons so both issue codes survive on one phase. In `_downstream_phases`, traverse the union of expected and observed nodes/edges, then intersect the final replanning set with observed phase IDs. In `_classify_target_relation` and `observe_lifecycle_operation`, classify malformed, expected-only, observed-only, both, and neither before mapping readiness; expected-only produces an identity-bearing graph decision without asking current readiness to resolve the removed target. In `_decision_from_observation`, emit `revalidate-mapping` for graph drift and emit `replan-affected-phases` only when final replanning targets are non-empty. Keep all projection tuples unique and UTF-8-byte sorted before `_decision_identity`. Commit GREEN as `feat(03-17): restore complete phase graph drift decisions`. REFACTOR only demonstrated duplication and keep the public wire/identity schema unchanged. Read the tracked 49-item assignment, construct a non-authoritative in-memory `PlanningInventory` candidate with exactly the five new canonical scenario assignments, and pass that candidate through the existing `planning_inventory` parameter of `preview_manifest_refresh` against exact commit `9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901`; do not create a second refresh path or write the candidate fixture. Write only `.planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json`. Require the preview to contain the complete 54-item `assignment_inventory`, its exact `assignment_inventory_sha256`, 54 active mappings, and 48 scenario headings while the tracked 49-item assignment remains byte-identical. Do not call `apply_manifest_refresh` or publish any tracked assignment, handoff, fixture, evidence, or GSD source pin in this task.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py -q -k "phase_graph or phase_added or phase_removed or phase_path or phase_dependencies or target_phase or identity"</automated>
  </verify>
  <acceptance_criteria>
    - Every fixed complete graph case is CLEAN or DRIFTED, never phase-incomplete; malformed/over-limit graph cases remain identity-free UNKNOWN.
    - Same-phase path and dependency changes retain both exact issue codes.
    - Revalidation includes each changed/removed phase; replanning uses old/new-edge downstream union intersected with observed IDs and controls the replan action.
    - Target relation classification precedes mapping readiness and uses removed/unknown/input-invalid only as next-action codes where specified.
    - `PhaseGraphObservation`, lifecycle decision fields/discriminator, manifest schema/version, and the pinned valid-clean identity literal remain unchanged.
    - The property family is confined to pure graph/remediation behavior and the preview is read-only, source-pinned, 54/54 mapped, binds the exact candidate assignment hash, and is not applied.
    - The tracked assignment fixture, handoff, derived fixtures/evidence, PROJECT, ROADMAP source/status metadata, REQUIREMENTS, and STATE remain byte-identical through this task.
  </acceptance_criteria>
  <done>Complete graph changes and target relations reach one deterministic identity-bound public decision, and an immutable exact refresh preview is ready for human approval.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve the exact 54-item source refresh preview</name>
  <read_first>
    - .planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json
    - .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
  </read_first>
  <files>.planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json</files>
  <action>Display the complete immutable preview summary, including `preview_sha256`, `assignment_inventory_sha256`, previous/candidate source commits, candidate manifest SHA-256, 49→54 active-item and mapping counts, 43→48 scenario counts, protected-subtree evidence, target path, staging name, and zero mutation evidence. Request a fresh explicit approval for that exact `preview_sha256`, explicitly stating that the approved bytes bind the displayed candidate assignment hash. Silence, a prior approval, a generic “continue”, or approval of another hash does not authorize Task 3. If approval is refused or the preview or candidate assignment hash changes, stop with the tracked 49-item assignment and handoff unchanged.</action>
  <verify>
    <automated>sha256sum .planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The user sees and approves one exact `preview_sha256` after the full preview summary is displayed.
    - The approval hash equals the immutable preview field and the preview still targets canonical commit `9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901`.
    - The displayed `assignment_inventory_sha256` equals the hash inside the immutable preview and is covered by the approved preview bytes.
    - No tracked assignment, handoff, expected fixture, lifecycle evidence, or GSD source/state metadata mutation occurs before approval.
  </acceptance_criteria>
  <what-built>Complete graph-contract implementation and a read-only 54-item refresh preview.</what-built>
  <how-to-verify>Compare the displayed source commit, counts, candidate hash, and preview hash with `03-REFRESH-PREVIEW.json`, then explicitly approve that exact preview hash.</how-to-verify>
  <resume-signal>Type `approve &lt;exact preview_sha256&gt;` or reject with the reason.</resume-signal>
  <done>Fresh exact-hash approval exists for the unchanged preview, or execution stops before publication.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the approved refresh and repin Plan A derived authorities once</name>
  <read_first>
    - .planning/phases/03-lifecycle-drift-gate/03-REFRESH-PREVIEW.json
    - .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
    - tests/test_handoff_manifest_refresh.py
    - tests/test_handoff_lifecycle_gate.py
    - tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
    - tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
    - .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
  </read_first>
  <files>tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json, .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json, tests/test_handoff_manifest_refresh.py, tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json, tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json, .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>After Task 2 approval, publish in this fail-closed order. First materialize the preview's complete `assignment_inventory` as the tracked `hardening-phase-assignments.json`, serialize it with the established fixture format, and require its recomputed hash to equal the approved preview's `assignment_inventory_sha256`; stop before apply on any mismatch. Second rerun `preview_manifest_refresh` from the now-current tracked inputs and require its serialized bytes, `assignment_inventory_sha256`, and recomputed `preview_sha256` to equal `03-REFRESH-PREVIEW.json` and the exact hash approved in Task 2, then call the existing `apply_manifest_refresh(preview, approved_preview_sha256=..., approved=True)` exactly once. Require structured applied success, the 54-item/54-mapping started-v2 target, exact source commit `9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901`, no staging residue, and protected-input hashes before proceeding. Third update the independent expected refresh fixture and count assertions in `tests/test_handoff_manifest_refresh.py`, then regenerate lifecycle evidence only through the existing public-gate producer after all graph tests are green; write identical deterministic bytes to the literal fixture and tracked evidence, retaining no raw repository path or raw decision identity. Fourth, only after successful apply and derived-authority checks, synchronize `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md` to the exact canonical pin `9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901`. Record Phase 3 as an incomplete 18-plan phase: Plans 03-01 through 03-16 remain complete, 03-17 is the current approved publication plan, 03-18 and independent reverification remain pending, Phase 4 stays blocked, and completed-phase/progress fields must not count Phase 3 as complete. Make that state machine-verifiable: ROADMAP has `**Plans:** 18 plans`, an unchecked Phase 3 phase row, an unchecked `03-18-PLAN.md` row, the exact progress row `| 3. Lifecycle Drift Gate | 16/18 | In progress | - |`, explicit pending independent reverification, and Phase 4 blocked on Phase 3; STATE has `status: executing`, `completed_phases: 2`, `total_plans: 28`, `completed_plans: 26`, `percent: 33`, the exact `Plan: 17 of 18` position, explicit `03-18` and reverification pending text, and Phase 4 blocked text. In PROJECT and ROADMAP retain the corrected approved property seams (checkbox normalization plus A-P-GRAPH and B-P-PATH-ROLE); in STATE replace the obsolete 16-of-16/ready-for-verification position with the current 18-plan execution/reverification position. Remove the old source pin and stale 16/16-complete, ready-for-verification, and Phase 4-ready semantics from all four current metadata authorities. Verify `openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md` task 3.1 remains unchecked; do not edit it until every canonical exit gate passes. Do not modify the historical Phase 2 preview, run optional real-tool smoke, mark Phase 3 complete, or repin any authority a second time.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_refresh.py tests/test_handoff_lifecycle_gate.py -q &amp;&amp; bash -ec 'new=9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901; old=4d8b5b173927ed518d39dee18a29b0271628afbd; for file in .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md; do rg -qF "$new" "$file"; ! rg -qF "$old" "$file"; done; rg -qF "**Plans:** 18 plans" .planning/ROADMAP.md; rg -q "^- \\[ \\] \\*\\*Phase 3: Lifecycle Drift Gate\\*\\*" .planning/ROADMAP.md; rg -q "^- \\[ \\] 03-18-PLAN\\.md" .planning/ROADMAP.md; rg -qi "independent reverification.*pending|pending.*independent reverification" .planning/ROADMAP.md; rg -qi "Phase 4.*blocked on Phase 3" .planning/ROADMAP.md; rg -q "^\\| 3\\. Lifecycle Drift Gate \\| 16/18 \\| In progress \\| - \\|$" .planning/ROADMAP.md; ! rg -qi "16/16 plans complete|Lifecycle Drift Gate \\| 16/16 \\| Complete|Phase 3: Lifecycle Drift Gate.*complete|ready[- ]for[- ]verification|Phase 4.*ready" .planning/ROADMAP.md; rg -q "^status: executing$" .planning/STATE.md; rg -q "^  completed_phases: 2$" .planning/STATE.md; rg -q "^  total_plans: 28$" .planning/STATE.md; rg -q "^  completed_plans: 26$" .planning/STATE.md; rg -q "^  percent: 33$" .planning/STATE.md; rg -q "^Plan: 17 of 18$" .planning/STATE.md; rg -qi "03-18.*pending" .planning/STATE.md; rg -qi "reverification.*pending" .planning/STATE.md; rg -qi "Phase 4.*blocked" .planning/STATE.md; ! rg -qi "completed_phases: 3|Plan: 16 of 16|16-of-16|16/16|ready[- ]for[- ]verification|Phase 4.*ready" .planning/STATE.md; rg -q "^- \\[ \\] 3\\.1 " openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md'</automated>
  </verify>
  <acceptance_criteria>
    - `apply_manifest_refresh` accepts only the exact freshly approved preview and leaves no staging file.
    - The tracked handoff has 54 active source items, 54 mappings, 48 scenarios, and exact source commit `9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901`.
    - The tracked 54-item assignment reproduces the approved `assignment_inventory_sha256`; expected preview, lifecycle literal, and tracked lifecycle evidence are regenerated once and agree byte-for-byte with their public producers.
    - Valid-clean identity compatibility, valid-drift identity determinism, stale replay, protected-input invariance, and all graph/target fixed cases remain green.
    - Historical Phase 2 preview evidence, canonical OpenSpec source files, optional smoke record, schema/version, dependencies, and CLI/package exports remain unchanged.
    - PROJECT, ROADMAP, REQUIREMENTS, and STATE each contain the exact new source pin and contain no old pin.
    - ROADMAP/STATE deterministically describe an incomplete 18-plan Phase 3 with 03-18 and independent reverification pending, Phase 4 blocked, and completed-phase accounting fixed at two completed phases; no stale 16/16-complete, ready-for-verification, or Phase 4-ready semantics remain.
    - OpenSpec task 3.1 remains exactly unchecked until `task check`, clean review, passed 10/10 verifier, Complete traceability, and zero-open-threat security evidence all exist.
  </acceptance_criteria>
  <done>The exact approved source refresh, every Plan A derived authority, and all GSD source/state metadata are synchronized once; no stale 49-item or 16-of-16 authority remains active, while Phase 3 and OpenSpec task 3.1 stay incomplete.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high; any unresolved high-severity threat stops completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| source-pinned expected graph / current observed graph → lifecycle decision | Complete set differences must remain drift while malformed, cyclic, duplicate, or over-limit graphs remain unknown. |
| lifecycle target → mapping readiness | Removed or unknown targets can be misclassified if current readiness is consulted first. |
| graph difference → remediation / identity | Old-edge downstream or deleted phases can be lost, duplicated, or encoded inconsistently. |
| read-only refresh preview → tracked handoff | A stale or unapproved candidate must never mutate the started manifest. |
| public decision → portable evidence | Raw roots/identities, stale counts, or nondeterministic ordering can create misleading reviewer evidence. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-17-GRAPH | Tampering/Elevation of Privilege | high | `_validate_phase_graph` / `_phase_changes` | mitigate | Independently validate both graphs, bind observed to current inventory, preserve complete set differences, and cover malformed plus valid drift at the public gate. |
| T-03-17-TARGET | Spoofing/Elevation of Privilege | high | target classification before readiness | mitigate | Validate canonical target scalar and classify four membership relations before readiness; only complete drift receives identity/remediation. |
| T-03-17-PROJECTION | Tampering/Repudiation | high | downstream/remediation/identity | mitigate | Use old/new graph union, observed intersection, set-like UTF-8 ordering, fixed literals, property invariants, and same-input identity checks. |
| T-03-17-REFRESH | Tampering/Elevation of Privilege | high | `apply_manifest_refresh` | mitigate | Immutable preview, fresh exact-hash human approval, fresh snapshot recomputation, one guarded apply, atomic replacement, and no staging residue. |
| T-03-17-EVIDENCE | Information Disclosure/Repudiation | high | golden/tracked evidence | mitigate | Public seams only, independent literal, byte equality, no raw root or identity, protected-input hashes, and one owner for all repins. |
| T-03-17-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_gate.py tests/test_handoff_manifest_refresh.py -q`
- Confirm `.planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json` parses as started-v2 with exact source commit `9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901`, 54 active items, 54 mappings, and 48 scenario headings.
- Compare `tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json` and `.planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json` byte-for-byte and run the producer twice.
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py tests/test_handoff_manifest_refresh.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py tests/test_handoff_manifest_refresh.py`
- `git diff --check`
- Confirm PROJECT, ROADMAP, REQUIREMENTS, and STATE use exact pin `9a7a313d06ae6df1c102f2515a3ad4bd5c0ca901`, Phase 3 has 18 plans and remains incomplete, Phase 4 remains blocked, and OpenSpec task 3.1 is still unchecked.
- Do not run final `task check` until dependent Plan 03-18 updates the mapping suite for the 54-item authority and path-role contract.
</verification>

<success_criteria>
- Verification gap 1 is closed for phase add, remove, path, and dependency changes with exact deterministic issue/revalidation/replanning/action tuples.
- HARD-R2 graph, target, compatibility, property, and repin evidence is complete without adding a second inventory or changing public wire/schema fields.
- The tracked assignment and handoff publication occur only after fresh approval of preview bytes that bind the exact candidate assignment hash; every Plan A derived authority and GSD source/state metadata file is then repinned once in order.
- Existing malformed graph, clean identity, stale replay, source, manifest, capability, and optional-smoke boundaries remain intact.
- All scoped high-severity ASVS Level 1 threats are mitigated; Phase 3 remains incomplete until Plan 03-18 and the canonical exit reports pass.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-17-SUMMARY.md` with RED/GREEN/REFACTOR evidence, fixed/property outcome matrix, exact approval and assignment hashes, refresh apply result, 49→54 and 43→48 repin evidence, PROJECT/ROADMAP/REQUIREMENTS/STATE synchronization evidence, the still-unchecked OpenSpec 3.1 boundary, identity compatibility, hashes, and threat dispositions.
</output>
