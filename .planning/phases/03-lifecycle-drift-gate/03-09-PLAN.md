---
phase: 03-lifecycle-drift-gate
plan: 09
type: tdd
wave: 8
depends_on:
  - 03-08
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
  - tests/test_handoff_lifecycle_drift.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: every nested canonical observation string is a valid UTF-8 scalar before comparison, sorting, set construction, or lifecycle decision identity encoding."
    - "Canonical observations enforce the producer-equivalent artifact, task, changed-source-ID, and aggregate-byte bounds at the public classifier boundary."
    - "The exact 4096-task boundary remains valid, while 4097 tasks on either classifier side are unknown and never receive a reusable lifecycle identity."
    - "Malformed Unicode on either expected or observed classifier side, and through the public lifecycle gate, returns canonical-observation-incomplete with empty evidence and no identity."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      provides: "UTF-8-scalar and producer-bound completeness validation before canonical semantic operations"
    - path: tests/test_handoff_lifecycle_drift.py
      provides: "Fixed expected/observed classifier regressions for malformed Unicode and 4096/4097 boundaries"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Fixed public-gate regressions proving malformed/over-limit canonical observations are wholly unknown"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/progress.py
      via: "_is_complete_progress reuses MAX_TASKS before task iteration contributes to comparison or identity"
      pattern: "MAX_TASKS|_is_complete_progress"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/reader.py
      via: "_is_complete_observation reuses DEFAULT_ARTIFACT_LIMITS count and aggregate-byte authority"
      pattern: "DEFAULT_ARTIFACT_LIMITS|_is_complete_observation"
    - from: tests/test_handoff_lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "FakeBoundary injects malformed canonical values and assertions observe only gate_lifecycle_operation"
      pattern: "gate_lifecycle_operation"
---

<objective>
Close the canonical structured-observation validation gap for HND-03 / HARD-R2.

Purpose: Ensure caller-constructed canonical values satisfy the same scalar and resource bounds as repository-produced values before any semantic comparison or reusable identity is possible.
Output: Hardened existing completeness validators and fixed public classifier/gate regressions for malformed Unicode and exact 4096/4097 boundaries.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@.planning/phases/03-lifecycle-drift-gate/03-07-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-08-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@src/ai_coding_template_ja/openspec_gsd_handoff/progress.py
@src/ai_coding_template_ja/openspec_gsd_handoff/reader.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_lifecycle_drift.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve `classify_canonical_source_drift(expected, observed)` and `gate_lifecycle_operation(...)` as the only test seams. Extend the existing `_is_complete_observation` and `_is_complete_progress` authority; do not create a second classifier or an exception sanitizer. Reuse `DEFAULT_ARTIFACT_LIMITS.max_files`, `DEFAULT_ARTIFACT_LIMITS.bytes_total`, `progress.MAX_TASKS`, and `SourceIdentityLimits().max_items` rather than duplicating numeric authorities. Malformed or over-limit values must converge on `canonical-observation-incomplete`; the public gate must keep its existing unknown/non-admitted/empty/no-identity projection.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, public symbol, serializer, root export, CLI operation, or evidence schema.
- Existing `_is_complete_observation` and `_is_complete_progress` gain scalar and bound validation.
- New fixed test nodes:
  - `tests/test_handoff_lifecycle_drift.py::test_canonical_observation_rejects_non_utf8_scalar_before_comparison`
  - `tests/test_handoff_lifecycle_drift.py::test_canonical_observation_accepts_4096_tasks_and_rejects_4097`
  - `tests/test_handoff_lifecycle_drift.py::test_canonical_observation_rejects_count_and_aggregate_limit_plus_one`
  - `tests/test_handoff_lifecycle_gate.py::test_malformed_unicode_and_over_limit_canonical_observation_is_wholly_unknown`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation consumes the same fresh fail-closed decision. | 03-09 | COVERED | Invalid canonical values stop before comparison and identity. |
| REQ | HND-03 / HARD-R2 | Incomplete inspection is unknown and stops. | 03-09 | COVERED | Fixed public classifier and gate outcomes are identity-free. |
| RESEARCH | validation order / bounds | Validate raw nested data before transform and keep fixed examples primary. | 03-09 | COVERED | Existing authorities and one classifier seam are reused. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Continuation without synthetic context was explicitly approved. |
| VERIFICATION | root gap 1 | UTF-8 scalar validity plus artifact/task/changed-ID/aggregate bounds. | 03-09 | COVERED | Both expected/observed sides and public gate receive fixed cases. |
| REVIEW | CR-02, CR-03 | Lone surrogate raises; 4097 tasks receive identity. | 03-09 | COVERED | Exact reproduced families become RED tests. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject non-UTF-8-scalar canonical strings before semantic use</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_lifecycle_drift.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py, tests/test_handoff_lifecycle_drift.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: a lone surrogate in artifact path, task id, task description, or changed source ID on either expected or observed side returns public classifier unknown instead of reaching comparison/sort.
    - RED: the same malformed values injected as the source-commit expected observation or the freshly observed current value return `canonical-observation-incomplete`, `UNKNOWN`, `admitted=False`, empty evidence, and `decision_identity is None` instead of `UnicodeEncodeError`.
    - GREEN: every exact-string field owned directly by `CanonicalSourceObservation`, `CanonicalArtifactObservation`, `Progress`, and `NormalizedTask` is UTF-8 encoded successfully before cardinality, sorting, set construction, comparison, or identity.
    - GREEN: `validate_source_identity_state` remains the only validator for nested source-state strings and its complete bound remains unchanged.
  </behavior>
  <action>Write the two fixed public-seam tests first and commit RED as `test(03-09): expose malformed Unicode canonical observations`. Mutate one field per row with the literal lone-surrogate family and parameterize `malformed_side` as expected/observed. For the public gate, parameterize `canonical_side` too: inject expected through `FakeBoundary.source_result`, inject observed by monkeypatching `observe_canonical_source` to return the malformed `Success`, and assert only the public `gate_lifecycle_operation` result; do not call private validators. In GREEN, extend `_is_complete_observation` / `_is_complete_progress` in the established outer → container → member → field → UTF-8 scalar → invariant order. Attempt strict UTF-8 encoding only after exact string type checks and return `False` on `UnicodeEncodeError`; do not normalize, replace, escape, or silently drop text. Keep source state delegated to `validate_source_identity_state`. Commit GREEN as `feat(03-09): validate canonical observation UTF-8 scalars`. After GREEN, review for repeated encoding logic; make a separate `refactor(03-09): consolidate canonical UTF-8 validation` commit only if behavior remains identical, otherwise record that no refactor was warranted. Do not change `_IdentityEncoder`, public decision fields, issue codes, checkbox normalization, or evidence JSON.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_drift.py::test_canonical_observation_rejects_non_utf8_scalar_before_comparison tests/test_handoff_lifecycle_gate.py::test_malformed_unicode_and_over_limit_canonical_observation_is_wholly_unknown -q</automated>
  </verify>
  <acceptance_criteria>
    - Every fixed malformed-string field on both classifier sides returns only `canonical-observation-incomplete`.
    - Every expected-side and observed-side public-gate malformed Unicode row is unknown, non-admitted, empty, and identity-free.
    - No row expects an exception, replacement character, normalized text, or partial evidence.
    - The RED and GREEN commits use the exact messages above; the summary records the behavior-preserving REFACTOR decision.
  </acceptance_criteria>
  <done>Malformed canonical Unicode is rejected by the shared completeness authority before it can crash or influence identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce producer-equivalent canonical count and aggregate-byte bounds</name>
  <read_first>
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/progress.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/reader.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_lifecycle_drift.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py, tests/test_handoff_lifecycle_drift.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: a self-consistent `Progress` containing exactly 4096 tasks remains complete, while 4097 tasks on expected and observed sides return unknown.
    - RED: artifact count, changed-source-ID count, and cumulative nested UTF-8 bytes at the authoritative limit pass; limit+1 returns unknown before set/sort/comparison.
    - RED: the public gate rejects expected-side and observed-side 4097-task and aggregate-byte limit+1 observations with empty evidence and no identity.
    - GREEN: valid existing clean, drifted, checkbox-only, source-state, and lifecycle identity behavior remains unchanged.
  </behavior>
  <action>Commit RED as `test(03-09): expose unbounded canonical structured observations`. Use fixed 4096/4097 fixtures and fixed limit/limit+1 strings; do not introduce a new Hypothesis family. Parameterize the public gate rows as source-commit expected versus freshly observed current using the same injection pattern as Task 1. In GREEN, reuse `MAX_TASKS`, `DEFAULT_ARTIFACT_LIMITS.max_files`, `DEFAULT_ARTIFACT_LIMITS.bytes_total`, and `SourceIdentityLimits().max_items`. Check collection lengths before iterating; after UTF-8 scalar validation, sum the bytes of artifact path/digests, task id/description, and changed source IDs with an early limit+1 stop before sorting, set construction, equality, or identity. Do not count source-state strings a second time because `validate_source_identity_state` owns their independent 4096-item/8-MiB validation. Commit GREEN as `feat(03-09): enforce canonical structured observation bounds`. If production code changes during REFACTOR, make exactly one behavior-preserving commit named `refactor(03-09): consolidate canonical bound validation`; if no refactor is warranted, create no refactor commit and record that decision explicitly in `03-09-SUMMARY.md`. Never weaken the exact 4096 accepted / 4097 rejected boundary. Do not add automatic retry, repair, rollback, route switching, evidence regeneration, dependency changes, or real-tool smoke.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_drift.py::test_canonical_observation_accepts_4096_tasks_and_rejects_4097 tests/test_handoff_lifecycle_drift.py::test_canonical_observation_rejects_count_and_aggregate_limit_plus_one tests/test_handoff_lifecycle_gate.py::test_malformed_unicode_and_over_limit_canonical_observation_is_wholly_unknown -q</automated>
  </verify>
  <acceptance_criteria>
    - Exact 4096/4097 public assertions exist for classifier expected/observed and gate expected/current-observed task observations.
    - Artifact, changed-ID, and aggregate-byte limit+1 cases are unknown before semantic operations.
    - Limit failures expose no artifact paths, source IDs, progress candidate, remediation, or reusable identity.
    - Exactly one existing `@given` family remains, limited to checkbox normalization.
    - Any production-code REFACTOR commit uses exactly `refactor(03-09): consolidate canonical bound validation`; otherwise `03-09-SUMMARY.md` records the no-refactor decision and no refactor commit exists.
  </acceptance_criteria>
  <done>Caller-constructed canonical observations cannot exceed producer count/byte limits or mint a reusable drift identity.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| boundary-constructed dataclasses → canonical classifier | Python annotations do not enforce UTF-8 scalar validity or producer resource bounds. |
| canonical classifier → lifecycle identity | Any malformed value reaching sorting or encoding can crash or mint reusable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-09-01 | Tampering/Denial of Service | high | nested canonical strings | mitigate | Exact-string and strict UTF-8-scalar validation precedes every comparison, sort, set, and identity operation; fixed public regressions cover both sides. |
| T-03-09-02 | Denial of Service/Elevation of Privilege | high | canonical collection and aggregate bounds | mitigate | Reuse producer authorities, accept 4096, reject 4097 and limit+1 bytes before iteration-dependent semantics, and expose no identity. |
| T-03-09-03 | Spoofing/Replay | high | lifecycle decision identity | mitigate | Only a complete bounded canonical decision reaches existing typed identity encoding; malformed rows are wholly unknown and identity-free. |
| T-03-09-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py`
- `git diff --check`
- Confirm exactly one `@given` family remains and no dependency, root export, CLI, serializer, evidence-schema, optional-smoke, automatic retry/repair/rollback/route-switch change exists.
</verification>

<success_criteria>
- Verification root gap 1 and REVIEW CR-02/CR-03 are closed at both public seams.
- Fixed 4096/4097 and malformed-Unicode rows cover expected, observed, and gate-injected values.
- All malformed or over-limit rows are unknown, non-admitted, empty, and identity-free.
- Existing lifecycle behavior and the sole approved checkbox property family remain green.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-09-SUMMARY.md` with RED/GREEN/REFACTOR commits, exact boundary results, focused/full verification, and threat dispositions.
</output>
