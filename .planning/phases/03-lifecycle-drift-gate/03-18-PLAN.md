---
phase: 03-lifecycle-drift-gate
plan: 18
type: tdd
wave: 14
depends_on:
  - 03-17
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
  - tests/test_handoff_execution_mapping.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: phase, plan, and evidence canonical path roles are mutually disjoint before filesystem observation, readiness, hashing, or identity generation."
    - "Exact, case, and Unicode alias collisions for phase=plan, phase=evidence, and plan=evidence return mapping-path-role-conflict from PlanningInventory validation."
    - "A plan artifact cannot satisfy its own source or plan evidence; one independent evidence declaration may own both a source and plan, and one owner may have multiple distinct evidence paths."
    - "Direct ManifestMapping values that bypass the public builder and collide across phase, plan, or evidence roles return mapping-set-invalid."
    - "VERIFY readiness and the lifecycle gate reject role-invalid inputs before any filesystem call and expose no ready result, decision identity, or remediation projection."
    - "Canonical path-role properties are input-order invariant, while malformed path forms retain their existing dimension-specific failure codes."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      provides: "Canonical path-role disjointness for PlanningInventory and direct ManifestMapping inputs"
    - path: tests/test_handoff_execution_mapping.py
      provides: "Fixed builder/readiness cases and canonical path-role property family"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Public gate early-rejection and zero-filesystem-call regressions"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: PlanningInventory
      via: "_validate_inventory_invariants compares canonical role namespaces after scalar/path validation and before readiness"
      pattern: "_validate_inventory_invariants|mapping-path-role-conflict"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: ManifestMapping
      via: "_validate_manifest_mappings independently enforces the same role invariant on direct immutable mappings"
      pattern: "_validate_manifest_mappings|mapping-set-invalid"
    - from: tests/test_handoff_lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "gate_lifecycle_operation receives the mapping failure and returns identity-free UNKNOWN before declared-path observation"
      pattern: "gate_lifecycle_operation|mapping-path-role-conflict"
---

<objective>
Close the PlanningInventory and ManifestMapping path-role collision gap for HND-03 / HARD-R2.

Purpose: Require independent enforcement evidence and prevent one path from satisfying phase, plan, and evidence roles before VERIFY or lifecycle admission.
Output: One shared role invariant at both mapping trust boundaries, fixed public regressions, a focused property family, and the final integrated project gate.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-11-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-17-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_execution_mapping.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve public `validate_planning_inventory`, `build_manifest_mappings`, `validate_mapping_readiness`, `gate_lifecycle_operation`, existing dataclass fields, canonical mapping projection, and valid 54-item readiness outputs. Modify `_validate_inventory_invariants` and `_validate_manifest_mappings`; reuse `_canonical_path` and `_alias_key`. Do not add a parallel public validator, change `EvidenceDeclaration` or `ManifestMapping` fields, repin Plan 03-17 artifacts, or change the lifecycle decision schema.
</interfaces>
</context>

## Artifacts this phase produces

- New stable failure-code value: `mapping-path-role-conflict`.
- New fixed test nodes for three cross-role pairs, exact/case/NFC-NFD aliases, evidence declaration splitting, allowed combined source/plan ownership, allowed multiple distinct evidence paths, direct ManifestMapping rejection, VERIFY refusal, and lifecycle zero-I/O refusal.
- New Hypothesis family for canonical role-disjointness and declaration-order invariance.
- Modified existing symbols: `_validate_inventory_invariants` and `_validate_manifest_mappings`.
- No new public/private function, dataclass field, CLI flag, file path, module, dependency, schema/version, source pin, fixture repin, or evidence serialization field.

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation consumes fail-closed complete mapping evidence. | 03-18 | COVERED | Role-invalid evidence cannot become ready. |
| REQ | HND-03 / HARD-R2 | VERIFY and lifecycle admission require role-correct source and plan evidence. | 03-18 | COVERED | Public inventory, readiness, direct mapping, and gate seams are covered. |
| RESEARCH | validation before semantic/filesystem work | Runtime shape and canonical paths precede role and I/O checks. | 03-18 Tasks 1-2 | COVERED | Existing validation order and issue families are preserved. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Continuation without context was explicitly approved. |
| VERIFICATION | gap 2 | A plan path can satisfy its own source/plan evidence and VERIFY returns ready. | 03-18 Tasks 1-2 | COVERED | Cross-role namespaces are disjoint and independent evidence sharing remains valid. |
| CANONICAL | B-P-PATH-ROLE / B-E-PATH-ROLE | Canonical role property and fixed public examples. | 03-18 Task 1 | COVERED | Exact/alias collisions and valid sharing are explicit. |
| CANONICAL | B-I-PATH-RACE | Missing/alias/symlink/identity-change fail closed; operation consumers recheck. | 03-18 Tasks 1-2 | COVERED | Task 1 proves structural rejection with zero I/O; Task 2 reruns the existing fixed I/O races and integrated gates. |
| CANONICAL | A-E-REPIN ownership | Plan B does not repin source-derived authorities. | 03-18 | COVERED | Plan 03-17 is the sole repin owner. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce disjoint path roles in inventory and direct mappings</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-11-SUMMARY.md
    - .planning/phases/03-lifecycle-drift-gate/03-17-SUMMARY.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_execution_mapping.py
    - tests/test_handoff_lifecycle_gate.py
    - tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py, tests/test_handoff_execution_mapping.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: phase=plan, phase=evidence, and plan=evidence exact collisions fail inventory validation with `mapping-path-role-conflict`.
    - RED: platform-case and NFC/NFD cross-role aliases fail with the same role code after canonical scalar/path validation; backslash, absolute, empty, dot-component, NUL, wrong-type, and malformed Unicode inputs retain existing shape/path codes.
    - RED: declaring the same evidence path in two EvidenceDeclaration records fails with `mapping-path-role-conflict`.
    - RED: one independent evidence record with both `source_id` and `plan_path`, and one owner with multiple distinct evidence paths, remain valid and can satisfy VERIFY.
    - RED: direct ManifestMapping phase/plan/evidence exact or alias collisions fail `_validate_manifest_mappings` through `validate_mapping_readiness` with `mapping-set-invalid`.
    - RED: the verifier's exact plan-as-own-evidence counterexample is rejected by the public builder and VERIFY readiness instead of returning ready.
    - RED: the public lifecycle gate maps that role-invalid PlanningInventory to identity-free UNKNOWN with empty remediation and zero declared-path filesystem calls.
    - GREEN: declaration order cannot change role acceptance, error code, canonical mappings, or readiness.
    - GREEN: a valid independent evidence artifact shared by source and plan owners remains VERIFY-ready when every declared path exists.
  </behavior>
  <action>RED: before any production change, add one fixed public table spanning all canonical B-E-PATH-ROLE rows through `validate_planning_inventory`, `build_manifest_mappings`, and `validate_mapping_readiness`; include the verifier's exact plan-as-own-evidence builder/readiness counterexample and the valid independent artifact shared by source and plan owners. Add the corresponding `gate_lifecycle_operation` counterexample using the Plan 03-17 boundary fixture, instrument the declared-path observation boundary, and require zero calls plus identity-free UNKNOWN and empty remediation when inventory role validation fails. Add one Hypothesis family that generates small already-canonical phase/plan/evidence path sets and declaration permutations, with independent literals for valid sharing and each role collision. Run this complete RED set and require the intended role-reuse failures before committing it as `test(03-18): expose cross-role mapping evidence reuse`. GREEN: in `_validate_inventory_invariants`, finish runtime/byte/canonical path validation before role checks, maintain separate alias-key namespaces for phase, plan, and evidence roles, and reject exact or case/NFC-NFD cross-role intersections plus split duplicate evidence declarations with `_InventoryError("mapping-path-role-conflict")`. Preserve existing same-role duplicate/alias and malformed path codes. In `_validate_manifest_mappings`, after complete shape/count/byte/canonical validation, apply the same phase/plan/evidence disjointness independently to caller-constructed mappings and return `mapping-set-invalid` on violation. The existing lifecycle call order must consume the structured inventory failure before declared-path observation; if the RED gate case exposes a bypass, change only that existing validation call order in `observe_lifecycle_operation`, without adding another validator. Do not reject a single independent EvidenceDeclaration that carries both owners or multiple distinct evidence paths for one owner. Update the 49-count compatibility assertions in `tests/test_handoff_execution_mapping.py` to the 54-item authority produced by Plan 03-17, without regenerating that fixture. Run the entire Task 1 public builder/readiness/direct-mapping/lifecycle/zero-I/O set together, then commit GREEN as `feat(03-18): separate mapping artifact path roles`; if a lifecycle call-order fix was required, commit it separately as `fix(03-18): reject mapping role conflicts before observation`. REFACTOR only demonstrated duplicate alias-set code within these two existing validators; do not add a new public seam.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py -q -k "path_role or own_evidence or evidence or malformed or readiness"</automated>
  </verify>
  <acceptance_criteria>
    - All three exact cross-role pairs, their case/NFC-NFD aliases, and split evidence declarations return the exact required structured code.
    - Malformed/canonical-path failures occur before role checks and retain their established dimension-specific codes.
    - Combined source/plan ownership on one independent evidence artifact and multiple distinct evidence artifacts for one owner remain valid.
    - Direct ManifestMapping violations return `mapping-set-invalid`; no malformed input reaches set comparison, filesystem work, hashing, or identity generation.
    - The exact verifier counterexample fails builder/readiness with `mapping-path-role-conflict`, and the public lifecycle gate returns UNKNOWN, admitted false, identity null, and empty remediation before any declared-path filesystem call.
    - The sole new property family targets canonical role behavior and declaration order; it does not generate filesystem races or lifecycle state.
    - Valid builder/readiness output contains all 54 Plan 03-17 source mappings.
  </acceptance_criteria>
  <done>Inventory, direct mapping, VERIFY readiness, and lifecycle admission share one explicit role-disjointness contract, and invalid structure is rejected before filesystem observation without weakening valid ownership sharing.</done>
</task>

<task type="auto">
  <name>Task 2: Run post-GREEN non-TDD race backstops and final integrated project gates</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-17-SUMMARY.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_execution_mapping.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>tests/test_handoff_execution_mapping.py, tests/test_handoff_lifecycle_gate.py</files>
  <action>As a post-GREEN, non-TDD verification task, after Task 1 is GREEN, make no new RED assertion and no production change. Rerun the existing fixed mapping path-race examples for missing paths, aliases, symlinks, removal during read, identity replacement, and post-observation recheck, plus the complete Plan 03-17 graph/target suite and the Task 1 public role-rejection cases. Then run the remaining drift, identity, and manifest-refresh suites, Ruff, BasedPyright, `task openspec:validate`, `task check`, and `git diff --check` as one post-GREEN integration gate against the 54-item authority. Record the focused and integrated commands, counts, and threat dispositions in `03-18-SUMMARY.md`. Any failure stops independent reverification and is diagnosed without weakening, deleting, or reclassifying assertions. Do not edit production/tests merely to make this verification task appear to have a GREEN change, add another property family, or regenerate/repin the handoff, preview, assignment, golden, tracked evidence, schema/version, dependency, CLI, or package exports.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py -q &amp;&amp; uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_identity.py tests/test_handoff_manifest_refresh.py -q &amp;&amp; uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py &amp;&amp; uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py &amp;&amp; task openspec:validate &amp;&amp; task check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - This post-GREEN, non-TDD task keeps Task 1's complete public builder/readiness/direct-mapping/lifecycle/zero-I/O unit green without adding a second RED cycle.
    - Existing missing, alias, symlink, removal-during-read, identity-change, post-observation recheck, and Plan 03-17 graph/target regressions remain green.
    - Both complete mapping/lifecycle suites, all three remaining focused backstop suites, Ruff, BasedPyright, `task openspec:validate`, `task check`, and `git diff --check` pass.
    - No Plan 03-17 source pin, refresh preview, handoff, assignment, expected preview, golden, tracked evidence, schema/version, dependency, CLI, or package export changes occur.
  </acceptance_criteria>
  <done>The Task 1 role-disjointness unit survives every fixed filesystem race and full project integration gate, leaving independent reverification as the next boundary.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high; any unresolved high-severity threat stops completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller PlanningInventory → builder/readiness | One canonical path may be declared under conflicting roles or split across declarations. |
| caller ManifestMapping → readiness | Direct construction can bypass inventory projection unless role invariants are revalidated. |
| validated declarations → filesystem observation | Structural invalidity must stop before path access and must not create partial green readiness. |
| mapping result → lifecycle decision | A structured mapping failure must remain identity-free unknown rather than an admitted or remediable drift decision. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-18-ROLE | Spoofing/Elevation of Privilege | high | PlanningInventory role namespaces | mitigate | Exact and alias-aware disjoint role sets, fixed three-pair regressions, and stable role-conflict failure before I/O. |
| T-03-18-DIRECT | Tampering/Elevation of Privilege | high | direct ManifestMapping input | mitigate | Independently validate the same role invariant in `_validate_manifest_mappings` and return mapping-set-invalid. |
| T-03-18-VERIFY | Spoofing/Repudiation | high | VERIFY evidence coverage | mitigate | Reject plan-as-own-evidence while preserving one independent artifact with combined owners; prove public readiness result. |
| T-03-18-I/O | Tampering/TOCTOU | high | declared-path observation | mitigate | Structural validation first, zero-call regression on rejection, and fixed missing/symlink/removal/identity-change backstops. |
| T-03-18-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py -q`
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_identity.py tests/test_handoff_manifest_refresh.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py tests/test_handoff_execution_mapping.py tests/test_handoff_lifecycle_gate.py`
- `task openspec:validate`
- `task check`
- `git diff --check`
- Confirm Plan 03-17 remains the sole owner of source-pin, assignment, preview, golden, and tracked-evidence repins.
- Phase 3 remains blocked until code review is clean with Critical 0 / Warning 0, verifier is passed 10/10 with behavior_unverified 0 and overrides 0, HND-03 traceability is Complete, and security review reports open threats 0.
</verification>

<success_criteria>
- Verification gap 2 is closed at PlanningInventory, builder, readiness, direct ManifestMapping, and lifecycle public seams.
- Role-invalid inputs stop before filesystem work and expose no ready result, reusable identity, or remediation.
- Valid independent evidence sharing and all 54 source mappings remain ready under the established operation horizons.
- Plan 03-17 graph/target/identity behavior and every existing filesystem-race backstop remain green.
- `task openspec:validate`, `task check`, and all scoped static/type/test gates pass with no Plan A repin churn.
- All scoped high-severity ASVS Level 1 threats are mitigated; Phase 4 remains blocked until the canonical review/verifier/security exit reports pass.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-18-SUMMARY.md` with RED/GREEN/REFACTOR evidence, role matrix, property results, zero-I/O gate evidence, preserved valid-sharing case, 54-item compatibility, full project checks, and threat dispositions.
</output>
