---
phase: 03-lifecycle-drift-gate
plan: 25
type: tdd
wave: 18
depends_on:
  - 03-24
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
  - tests/test_handoff_migration.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "A valid falsey ManifestMigrationFileOperations adapter supplied to preview_manifest_migration is used instead of the default filesystem adapter."
    - "A valid falsey ManifestMigrationFileOperations adapter supplied to apply_manifest_migration owns the injected failure/effect boundary, and an approved apply cannot bypass it to change the real target."
    - "Both public migration seams choose the default adapter only when operations is None; no dependency, persistence protocol, schema, issue taxonomy, or unrelated behavior changes."
    - "Plan 03-25 hands the completed behavior to Plan 03-23 for fresh independent review/security/verifier evidence without declaring the review clean itself."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      provides: "None-only default selection at both public migration seams"
    - path: tests/test_handoff_migration.py
      provides: "Fixed public preview/apply regressions for a valid falsey operations adapter"
  key_links:
    - from: tests/test_handoff_migration.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      via: "public preview_manifest_migration and apply_manifest_migration calls with falsey ManifestMigrationFileOperations subclasses"
      pattern: "preview_manifest_migration|apply_manifest_migration|__bool__"
---

<objective>
Close the current Phase 3 review blocker by preserving caller-supplied valid migration filesystem adapters regardless of truthiness.

Purpose: Prevent public preview/apply dependency injection from silently falling back to real filesystem operations and bypassing caller-owned fault-containment boundaries.
Output: Two fixed RED-to-GREEN public-seam regressions and two None-only adapter-selection fixes.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
@.agents/skills/self-review/SKILL.md
@.agents/skills/verify-change/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-24-PLAN.md
@.planning/phases/03-lifecycle-drift-gate/03-24-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
@tests/test_handoff_migration.py
@tests/test_handoff_manifest_refresh.py

<interfaces>
The confirmed seams are the public `preview_manifest_migration` and `apply_manifest_migration` functions. Their typed adapter input is `ManifestMigrationFileOperations | None`: every supplied instance of the supported adapter class, including a subclass whose `__bool__` returns `False`, is caller-owned; only literal `None` selects `ManifestMigrationFileOperations()`.

The current review probe and blocker concern a valid falsey subclass. Migration currently has no `migration-operations-invalid` issue code or established wrong-runtime-type result shared by preview and apply, while refresh has its own distinct preview/apply taxonomy. Therefore this plan does not invent wrong-type migration validation or a new public issue code. That omission does not narrow CR-01: both reported public seams are regression-tested with a valid supported adapter, including target preservation on apply. If execution discovers existing migration authority that requires wrong-type runtime validation, stop and return that contract question to gap planning rather than adding an ungrounded result.
</interfaces>
</context>

## Artifacts this plan produces

- No canonical OpenSpec, `03-REVIEW.md`, completed SUMMARY, REQUIREMENTS, schema, issue-code taxonomy, dependency, CAS-like persistence, lock protocol, retry, rollback, repair, or route-switch edit.
- One falsey preview adapter that makes its use observable through an injected repository-open failure and call count.
- One falsey apply adapter that makes its use observable through the established create-failure boundary, mutation record, and exact target preservation.
- One None-only selection expression at each existing public migration seam; no helper or second adapter authority.

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on the same fresh, fail-closed drift decision. | 03-25 then 03-23 | COVERED | Migration effect-boundary injection cannot be silently bypassed before final independent exit evidence. |
| REQ | HND-03 / HARD-R2 | Incomplete or unsafe lifecycle evidence stops progression before unintended writes. | 03-25 then 03-23 | COVERED | Fixed public preview/apply examples preserve caller-owned observation/effect boundaries. |
| RESEARCH | — | Reuse existing public seams, fixed filesystem examples, no new dependency, minimal local change, focused/static/project gates. | 03-25 | COVERED | Two existing lines and one existing migration test file are the only implementation surfaces. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | The approved gap brief supplies the current locked scope; there are no deferred CONTEXT decisions. |
| REVIEW | CR-01 | Valid falsey migration operations adapters are discarded and default real filesystem operations run. | 03-25 | COVERED | Separate public preview and apply RED-to-GREEN slices prove the supplied adapter is used. |
| PLANNING BRIEF | UD-01 | Keep the current finding at Critical; fix by selecting the default only for None. | 03-25 | COVERED | No weakening, suppression, or truthiness-based fallback remains. |
| PLANNING BRIEF | UD-02 | Do not add an ungrounded wrong-type adapter contract or issue code. | 03-25 | COVERED | The plan records why valid-adapter closure is complete under the existing typed migration contract. |
| PLANNING BRIEF | UD-03 | Preserve canonical OpenSpec, current review, completed history, dependencies, and unrelated code. | 03-25 | COVERED | Only migration source/test files change during execution. |
| PLANNING BRIEF | UD-04 | Resume fresh 03-23 review/security/verifier only after the gap is green. | 03-25 then 03-23 | COVERED | 03-25 does not self-declare a clean review. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve a falsey valid adapter at the migration preview seam</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md (current CR-01 only; retain the file unchanged)
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py (`ManifestMigrationFileOperations`, `_failure`, `_resolve_target`, `preview_manifest_migration`)
    - tests/test_handoff_migration.py (`ReadOnlyCountingOperations`, `_write_repository`, `_inputs`, `_preview`, public preview failure examples)
    - tests/test_handoff_manifest_refresh.py (`FalseyFaultInjectingRefreshOperations` as the existing falsey-adapter analog)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py, tests/test_handoff_migration.py</files>
  <behavior>
    - RED: a valid `ManifestMigrationFileOperations` subclass whose `__bool__` returns `False` is supplied to public `preview_manifest_migration`; its `open_parent_directory` probe is reached exactly once instead of the default adapter.
    - RED: the probe raises its injected `OSError`, so preview returns the existing exact `Failure` code `migration-repository-unreadable`, category `IssueCategory.INPUT`, and `KnownState.UNKNOWN`; the target and repository tree remain byte-for-byte unchanged.
    - GREEN control: omitting operations or passing literal `None` retains the existing default-adapter preview behavior.
  </behavior>
  <action>RED: add a narrowly named falsey preview operations subclass and public regression `test_preview_uses_falsey_supplied_operations_without_default_fallback`. Its `__bool__` returns `False`; its `open_parent_directory` increments a counter and raises `OSError`. Invoke `preview_manifest_migration` with otherwise valid `_write_repository` / `_inputs` arguments and this adapter. Assert the existing exact structured `migration-repository-unreadable` INPUT/UNKNOWN result, one supplied-adapter call, exact target bytes, and unchanged repository tree. Run only this node and require RED because the current truthiness expression discards the adapter, returns `Success`, and leaves the supplied counter at zero. Do not mock a private helper or inspect a local variable.

GREEN: change only preview's adapter selection to construct `ManifestMigrationFileOperations()` when `operations is None`, otherwise retain the supplied object. Do not add an adapter validator, issue code, coercion, broad catch, helper abstraction, or filesystem behavior. Run the focused node plus the existing deterministic/read-only preview controls. Commit the RED and GREEN slices separately according to the execute-plan TDD protocol.</action>
  <acceptance_criteria>
    - Public `preview_manifest_migration` reaches the supplied falsey adapter's `open_parent_directory` exactly once and returns existing `Failure` code `migration-repository-unreadable`, `IssueCategory.INPUT`, and `KnownState.UNKNOWN`.
    - The preview target and complete isolated repository tree remain byte-for-byte equal to their pre-call snapshots, with no mutation or staging residue.
    - The preview implementation contains no truthiness fallback: `ManifestMigrationFileOperations()` is selected only when `operations is None`; every supplied supported adapter is retained.
    - Existing deterministic, read-only, explicit-adapter, and default/None preview behavior remains green without a new issue code or public contract.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_migration.py::test_preview_uses_falsey_supplied_operations_without_default_fallback -q &amp;&amp; uv run pytest tests/test_handoff_migration.py -q -k "preview and (deterministic or without_mutation or falsey)"</automated>
  </verify>
  <done>Public migration preview uses the supplied supported adapter regardless of truthiness, propagates its established structured failure, and performs no unintended filesystem work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve a falsey valid adapter at apply and verify the complete gap</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/self-review/SKILL.md
    - .agents/skills/verify-change/SKILL.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py (`ManifestMigrationFailure`, `_migration_failure_after_staging_at`, `apply_manifest_migration`)
    - tests/test_handoff_migration.py (`MutationRecordingOperations`, `FaultInjectingOperations`, `test_apply_reports_pre_replace_faults_and_preserves_exact_v1`)
    - tests/test_handoff_manifest_refresh.py (`test_apply_uses_falsey_supplied_operations_without_default_fallback`)
    - .planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md (fresh independent exit handoff)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py, tests/test_handoff_migration.py</files>
  <behavior>
    - RED: an exact approved migration preview with a valid falsey fault-injecting adapter reaches that adapter's create boundary, returns `ManifestMigrationFailure`, and does not install the candidate through the default adapter.
    - RED: the existing create-failure evidence remains exact: failure point `CREATE`, target state `V1_PRESERVED`, staging state `UNKNOWN`, cleanup `NOT_NEEDED`, and mutations `['create']`.
    - GREEN: target bytes remain the exact schema-v1 preimage, no staging residue remains, and ordinary supplied/default apply controls retain their existing outcomes.
  </behavior>
  <action>RED: add a falsey subclass of the existing `FaultInjectingOperations` and public regression `test_apply_uses_falsey_supplied_operations_without_default_fallback`. Build a valid preview through the existing helper, freshly approve its exact hash, and supply the falsey adapter configured for the established create failure. Assert the exact existing `ManifestMigrationFailure` point/target/staging/cleanup tuple, mutation record `['create']`, unchanged schema-v1 target bytes, and no staging residue. Run only this node and require RED because the current truthiness expression discards the supplied adapter, applies through the default adapter, changes the target, and returns `Success`.

GREEN: change only apply's adapter selection to construct the default when `operations is None`, otherwise retain the supplied object. Do not add wrong-type runtime validation: the current typed public contract admits only `ManifestMigrationFileOperations | None`, current migration has no grounded operations-invalid issue taxonomy, and CR-01 is fully reproduced with a valid supported subclass at both public seams. Do not alter approval order, preview identity, writer locking, target revalidation, persistence, or failure evidence. Run both new nodes, the complete migration test file, and the existing refresh falsey-adapter analog.

After GREEN, keep refactoring outside the TDD loop per the project skill. Perform `self-review` over the exact source/test diff for truthiness fallback, scope creep, unintended public-contract additions, target-preservation assertions, and AGENTS.md compliance; fix only obvious defects and rerun the focused nodes. Then perform behavior verification through the two public isolated-filesystem tests, run Ruff, BasedPyright, `task check`, and `git diff --check`, and record all results plus the explicit handoff to Plan 03-23 in `03-25-SUMMARY.md`. Do not replace or edit `03-REVIEW.md`, do not claim clean review/security/verifier evidence, and do not advance HND-03 or Phase 4.</action>
  <acceptance_criteria>
    - Public `apply_manifest_migration` reaches the supplied falsey adapter's create boundary and returns existing `ManifestMigrationFailure` with `CREATE`, `V1_PRESERVED`, `UNKNOWN`, and `NOT_NEEDED` evidence plus exact mutation record `['create']`.
    - The approved apply leaves the target byte-for-byte equal to the schema-v1 preimage and leaves no staging residue; it does not return `Success` or install candidate bytes through the default adapter.
    - The apply implementation contains no truthiness fallback: `ManifestMigrationFileOperations()` is selected only when `operations is None`; every supplied supported adapter owns the effect boundary.
    - Both new public regression nodes, the complete migration suite, the existing refresh falsey-adapter analog, Ruff, BasedPyright, `task check`, self-review/behavior verification, and `git diff --check` are green with no unrelated change.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_migration.py::test_preview_uses_falsey_supplied_operations_without_default_fallback tests/test_handoff_migration.py::test_apply_uses_falsey_supplied_operations_without_default_fallback -q &amp;&amp; uv run pytest tests/test_handoff_migration.py tests/test_handoff_manifest_refresh.py::test_apply_uses_falsey_supplied_operations_without_default_fallback -q &amp;&amp; uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py tests/test_handoff_migration.py &amp;&amp; uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py tests/test_handoff_migration.py &amp;&amp; task check &amp;&amp; git diff --check</automated>
  </verify>
  <done>Both public migration seams preserve every valid supplied adapter regardless of truthiness, the real target is protected by the injected apply boundary, project gates pass, and fresh exit review remains owned by 03-23.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller-supplied adapter → migration preview | A supported adapter controls repository observation and must not be replaced because its truth value is false. |
| caller-supplied adapter → approved migration apply | A supported adapter owns fault containment and filesystem effects; fallback can bypass it and mutate the real target. |
| typed migration contract → runtime validation | The existing type accepts supported adapters or None; this gap does not authorize a new wrong-type result taxonomy. |
| completed gap → fresh exit roles | 03-25 proves behavior but cannot author its own clean review, security audit, or verifier result. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-25-ADAPTER | Spoofing/Tampering | high | preview/apply operations selection | mitigate | Select the default only for None and exercise valid falsey subclasses through both public seams. |
| T-03-25-WRITE | Tampering/Denial of Service | high | approved migration apply | mitigate | Inject the established create failure, require exact structured evidence, and prove target bytes/staging state are preserved. |
| T-03-25-CONTRACT | Repudiation/Elevation of Privilege | medium | migration adapter validation taxonomy | accept | The typed public contract already excludes arbitrary objects and migration has no grounded operations-invalid code; no new public result is invented. |
| T-03-25-REVIEW | Repudiation | high | Phase 3 exit evidence | mitigate | Preserve the issues_found review and require fresh 03-23 review/security/verifier coverage after 03-25. |
| T-03-25-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- RED evidence for each public seam must fail on adapter bypass, not on a different exception or malformed setup.
- GREEN focused evidence must show the supplied falsey preview adapter is called exactly once and the supplied falsey apply adapter records exactly the injected create boundary.
- Behavioral evidence must prove the preview tree and apply target remain exact preimages; the apply path leaves no staging residue.
- Regression/static/project evidence: complete migration tests, refresh falsey analog, Ruff, BasedPyright, `task check`, and `git diff --check` all pass.
- Authority integrity: canonical OpenSpec, `03-REVIEW.md`, REQUIREMENTS, completed summaries, dependency metadata, and unrelated production/tests remain unchanged during execution.
- Review integrity: 03-25 records test and self-review evidence only; Plan 03-23 owns any later clean review/security/verifier declaration.
</verification>

<success_criteria>
- Current CR-01 is closed behaviorally at both reported public migration seams through RED then GREEN fixed tests.
- The implementation uses explicit None selection and contains no remaining truthiness fallback for migration operations.
- The plan adds no wrong-type migration contract, issue code, dependency, schema, CAS, or unrelated refactor.
- Focused, full migration, static, project, diff, self-review, and public behavior checks pass.
- Phase 3 remains incomplete at 23/25 executed until 03-25 completes and Plan 03-23 subsequently produces fresh independent exit evidence across all 25 plans.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-25-SUMMARY.md` when done, including separate RED/GREEN evidence for preview and apply, exact target-preservation results, the no-new-wrong-type-contract rationale, self-review/behavior verification results, and handoff to Plan 03-23.
</output>
