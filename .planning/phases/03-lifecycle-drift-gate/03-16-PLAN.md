---
phase: 03-lifecycle-drift-gate
plan: 16
type: tdd
wave: 12
depends_on:
  - 03-15
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
  - tests/test_handoff_identity.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: source_inventory_from_bytes and read_source_inventory validate untrusted outer containers before truthiness, len, iteration, tuple unpacking, path conversion, or filesystem work."
    - "None, object, str, bytes, unsupported containers, malformed source-byte members, and malformed source-path members return Failure with source-files-invalid and expose no partial SourceInventory."
    - "A well-shaped source-byte row with non-bytes content retains source-bytes-invalid, empty accepted sequences retain source-paths-empty, and accepted limit+1 sequences retain source-path-count-limit-exceeded."
    - "Valid list/tuple inputs, canonical path validation, byte bounds, root anchoring, parsing, and reconciliation behavior remain unchanged."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      provides: "Validation-before-use for both public source inventory reader input aggregates"
    - path: tests/test_handoff_identity.py
      provides: "Fixed public reader outer/member/boundary regressions with stable structured failure codes"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "both public readers reject string/bytes/non-Sequence containers before len and validate members before unpack/path use"
      pattern: "source-files-invalid|source_inventory_from_bytes|read_source_inventory"
    - from: tests/test_handoff_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "fixed tables call only the two public reader Result seams"
      pattern: "source_inventory_from_bytes|read_source_inventory"
---

<objective>
Close malformed aggregate handling at both public source inventory reader seams for HND-03 / HARD-R2.

Purpose: Ensure caller-constructed containers and members become stable structured non-success before Python runtime operations can raise or partial source observation work can begin.
Output: Ordered outer/member validation in both readers plus fixed public-seam malformed and boundary regressions.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-13-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-15-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_identity.py

<interfaces>
Preserve the public signatures of `source_inventory_from_bytes` and `read_source_inventory`, accepted non-string `Sequence` inputs, `SourceIdentityLimits`, and existing semantic issue codes. Add `source-files-invalid` as the stable outer/member runtime-shape failure. Preserve `source-paths-empty`, `source-path-count-limit-exceeded`, `source-path-invalid`, `source-path-noncanonical`, `source-bytes-invalid`, root/path/read failures, parser failures, and limit failures for inputs that have already passed aggregate shape validation.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, test file, public function, private helper, root export, CLI operation, dependency, or schema artifact.
- New stable public issue-code value:
  - `source-files-invalid`
- Modified existing production symbols:
  - `source_inventory_from_bytes`
  - `read_source_inventory`
- New fixed test nodes:
  - `tests/test_handoff_identity.py::test_source_inventory_from_bytes_rejects_malformed_file_inputs`
  - `tests/test_handoff_identity.py::test_read_source_inventory_rejects_malformed_path_inputs`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on one fresh fail-closed drift decision. | 03-14, 03-15, 03-16 | COVERED | Public source observation no longer escapes through Python exceptions. |
| REQ | HND-03 / HARD-R2 | Malformed or incomplete canonical inspection stops without partial green evidence. | 03-16 | COVERED | Both source inventory readers return structured non-success before use. |
| RESEARCH | — | Validate untrusted aggregate outer/container/member/field values before normalization, dereference, sorting, hashing, or I/O. | 03-16 | COVERED | Fixed examples retain the sole approved property family unchanged. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Gap closure is grounded in ROADMAP, canonical OpenSpec, verification, review, and accumulated Phase 3 decisions. |
| VERIFICATION | gap 1 / REVIEW CR-01 | Stale rejection exposes a clean identity that admits its next replay. | 03-14 | COVERED | Wave 11 disjoint lifecycle-gate plan. |
| VERIFICATION | gap 2 / REVIEW CR-02 | Repository-root replacement can return detached source content. | 03-15 | COVERED | Predecessor plan owns the same files and establishes root-race behavior first. |
| VERIFICATION | gap 3 / REVIEW CR-03 | Public source readers raise for malformed containers/members. | 03-16 | COVERED | Both public reader functions receive fixed outer/member/boundary examples. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate source_inventory_from_bytes aggregates before runtime use</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py (`_validate_inventory`, `_validate_explicit_matches`, `reconcile_source_items`, `source_inventory_from_bytes`, `read_source_inventory`)
    - tests/test_handoff_identity.py (`test_reconcile_rejects_malformed_inventory_runtime_shapes`, `test_reconcile_rejects_malformed_explicit_match_runtime_shapes`)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, tests/test_handoff_identity.py</files>
  <behavior>
    - RED: `source_inventory_from_bytes` receives `None`, `object()`, `str`, `bytes`, or a set and returns `Failure("source-files-invalid")` instead of raising from truthiness or len.
    - RED: one non-tuple member, list member, tuple whose length is not two, or tuple whose path is neither str nor Path returns `source-files-invalid` before unpack or path conversion.
    - GREEN: a shaped `(canonical_path, non_bytes)` row retains `source-bytes-invalid`, while malformed canonical path text retains its existing path code after aggregate shape is accepted.
    - GREEN: empty accepted list/tuple values retain `source-paths-empty`, accepted `max_items + 1` inputs retain `source-path-count-limit-exceeded`, and valid list/tuple inputs produce the same deterministic SourceInventory.
  </behavior>
  <action>Follow the exact validation-before-use ordering in `_validate_inventory` as invoked by `reconcile_source_items`: prove the outer aggregate type, collection type and bound, member class, and member field runtime types before normalization, identity construction, allocation, or member-derived work. Copy the one-seam table-test shape of `test_reconcile_rejects_malformed_inventory_runtime_shapes` and `test_reconcile_rejects_malformed_explicit_match_runtime_shapes`: one parameterized table with explicit `pytest.param(..., id=...)` rows and `cast(Any, ...)`, one public function call per row, exact `Failure` code, and no `value`.

SLICE 1 RED — `source_inventory_from_bytes` only: add `test_source_inventory_from_bytes_rejects_malformed_file_inputs` with finite outer/member rows plus companion rows for empty accepted sequences, accepted limit+1, shaped non-bytes content, malformed canonical path text, and valid list/tuple inputs. Call only `source_inventory_from_bytes`. Run `uv run pytest tests/test_handoff_identity.py::test_source_inventory_from_bytes_rejects_malformed_file_inputs -q`; require failure from the current TypeError/ValueError or wrong code, then commit `test(03-16): expose malformed source byte inputs`.

SLICE 1 GREEN — modify only the `source_inventory_from_bytes` validation path. Keep `_valid_limits` first; reject `str`, `bytes`, and non-`Sequence` outers before truthiness or `len`; preserve empty/count-limit precedence after the outer type is known; validate the complete collection as exact two-item tuples whose path is `str` or `Path` before the existing loop unpacks any member. Only then retain `source-bytes-invalid` for a non-bytes content field and existing canonical-path semantics. Add no helper and do not anticipate `read_source_inventory`. Run the focused node, `uv run pytest tests/test_handoff_identity.py -q`, and `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`; commit `feat(03-16): validate source byte inputs before use` only when all are green.</action>
  <acceptance_criteria>
    - The named `source_inventory_from_bytes` public-seam test covers its required outer, exact-two-tuple member, path-member, empty, limit+1, shaped-content, canonical-path, and valid list/tuple rows with fixed examples.
    - Every malformed outer/member row returns exact `Failure("source-files-invalid")` without TypeError, ValueError, unpack, path conversion, or partial `value`.
    - Shaped rows preserve `source-bytes-invalid` and canonical path codes; empty and limit+1 accepted sequences preserve `source-paths-empty` and `source-path-count-limit-exceeded`.
    - Slice 1 has its own RED then GREEN commits and leaves `read_source_inventory` unchanged for Slice 2.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_identity.py::test_source_inventory_from_bytes_rejects_malformed_file_inputs -q</automated>
  </verify>
  <done>The byte-backed public reader converts malformed outer/member inputs into stable structured failures before runtime use while preserving its established semantic codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate read_source_inventory aggregates before filesystem work</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py (`_validate_inventory`, `reconcile_source_items`, `read_source_inventory`)
    - tests/test_handoff_identity.py (`test_reconcile_rejects_malformed_inventory_runtime_shapes`, `test_reconcile_rejects_malformed_explicit_match_runtime_shapes`)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, tests/test_handoff_identity.py</files>
  <behavior>
    - RED: `read_source_inventory` receives `None`, `object()`, `str`, `bytes`, or a set and returns `Failure("source-files-invalid")` without filesystem access.
    - RED: a source-path member that is neither str nor Path returns `source-files-invalid` before `_canonical_source_path`, repository resolution, open, or traversal.
    - GREEN: empty accepted list/tuple values retain `source-paths-empty`, accepted `max_items + 1` inputs retain `source-path-count-limit-exceeded`, and malformed canonical path text retains its existing path code.
    - GREEN: valid list/tuple reads stay deterministic, and the Plan 03-15 root replacement table remains exact `source-root-identity-changed`.
  </behavior>
  <action>Start this slice only after Task 1 has its GREEN commit. Copy `_validate_inventory` and `reconcile_source_items` ordering: prove aggregate and member runtime shape before normalization or member use. Copy the one-seam parameter-table shape of `test_reconcile_rejects_malformed_inventory_runtime_shapes` and `test_reconcile_rejects_malformed_explicit_match_runtime_shapes`: explicit `pytest.param(..., id=...)` rows with `cast(Any, ...)`, one `read_source_inventory` call per row, exact `Failure` code, and no `value`. Do not merge the two public readers into one RED or one GREEN.

SLICE 2 RED — `read_source_inventory` only: add `test_read_source_inventory_rejects_malformed_path_inputs` with finite outer/path-member rows and explicit boundary companions for empty accepted sequences, accepted limit+1, malformed canonical path text, and valid list/tuple reads. Instrument the filesystem boundary so malformed aggregate rows also prove no repository resolution, open, or traversal occurs. Call only `read_source_inventory`. Run `uv run pytest tests/test_handoff_identity.py::test_read_source_inventory_rejects_malformed_path_inputs -q`; require failure from the current TypeError or wrong result/code, then commit `test(03-16): expose malformed source path inputs`.

SLICE 2 GREEN — modify only the `read_source_inventory` input validation path. Keep `_valid_limits` first; reject `str`, `bytes`, and non-`Sequence` outers before truthiness or `len`; preserve empty/count-limit precedence after the outer type is known; validate every member as `str` or `Path` before `_canonical_source_path`, `Path.resolve`, root stat/open, or traversal. Return `source-files-invalid` only for aggregate runtime shape; do not coerce, stringify arbitrary objects, catch broad exceptions, weaken limits, or alter valid ordering. Add no private helper in this plan. Run the focused node, both 03-16 nodes together, the Plan 03-15 root-replacement node, `uv run pytest tests/test_handoff_identity.py -q`, and the declared Ruff/BasedPyright checks; commit `feat(03-16): validate source path inputs before use` only when all are green.

After both GREEN slices, no REFACTOR is planned: do not extract or add a private helper and do not create a REFACTOR commit. Record in the summary that no behavior-preserving cleanup remained in this plan; any later cleanup requires a separate plan. Do not add a Hypothesis family, dependency, schema operation, alternate reader, retry, repair, rollback, route switch, canonical OpenSpec mutation, or final-completion state.</action>
  <acceptance_criteria>
    - The named `read_source_inventory` public-seam test covers its required outer/path-member, no-filesystem-work, empty, limit+1, canonical-path, and valid list/tuple rows with fixed examples.
    - Every malformed outer/path-member row returns exact `Failure("source-files-invalid")` without TypeError, filesystem access, or partial `value`.
    - Empty, limit+1, and canonical-path rows preserve `source-paths-empty`, `source-path-count-limit-exceeded`, and existing path codes.
    - The 03-15 root replacement test remains exact `source-root-identity-changed`; valid list/tuple reads remain deterministic.
    - Slice 2 has its own RED then GREEN commits after Slice 1; no private helper or REFACTOR commit is added, and the summary records that decision.
    - Both slices add no Hypothesis family, broad exception sanitizer, coercion, dependency, or alternate validation authority; independent verification still owns phase completion.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_identity.py::test_read_source_inventory_rejects_malformed_path_inputs tests/test_handoff_identity.py::test_source_inventory_rejects_repository_root_replacement_during_read -q</automated>
  </verify>
  <done>The filesystem-backed public reader rejects malformed aggregate inputs before filesystem work while preserving root-race and established semantic behavior.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Security Assets

| Asset | Security property |
|---|---|
| Canonical source inventory | Is produced only from fully validated aggregate inputs and the same validated repository root. |
| Reusable lifecycle `decision_identity` | Is never derived from an exception-skipped or partially observed source inventory and cannot replay a stale rejection into admission. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller aggregate → public source readers | Python annotations do not enforce outer/container/member runtime shape. |
| validated source declarations → path and filesystem observation | Only fully validated declarations may reach normalization, limits, root opening, or reads. |
| repository path → root descriptor | Root substitution is a separate high-severity boundary mitigated by predecessor Plan 03-15. |
| prior identity → lifecycle gate | Stale replay is a separate high-severity boundary mitigated by Plan 03-14. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-16-INPUT | Tampering/Denial of Service | high | `source_inventory_from_bytes`, `read_source_inventory` | mitigate | Validate outer Sequence and complete member shape before all runtime/semantic operations; fixed public tables require stable Failure and no partial value. |
| T-03-16-ROOT | Tampering/Elevation of Privilege | high | `read_source_inventory` repository root | transfer | Plan 03-15 performs pre-open/descriptor/post-read identity binding; 03-16 reruns its exact public regression after input validation changes. |
| T-03-16-STALE | Spoofing/Elevation of Privilege | high | lifecycle stale decision identity | transfer | Plan 03-14 binds identity to the complete stale decision; final combined verification reruns its two-step replay node. |
| T-03-16-SC | Tampering | low | package supply chain | accept | No package install, dependency change, or schema operation occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_identity.py::test_source_inventory_from_bytes_rejects_malformed_file_inputs tests/test_handoff_identity.py::test_read_source_inventory_rejects_malformed_path_inputs -q`
- `uv run pytest tests/test_handoff_identity.py::test_source_inventory_rejects_repository_root_replacement_during_read -q`
- `uv run pytest tests/test_handoff_identity.py -q`
- `uv run pytest tests/test_handoff_lifecycle_gate.py::test_stale_rejection_identity_cannot_be_replayed_into_admission -q`
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q --no-cov`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- `task check`
- `git diff --check`
- Confirm only the declared source/test files changed in this plan, all three new gap families are green together, the existing checkbox-normalization test remains the sole Phase 3 Hypothesis family, and no dependency/schema/canonical OpenSpec/later-phase surface changed.
- Phase 03 remains incomplete until `$gsd-verify-work` independently confirms the three verifier gaps are closed.
</verification>

<success_criteria>
- Verification gap 3 and REVIEW CR-03 are closed at both public source reader seams.
- Malformed outer/member inputs return one stable aggregate-shape issue without exceptions, I/O, or partial inventory.
- Existing field/semantic/root/limit codes and valid reader behavior remain unchanged.
- The predecessor root-substitution regression and parallel stale-replay regression remain green in the final combined gate.
- No unrelated refactor, dependency, property family, schema operation, or final-completion claim is added.
- All high-severity scoped threats are mitigated directly or by the named blocking predecessor/sibling plan.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-16-SUMMARY.md` with both sequential RED/GREEN slice commits, an explicit no-REFACTOR record, the malformed-family failure matrix, exact preserved-code companions, combined three-gap verification results, threat dispositions, and an explicit statement that independent verification still owns Phase 03 completion.
</output>
