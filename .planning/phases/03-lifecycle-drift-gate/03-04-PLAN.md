---
phase: 03-lifecycle-drift-gate
plan: 04
type: tdd
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
  - tests/test_handoff_lifecycle_drift.py
autonomous: true
requirements:
  - HND-03
gap_closure: true
must_haves:
  truths:
    - "HND-03 / HARD-R2: malformed canonical structured results classify as unknown through the public classifier and never raise."
    - "An invalid Success payload or invalid artifact tuple member yields canonical-observation-incomplete with no partial artifact, source-ID, or progress evidence."
    - "Complete canonical observations retain the existing clean, drifted, and checkbox-only behavior."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      provides: "Type-safe completeness validation before any canonical observation or artifact attribute access"
    - path: tests/test_handoff_lifecycle_drift.py
      provides: "Public-seam regressions for malformed Success payloads and nested artifact members"
  key_links:
    - from: tests/test_handoff_lifecycle_drift.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      via: "classify_canonical_source_drift receives malformed Result payloads at the public seam"
      pattern: "classify_canonical_source_drift"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      to: CanonicalSourceDriftDecision
      via: "_is_complete_observation rejects shape/type failures before classification reads nested fields"
      pattern: "_is_complete_observation|canonical-observation-incomplete"
---

<objective>
Close the malformed canonical-observation gap in HND-03 / HARD-R2 with test-first, public-seam fail-closed behavior.

Purpose: Prevent malformed structured success values from crashing the shared lifecycle gate or contributing partial green evidence.
Output: Hardened canonical observation completeness checks and focused regression tests.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-01-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
@tests/test_handoff_lifecycle_drift.py

<interfaces>
Preserve the public `classify_canonical_source_drift(expected, observed) -> CanonicalSourceDriftDecision` seam. Follow the research order at this seam: validate the raw `Success` payload and nested members before dereference, then apply the existing canonical checks and projection. Malformed complete-looking values use the existing stable issue code `canonical-observation-incomplete`; the returned unknown decision keeps `drifted_artifact_paths=()`, `changed_source_item_ids=()`, and `progress_update_candidate=None`. `manifest_v2.py` is an ordering and exact-validation analog, not an exhaustive schema for these dataclasses. Do not add a second classifier or alter checkbox normalization.
</interfaces>
</context>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py`
  - modified public behavior: `classify_canonical_source_drift`
  - hardened internal validation seam: `_is_complete_observation`
- `tests/test_handoff_lifecycle_drift.py`
  - new public-seam malformed-payload and malformed-artifact regression cases
- `.planning/phases/03-lifecycle-drift-gate/03-04-SUMMARY.md`
  - RED/GREEN/REFACTOR evidence and focused verification results

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Later lifecycle operations receive one fresh fail-closed decision. | 03-04 | COVERED | This plan prevents malformed canonical evidence from escaping as an exception. |
| REQ | HND-03 / HARD-R2 | Incomplete canonical observation stops progression. | 03-04 | COVERED | Uses the existing public classifier and unknown issue code. |
| VERIFICATION | Gap 4 | Malformed canonical structured observations can raise instead of unknown. | 03-04 | COVERED | Top-level and nested malformed values receive public regressions. |
| RESEARCH | G4 | Raw canonical values must be validated before nested access, cardinality, ordering, normalization, or digest comparison. | 03-04 | COVERED | Reuses the existing `_unknown` projection and `manifest_v2.py` validation-order analog without adding another sanitizer. |
| VALIDATION | 03-04-01, 03-04-02 | Nyquist requires the named malformed-payload slice followed by the complete drift-file backstop. | 03-04 | COVERED | Task commands match `03-VALIDATION.md` exactly. |
| PATTERNS | lifecycle drift classifier | Preserve frozen decisions, public classifier tests, and the sole checkbox Hypothesis family. | 03-04 | COVERED | Partial analogs guide ordering but do not replace the actual observation contract. |
| CONTEXT | — | No Phase 3 CONTEXT.md exists. | — | EXCLUDED | Continuation without context was explicitly approved. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject malformed structured Success payloads before dereference</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - .planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - tests/test_handoff_lifecycle_drift.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/models.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py, tests/test_handoff_lifecycle_drift.py</files>
  <behavior>
    - `Success(None)` in either expected or observed position returns unknown with only `canonical-observation-incomplete`.
    - A `Success` payload of an unrelated object type returns the same unknown decision without `AttributeError`.
    - The result exposes no drifted paths, changed source IDs, or progress update candidate.
  </behavior>
  <action>RED: add focused tests named around malformed structured payloads that call `classify_canonical_source_drift` directly and prove the current attribute dereference raises. GREEN: apply the research trust-boundary order: make `_is_complete_observation` accept an untrusted object, require `isinstance(observation, CanonicalSourceObservation)` before reading `artifacts`, `progress`, `source_items`, or `changed_source_item_ids`, and only then continue into the existing canonical checks and unknown projection. Use `manifest_v2.py` only as the closest validate-before-transform analog; its API surface is not the complete acceptance list for `CanonicalSourceObservation`. Preserve `canonical-observation-incomplete` and existing `Failure` issue propagation. REFACTOR: remove only duplicated test construction discovered by the two cases and rerun the focused slice. Do not catch broad exceptions in the classifier or change valid classification behavior.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_drift.py -q -k "malformed_structured_payload"</automated>
  </verify>
  <acceptance_criteria>
    - Both malformed top-level payload cases return `DriftState.UNKNOWN` and `canonical-observation-incomplete`.
    - The focused command exits 0 and no test accepts an exception as the expected outcome.
    - Unknown output contains empty evidence tuples and a null progress candidate.
  </acceptance_criteria>
  <done>Top-level malformed structured success values are converted to a complete unknown decision before attribute access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject malformed nested artifact members before field access</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - tests/test_handoff_lifecycle_drift.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/models.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
    - .planning/phases/03-lifecycle-drift-gate/03-01-SUMMARY.md
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py, tests/test_handoff_lifecycle_drift.py</files>
  <behavior>
    - An artifacts tuple containing `None` or another non-`CanonicalArtifactObservation` member returns unknown.
    - Malformed nested artifact field types are rejected before cardinality, ordering, uniqueness, or digest comparisons.
    - Existing complete clean, drifted, and checkbox-only tests remain green.
  </behavior>
  <action>RED: construct complete-looking `CanonicalSourceObservation` values whose `artifacts` tuple contains an invalid member and whose artifact fields have invalid runtime types, then assert public unknown behavior. GREEN: continue the raw-validation stage in `_is_complete_observation`: require the artifacts container to be an exact tuple, require every member to be a `CanonicalArtifactObservation`, and validate each nested runtime field before reading, encoding, sorting, deduplicating, or comparing `kind`, `path`, `raw_sha256`, or `specification_sha256`. After raw shape and types pass, run the existing cardinality, canonical ordering, uniqueness, digest, progress, source-state, and changed-ID checks, then reuse the existing public unknown projection. Treat the PATTERNS analogs as ordering guidance rather than an exhaustive field list; preserve every invariant already enforced by the current classifier. REFACTOR: keep one validation path and avoid a parallel sanitizer or coercion layer. Run the entire lifecycle-drift test file after the focused RED/GREEN slice.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_drift.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Invalid tuple members and invalid nested field types return `canonical-observation-incomplete` without raising.
    - All pre-existing lifecycle drift tests, including the sole Hypothesis checkbox family, pass.
    - The test file still contains exactly one `@given` family and no new property family.
  </acceptance_criteria>
  <done>Every malformed canonical observation shape is rejected safely while valid classification semantics remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| structured Result payload → canonical classifier | Runtime values may claim success while carrying malformed top-level or nested objects. |
| canonical classifier → lifecycle gate | Exceptions or partial evidence here can bypass the intended fail-closed aggregate decision. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04-01 | Tampering/Elevation of Privilege | high | `_is_complete_observation` validation order | mitigate | Validate exact observation and artifact member types before all attribute access; malformed values become unknown with no reusable evidence. |
| T-03-04-02 | Denial of Service | high | `classify_canonical_source_drift` | mitigate | Public regressions cover malformed success payloads and nested members without broad exception-based control flow. |
| T-03-04-SC | Tampering | low | package supply chain | accept | No package install or dependency change is in scope. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_drift.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py`
- `git diff --check`
</verification>

<success_criteria>
- Verification gap 4 is closed at the existing public classifier seam.
- Malformed structured successes never raise or expose partial green evidence.
- Existing canonical drift and checkbox-only behavior remains green.
- No canonical OpenSpec artifact, mapping code, gate code, package export, or CLI surface is modified.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-04-SUMMARY.md` with RED/GREEN/REFACTOR evidence, exact focused commands, and the malformed cases proven unknown.
</output>
