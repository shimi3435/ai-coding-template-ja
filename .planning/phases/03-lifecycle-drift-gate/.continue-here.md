---
context: phase
phase: 03-lifecycle-drift-gate
task: 0
total_tasks: 2
status: paused
last_updated: 2026-08-09T06:01:00.641Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> 以下は実際の失敗から確定した制約である。再開前に各項目を確認すること。

- [ ] CONSTRAINT: stale-green-evidence — Plan 03-23 の clean review、970 tests、10/10 verification は後続 review で失効した。現在の Phase 3 完了根拠として使用しない。
- [ ] CONSTRAINT: frozen-gap-inventory — 最新 `03-REVIEW.md` は Blocker 7件・Warning 1件。`03-VERIFICATION.md` と `STATE.md` の Critical 4件・Warning 1件は古い inventory であり、再開時は先に整合させる。
- [ ] CONSTRAINT: non-convergent-gap-loop — 個別 gap 修正後に新しい public-boundary gap が繰り返し発見され、未実行計画が 03-30〜03-50、64 tasks まで拡大した。新路線の仕様判断なしに実装を再開しない。

**すべて確認するまで Phase 3 の実装・追加計画・Phase 4 への進行を禁止する。**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| test-suite-green-as-goal-proof | 970 tests pass と旧 clean review を Phase goal 達成と解釈した後、未検証 public seam から blocker が再発見された | blocking | 最新 frozen review の反例を acceptance gate として扱い、旧 green evidence を再利用しない |
| piecemeal-gap-closure | 一件ずつ閉じた後に隣接境界の gap が増え、計画範囲が 21 plans / 64 tasks へ膨張した | blocking | 新しい OpenSpec change / ADR で製品方向と境界を再確定し、旧 Gate E を継続する場合だけ bounded plan を再作成する |
| stale-metadata-authority | REVIEW、VERIFICATION、SECURITY、STATE の対象 HEAD と gap 件数が一致しない状態が発生した | blocking | Git commit と frozen inventory を一つに固定し、review → security → verifier → traceability の順序を再構成する |

<current_state>

Phase 3 は未完了のまま停止した。現 branch は `agent/harden-openspec-gsd-handoff-lifecycle`、HEAD は `77a8c02`。HND-03 は Pending、OpenSpec task 3.1 は未チェック、Phase 4〜6 は未着手かつ blocked のまま維持する。

最新 `03-REVIEW.md` は `status: issues_found`、Blocker 7件・Warning 1件を `80dcfef` 時点の frozen inventory として記録する。現 `03-VERIFICATION.md` は一つ前の Critical 4件・Warning 1件、3/10 判定であり、最新 review と件数が一致しない。`03-SECURITY.md` も旧 clean review を前提とするため current exit evidence ではない。

Gate E の仕様追加は `77a8c02` に commit 済み。ただし実装計画 03-30〜03-50 は全て未実行・未追跡。`03-PATTERNS.md` と `03-VALIDATION.md` も未コミット変更である。

</current_state>

<completed_work>

Completed work:

- Phase 1 と Phase 2 は履歴上完了。
- Phase 3 Plans 03-01〜03-29 は実装・証跡を作成済み。ただし Phase 3 全体の完了判定は失効。
- 最後に記録された full-suite green evidence は summary commit `0d8eb67`。`task check` は 970 tests pass を記録し、対象 production code の最終 commit は `30199ab`。後続 review が未検証反例を発見したため、どちらも現在の Phase 3 成功証拠ではない。
- 最新 gap freeze は review commit `f8173a6`。Blocker 7件・Warning 1件を再現手順付きで保存済み。
- Gate E failure-boundary 仕様は `77a8c02` に保存済み。

</completed_work>

<remaining_work>

現在の Phase 3 では実行しない:

- Plan 03-30 Task 1、および Plans 03-30〜03-50 の全64 tasks。
- Phase 3 の追加 gap plan 作成。
- Phase 4〜6 の planning / execution。

次の独立作業:

- Node 依存と skill 自動更新の意味を、新しい OpenSpec change と ADR で確定する。
- 自動更新を silent latest apply にするか、検知・検証・diff/PR・承認付き apply にするか確定する。
- 新仕様レビュー後、現 change を supersede / archive / 明示再開のどれにするか人間が決定する。

</remaining_work>

<decisions_made>

- Phase 3 の追加計画と実装を停止する。gap closure の非収束と路線変更候補が理由。
- Phase 3 は incomplete / superseded candidate として保存し、完了扱いしない。
- HND-03、OpenSpec task 3.1、Phase 3、Phase 4 unblock の metadata を変更しない。
- Phases 4〜6 を旧 roadmap のまま開始しない。
- 現在の未コミット計画・pattern・validation変更は削除も commit もせず、そのまま保全する。

</decisions_made>

<blockers>

最新 frozen inventory:

- CR-01: refresh が detached parent を検証し、canonical target 未更新でも Success を返せる。
- CR-02: refresh production API が一つの change、Phase 02、固定件数、package 外 test fixture に依存する。
- CR-03: NFD path から存在しない NFC persisted identity を作り、read → persist → reuse が成立しない。
- CR-04: drift classifier / lifecycle gate が malformed structured getter と outer Result の通常例外を漏出する。
- CR-05: supported persistence adapter の通常例外が taxonomy / cleanup を迂回し、staging residue を残せる。
- CR-06: accepted Sequence の列挙途中例外が public source / preview seam から漏出する。
- CR-07: 複数 public Result / decision API が malformed root / scalar / wrapper の通常例外を漏出する。
- WR-01: migration preview / apply が unsupported collection / operations を structured failure にしない。

</blockers>

## Reuse Candidates

以下は概念または局所実装の候補であり、現状のまま安全に再利用できる保証ではない。

- exact commit / hash pin、versioned manifest、deterministic identity。
- read-only preview と exact approval hash の分離。
- drift / stale 判定を mutation 前に置く設計。
- atomic staging / replace、fresh proof、receipt のテストパターン。
- `skills.lock.json` の source / commit / license / sha256 と既存 upstream drift check。
- Phase 1〜2 の stable mapping と migration fixtures。ただし Node updater の domain model に合わせて選別する。

## Retirement Candidates

- change ID、Phase 02、42/49/54件、`tests/fixtures` path を埋め込んだ refresh 実装。
- OpenSpec requirements から GSD plan/evidence への54-item固定 mapping。新 updater が同じ handoff を使わない場合は historical provenance のみにする。
- Gate E の全 public API totality matrix、authority repin、review-chain closeoutを一括実行する Plans 03-30〜03-50。
- 現 ROADMAP の Phase 4 ownership、Phase 5 recovery、Phase 6 finalize を旧 handoff domain のまま実装する経路。

## Required Reading (in order)

1. `.planning/phases/03-lifecycle-drift-gate/.continue-here.md` — 停止判断と禁止事項。
2. `.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md` — 最新 Blocker 7件・Warning 1件と再現手順。
3. `.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md` — 旧4件の反例と3/10判定。最新件数ではない点に注意。
4. `openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md` と対応 spec — commit `77a8c02` の Gate E 仕様。
5. `.planning/ROADMAP.md` と `openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md` — Phase 4 block と未完了 canonical gates。
6. `03-30-PLAN.md`〜`03-50-PLAN.md` — 現 handoff を明示再開すると決めた場合だけ読む。自動実行しない。

## Critical Anti-Patterns (do NOT repeat these)

- 旧 clean report や test pass を、後続 counterexample より優先しない。
- 一件閉じるたびに新計画を追加する loop を再開しない。まず製品境界を再仕様化する。
- 最新 review と対象 commit が違う SECURITY / VERIFICATION / STATE を current authority として使わない。

## Infrastructure State

- Background processes: 関連 server、watcher、pytest、GSD process なし。
- Async external jobs: `.planning/async-jobs/` に非終端 job manifest なし。
- External/manual setup: なし。
- Working tree: `03-PATTERNS.md`、`03-VALIDATION.md` が変更済み。03-30〜03-50 PLAN は未追跡。handoff commit に含めない。

## Pre-Execution Critique Required

- Design artifact: `openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md`、対応 spec、Plans 03-30〜03-50。
- Critique focus: Node/updater 路線でも OpenSpec–GSD handoff lifecycle が製品機能として必要か、Gate E の64 tasksが最小境界か、silent auto-updateを禁止するか、どの部品を再利用するか。
- Gate: 新しい OpenSpec change / ADR のレビューと旧 change の明示 disposition が完了するまで、Plan 03-30 を実行しない。

<context>

ユーザーは Phase 3 の gap が収束せず完了見込みも不明なため、追加修正を待たずに停止する判断をした。今後は Node 依存と各 skill の自動更新を検討する。安全な既定案は lock pin を維持し、検知・一時取得・license/hash/schema検証・diff/PR生成・CI・人間承認後に適用する方式。現 handoff 実装を完成させることは新路線の前提にしない。

</context>

<next_action>

Start with: Phase 3 を再開せず、新しい OpenSpec change と ADR で Node 依存 skill updater の更新契約を仕様化する。そのレビュー後に旧 change の supersede / archive / resume を明示決定する。

</next_action>
