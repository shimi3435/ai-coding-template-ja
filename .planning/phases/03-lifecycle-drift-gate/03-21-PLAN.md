---
phase: 03-lifecycle-drift-gate
plan: 21
type: tdd
wave: 15
depends_on:
  - 03-18
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
  - tests/test_handoff_manifest_refresh.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: manifest refresh approval evidence lists every created, updated, and active-to-tombstone source-state difference exactly once in deterministic UTF-8 byte order."
    - "The public refresh preview exposes exact before/after evidence for a removed source identity instead of an empty change tuple."
    - "Current published previews with no tombstone transition remain byte-compatible."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
      provides: "Complete deterministic source change projection"
    - path: tests/test_handoff_manifest_refresh.py
      provides: "Fixed tombstone approval-evidence regression"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
      to: ManifestRefreshPreview.changes
      via: "_changes projects validated candidate active and tombstone states"
      pattern: "_changes|tombstoned|source-removed"
---

<objective>
Close the refresh approval-evidence omission for HND-03 / HARD-R2.

Purpose: Ensure an approver sees source removals as exact first-class differences.
Output: Complete deterministic change evidence and a fixed preview-level regression.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-17-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
@tests/test_handoff_manifest_refresh.py

<interfaces>
Preserve `RefreshCandidateChange` fields, `ManifestRefreshPreview` fields/serialization, `preview_manifest_refresh`, exact preview approval, valid no-op/apply results, and existing issue/failure enums. Extend only the existing `_changes` projection; do not add another preview or serializer authority.
</interfaces>
</context>

## Artifacts this phase produces

- No new module, public top-level API, dataclass field, package export, dependency, schema, or evidence format.
- New fixed test nodes:
  - `tests/test_handoff_manifest_refresh.py::test_refresh_preview_lists_created_updated_and_tombstoned_changes`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Later lifecycle operations rely on complete fresh approval evidence. | 03-21 | COVERED | The approval view includes removals. |
| REQ | HND-03 / HARD-R2 | Approval-relevant source differences are complete and deterministic. | 03-21 | COVERED | Fixed preview example covers change projection. |
| RESEARCH | — | Preserve immutable preview, exact approval, and deterministic projection. | 03-21 | COVERED | No new dependency or property family. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Operator approved planning without it. |
| VERIFICATION | gap 3 / WR-02 | Active-to-tombstone changes are omitted from approval evidence. | 03-21 | COVERED | Candidate tombstones are projected exactly once. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose tombstoned source differences in refresh approval evidence</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py (`RefreshCandidateChange`, `_changes`, `_machine_view`, `preview_manifest_refresh`)
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py (`SourceIdentityState`, `SourceIdentity`, `SourceTombstone`, reconciliation result)
    - tests/test_handoff_manifest_refresh.py (`_preview`, source/assignment fixtures, pinned complete preview assertions)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py, tests/test_handoff_manifest_refresh.py</files>
  <behavior>
    - RED: one fixed public `preview_manifest_refresh` scenario that creates one ID, updates one ID, and moves one previous active ID to candidate tombstones exposes three exact `RefreshCandidateChange` rows.
    - GREEN: the tombstone row has kind `tombstoned`, the same source ID/category, last canonical source path, previous active fingerprint, tombstone fingerprint, and reason `source-removed`.
    - GREEN: each changed source ID appears once and rows are ordered by fixed kind rank then UTF-8 bytes of source ID; unchanged and current published no-tombstone previews retain existing bytes/results.
  </behavior>
  <action>RED: add the named fixed preview-level test using the existing isolated repository, source, explicit-match, and planning-inventory fixtures. Produce one valid created, one valid updated, and one valid active-to-tombstone transition through `preview_manifest_refresh`; assert exact full `RefreshCandidateChange` values, uniqueness by source ID, and deterministic order. Do not test `_changes` as the primary seam. Run the node and require the current missing tombstone row; commit `test(03-21): expose omitted tombstone refresh evidence`.

GREEN: extend `_changes` after validated reconciliation to index previous active IDs, emit existing created/updated rows from candidate active, and emit `tombstoned` only when a candidate tombstone ID existed in previous active. Populate category, `last_source_path`, previous active fingerprint, tombstone fingerprint, and `source-removed` exactly. Sort with explicit kind rank `created`, `updated`, `tombstoned` and UTF-8 encoded source ID; reject no valid state and emit no duplicate. Keep `RefreshCandidateChange` and machine-view fields unchanged. Run the focused node, current pinned preview/no-op evidence tests, and full refresh file; commit `feat(03-21): include tombstones in refresh approval evidence`.

Do not regenerate the historical/published preview fixtures because their candidate has no tombstones and serialized bytes must remain unchanged. Add no property family, schema/version change, dependency, or new approval path.</action>
  <acceptance_criteria>
    - The named test observes created, updated, and tombstoned rows through `preview_manifest_refresh`, not a private helper-only assertion.
    - The tombstone row contains exact before/after source evidence and `source-removed`; all changed IDs are unique and deterministically ordered.
    - Existing 54-item published preview, no-op preview, and protected subtree evidence remain byte-compatible.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_refresh.py::test_refresh_preview_lists_created_updated_and_tombstoned_changes tests/test_handoff_manifest_refresh.py::test_pinned_started_v2_builds_exact_complete_read_only_candidate -q</automated>
  </verify>
  <done>Refresh approval evidence exposes every created, updated, and active-to-tombstone source-state difference exactly once.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| reconciled source state → approval preview | Omitted removals can mislead approval. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-21-EVIDENCE | Tampering/Repudiation | high | `_changes`, preview machine view | mitigate | Project all three change kinds with exact before/after evidence, uniqueness, and deterministic byte ordering. |
| T-03-21-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_manifest_refresh.py::test_refresh_preview_lists_created_updated_and_tombstoned_changes tests/test_handoff_manifest_refresh.py::test_pinned_started_v2_builds_exact_complete_read_only_candidate -q`
- `uv run pytest tests/test_handoff_manifest_refresh.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py tests/test_handoff_manifest_refresh.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py tests/test_handoff_manifest_refresh.py`
- `git diff --check`
</verification>

<success_criteria>
- Verification gap 3 and review WR-02 are closed exactly once.
- Approval evidence includes exact deterministic removal rows while published no-tombstone previews remain compatible.
- No unapproved property family, dependency, schema, or recovery behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-21-SUMMARY.md` when done.
</output>
