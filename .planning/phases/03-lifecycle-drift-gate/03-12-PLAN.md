---
phase: 03-lifecycle-drift-gate
plan: 12
type: tdd
wave: 10
depends_on:
  - 03-10
  - 03-11
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
  - tests/test_handoff_lifecycle_drift.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: a source-pinned canonical baseline is complete only when its own changed_source_item_ids tuple is empty."
    - "A non-empty expected/source-pinned reconciliation delta returns source-reconciliation-incomplete before artifact comparison, progress projection, or lifecycle identity generation."
    - "The public lifecycle gate projects that inconsistent baseline as UNKNOWN, admitted=false, empty evidence, and decision_identity=None."
    - "Fixed classifier and FakeBoundary public-gate regressions cover expected changed IDs with an otherwise clean observed value; no new property family is added."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      provides: "Source-pinned baseline consistency check in the existing canonical classifier"
    - path: tests/test_handoff_lifecycle_drift.py
      provides: "Fixed direct-classifier regression for an inconsistent expected reconciliation baseline"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Fixed public-gate regression proving the inconsistent baseline is wholly unknown and identity-free"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "classify_canonical_source_drift returns the stable unknown decision consumed by gate_lifecycle_operation"
      pattern: "classify_canonical_source_drift|source-reconciliation-incomplete"
    - from: tests/test_handoff_lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "FakeBoundary injects the inconsistent SourceCommitObservation and assertions observe only gate_lifecycle_operation"
      pattern: "FakeBoundary|gate_lifecycle_operation"
---

<objective>
Close the source-pinned canonical baseline consistency gap for HND-03 / HARD-R2.

Purpose: Prevent a baseline that reports its own reconciliation drift from being treated as clean, admitted, or reusable by any lifecycle operation.
Output: One minimal classifier guard plus fixed direct-classifier and public lifecycle-gate regressions.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-09-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-10-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_lifecycle_drift.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve `classify_canonical_source_drift(expected, observed)` and `gate_lifecycle_operation(...)` as the public test seams. Reuse the existing `source-reconciliation-incomplete` issue code and `_unknown` projection. The expected/source-pinned `CanonicalSourceObservation.changed_source_item_ids` must be empty before artifact maps, source-state comparison, progress comparison, or lifecycle decision identity can be used. The observed/current tuple retains its existing meaning as deterministic drift evidence.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, public symbol, private helper, serializer, root export, CLI operation, dependency, or evidence schema.
- Existing `classify_canonical_source_drift` gains one source-pinned baseline consistency guard.
- New fixed test nodes:
  - `tests/test_handoff_lifecycle_drift.py::test_source_pinned_reconciliation_changes_are_unknown`
  - `tests/test_handoff_lifecycle_gate.py::test_source_pinned_reconciliation_changes_are_wholly_unknown`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on the same fresh fail-closed drift decision. | 03-12, 03-13 | COVERED | 03-12 closes admission consistency; 03-13 closes malformed reconciliation input safety. |
| REQ | HND-03 / HARD-R2 | Incomplete or internally inconsistent source inspection is unknown and stops. | 03-12, 03-13 | COVERED | Both remaining verifier gaps are assigned without adding another requirement. |
| RESEARCH | ordered trust boundary | Validate complete evidence before comparison, projection, or identity; use fixed examples. | 03-12, 03-13 | COVERED | No dependency or new admission authority is introduced. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | The explicit gap-closure invocation authorizes planning from verification and existing decisions. |
| VERIFICATION | remaining gap 1 / REVIEW CR-01 | Non-empty expected changed IDs must not become clean, admitted, or reusable. | 03-12 | COVERED | Direct classifier and FakeBoundary gate regressions fix the reproduced asymmetric case. |
| VERIFICATION | remaining gap 2 / REVIEW CR-02 | Malformed reconciliation aggregates must return structured non-success. | 03-13 | COVERED | Separate file ownership permits same-wave execution. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject an internally inconsistent source-pinned baseline</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_drift.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py, tests/test_handoff_lifecycle_drift.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: direct classification of a complete expected value with `changed_source_item_ids=("REQ-000001",)` and an otherwise identical observed value with an empty tuple returns clean in the current implementation, exposing the gap.
    - RED: the same source-pinned value injected through `FakeBoundary.source_result` makes the public lifecycle gate admit and assign an identity in the current implementation.
    - GREEN: the direct classifier returns `UNKNOWN` with issue `source-reconciliation-incomplete`, empty artifact/source evidence, and no progress candidate.
    - GREEN: the public gate returns `UNKNOWN`, `admitted=False`, exactly the stable issue code, empty remediation/progress evidence, and `decision_identity is None`.
    - GREEN: expected-empty/observed-non-empty reconciliation evidence retains the existing drifted behavior and exact observed changed IDs.
  </behavior>
  <action>Write both named fixed public-seam tests first. Build the direct case by copying an existing complete observation and changing only the expected/source-pinned `changed_source_item_ids`; build the gate case with the existing `_fixture`, `FakeBoundary`, `SourceCommitObservation`, and `gate_lifecycle_operation`, changing no other dimension. Run the focused command and confirm RED for the reproduced clean/admitted behavior, then commit `test(03-12): expose inconsistent source-pinned baseline admission`. In GREEN, add the smallest guard in `classify_canonical_source_drift` after both structured observations pass completeness validation but before artifact dictionaries, source-state comparison, progress comparison, or identity-relevant projection: any non-empty expected/source-pinned changed-ID tuple returns `_unknown("source-reconciliation-incomplete")`. Do not merge expected and observed changed IDs, reclassify the baseline as ordinary drift, or expose the expected tuple as remediation evidence. Keep observed/current non-empty changes as the existing drift signal. Commit `feat(03-12): reject inconsistent source-pinned baseline`. After GREEN, review only the touched classifier branch and public assertions; create `refactor(03-12): clarify source-pinned baseline guard` only if a behavior-preserving cleanup is necessary, otherwise record no refactor in the summary. Do not modify `lifecycle_gate.py`, issue vocabulary, identity encoding, evidence JSON, normalization, source reconciliation, mapping, retry, repair, rollback, or route-switch behavior.</action>
  <acceptance_criteria>
    - `test_source_pinned_reconciliation_changes_are_unknown` proves the exact asymmetric expected-non-empty/observed-empty case returns only `source-reconciliation-incomplete`.
    - `test_source_pinned_reconciliation_changes_are_wholly_unknown` proves `gate_lifecycle_operation` returns unknown, non-admitted, empty remediation/progress evidence, and no identity.
    - A fixed companion assertion preserves expected-empty/observed-non-empty as drifted with the exact observed ID.
    - Both tests use the public classifier/gate seams; no private validator or identity encoder is tested directly.
    - RED fails for the reproduced false-clean behavior, GREEN passes after the guard, and the summary records the optional REFACTOR decision.
    - Exactly one existing Hypothesis family remains, limited to checkbox normalization.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_drift.py::test_source_pinned_reconciliation_changes_are_unknown tests/test_handoff_lifecycle_gate.py::test_source_pinned_reconciliation_changes_are_wholly_unknown -q</automated>
  </verify>
  <done>An inconsistent source-pinned reconciliation baseline cannot become clean, admitted, or reusable through either public seam.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| source-commit observation → canonical classifier | A structurally valid source-pinned value can still contradict the manifest source identity through its own reconciliation delta. |
| canonical classifier → public lifecycle gate and identity | A false clean result is authorization evidence shared by all lifecycle operations and can mint a reusable identity. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-12-01 | Tampering/Elevation of Privilege | high | `classify_canonical_source_drift` expected baseline | mitigate | Require the complete source-pinned changed-ID tuple to be empty before comparison or projection; return the stable unknown issue with no partial evidence. |
| T-03-12-02 | Spoofing/Replay | high | `gate_lifecycle_operation` admission and identity | mitigate | Fixed FakeBoundary regression requires unknown, non-admitted, empty, identity-free output for the inconsistent baseline. |
| T-03-12-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_drift.py::test_source_pinned_reconciliation_changes_are_unknown tests/test_handoff_lifecycle_gate.py::test_source_pinned_reconciliation_changes_are_wholly_unknown -q`
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py`
- `git diff --check`
- Confirm the diff changes only the declared classifier and test files, adds no property family, and leaves lifecycle gate code, identity encoding, evidence artifacts, dependencies, and Phase 4-6 surfaces untouched.
</verification>

<success_criteria>
- Verification remaining gap 1 and REVIEW CR-01 are closed at the direct classifier and public lifecycle-gate seams.
- A non-empty expected/source-pinned reconciliation delta always produces stable unknown with no partial evidence or reusable identity.
- Valid observed/current reconciliation drift remains deterministic and unchanged.
- No unrelated refactor, new authority, dependency, property family, or later-phase behavior is added.
- All high-severity scoped threats are mitigated.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-12-SUMMARY.md` with RED/GREEN/REFACTOR evidence, the exact classifier/gate outcomes, focused/full verification results, and threat dispositions.
</output>
