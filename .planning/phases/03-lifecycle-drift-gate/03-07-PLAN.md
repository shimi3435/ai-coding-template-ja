---
phase: 03-lifecycle-drift-gate
plan: 07
type: tdd
wave: 6
depends_on:
  - 03-06
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
  - tests/test_handoff_lifecycle_drift.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: malformed nested Progress, SourceIdentityState, or changed source-ID evidence is classified as unknown before comparison, sorting, or decision-identity encoding."
    - "Both expected and observed malformed canonical observations return canonical-observation-incomplete with empty artifact paths, empty changed source IDs, no progress candidate, and no reusable identity."
    - "Progress completeness includes exact counter types, a tuple of valid NormalizedTask members, exact task field types, and counter/task invariants."
    - "SourceIdentityState has one reusable runtime-validation authority that checks the outer value, counters, collections, member types, fields, parents, paths, fingerprints, uniqueness, and counter invariants before dereference."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      provides: "Safe reusable complete SourceIdentityState validation"
      exports:
        - validate_source_identity_state
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      provides: "Complete nested canonical-observation validation before comparison or sorting"
    - path: tests/test_handoff_lifecycle_drift.py
      provides: "Fixed expected/observed public-classifier regressions for every canonical nested-state family"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Fixed public-gate regressions proving malformed canonical nested state cannot reach identity encoding"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "_is_complete_observation delegates complete SourceIdentityState validation to the shared safe validator"
      pattern: "validate_source_identity_state"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "classification rejects malformed nested canonical state before the gate projects or encodes it"
      pattern: "canonical-observation-incomplete"
    - from: tests/test_handoff_lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "FakeBoundary and the public gate exercise the malformed canonical observation without private-validator assertions"
      pattern: "gate_lifecycle_operation"
---

<objective>
Close the remaining canonical-observation validation gap for HND-03 / HARD-R2 through the existing public classifier and lifecycle gate.

Purpose: Ensure annotated frozen dataclasses containing malformed nested runtime values cannot be treated as complete, sorted, compared, or encoded into reusable admission evidence.
Output: One shared safe SourceIdentityState validation seam, complete Progress and changed-ID validation, and fixed public-seam regression evidence.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-04-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-06-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/models.py
@src/ai_coding_template_ja/openspec_gsd_handoff/progress.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_lifecycle_drift.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve `classify_canonical_source_drift(expected, observed)` and `gate_lifecycle_operation(...)` as the fixed test seams. Add only `validate_source_identity_state(value: object) -> Result[SourceIdentityState]` in `source_identity.py`; it must reuse the existing source-state invariant logic and must not create a second validation authority. `_is_complete_observation` remains the canonical completeness conjunction and converts every malformed complete-looking value to the existing `canonical-observation-incomplete` unknown decision. Do not add a root-package export, serializer, CLI operation, dependency, broad exception wrapper, or another property family.
</interfaces>
</context>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py`
  - public module-level `validate_source_identity_state`
  - safe member-before-dereference ordering shared with existing reconciliation validation
- `src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py`
  - complete Progress validation inside the existing completeness seam
  - exact-string validation for every `changed_source_item_ids` member before uniqueness or sorting
  - delegation to the shared SourceIdentityState validator
- `tests/test_handoff_lifecycle_drift.py`
  - fixed expected/observed malformed Progress, source-state, and changed-ID matrices
- `tests/test_handoff_lifecycle_gate.py`
  - fixed malformed canonical-state public-gate regressions with unknown/empty/no-identity assertions
- `.planning/phases/03-lifecycle-drift-gate/03-07-SUMMARY.md`
  - executor-produced RED/GREEN/REFACTOR evidence and focused verification record

<source_audit>

| SOURCE | ID | Feature / Requirement | Task / Must-have / Verify | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation receives the same fresh fail-closed decision. | Tasks 1-2; truths 1-2; lifecycle backstop | COVERED | Malformed canonical evidence stops at the shared classifier before identity use. |
| REQ | HND-03 / HARD-R2 | Incomplete inspection is unknown and stops progression. | Tasks 1-2; truths 1-4 | COVERED | No partial evidence or identity escapes. |
| RESEARCH | — | Validate untrusted top-level and nested values before attribute access or normalization; retain one authority. | Tasks 1-2 actions | COVERED | Existing Result/Failure vocabulary and public seams are reused. |
| CONTEXT | — | No Phase 3 CONTEXT.md; continuation from research, requirements, and verification was approved. | — | EXCLUDED | No locked decisions or deferred ideas exist for this phase. |
| VERIFICATION | GAP-1.1 | Validate Progress counters, task tuple/member types, task fields, and counter invariants before comparison. | Task 1 behavior/action/acceptance | COVERED | Fixed examples cover both sides and boolean-as-integer rejection. |
| VERIFICATION | GAP-1.2 | Share a safe complete SourceIdentityState validator that checks member types before dereference. | Task 2 behavior/action; artifact `validate_source_identity_state` | COVERED | Existing invariant logic is made safely reusable instead of copied. |
| VERIFICATION | GAP-1.3 | Validate every changed source-ID member as an exact string before set/sort. | Task 1 behavior/action/acceptance | COVERED | Mixed types converge on canonical-observation-incomplete. |
| VERIFICATION | GAP-1.4 | Add expected/observed public classifier and boundary-injected public-gate regressions with empty evidence and no identity. | Task 1 named classifier tests; Task 2 named classifier/gate tests; truths 1-2 | COVERED | The classifier matrix covers malformed Progress and changed IDs on both expected/observed sides; the distinct `FakeBoundary` gate matrix covers Progress, changed-ID, and SourceIdentityState families through `gate_lifecycle_operation`. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject malformed Progress and changed source IDs at the public classifier</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/models.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/progress.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - tests/test_handoff_lifecycle_drift.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py, tests/test_handoff_lifecycle_drift.py</files>
  <behavior>
    - RED: `test_malformed_progress_observation_is_unknown_before_comparison` uses fixed expected-side and observed-side cases to reject `Progress` counters that are non-integers, booleans, negative, or inconsistent with the task tuple and done counts.
    - RED: the same named Progress test rejects a non-tuple `tasks`, a non-`NormalizedTask` member, and every malformed `id`, `description`, or `done` field before comparison.
    - RED: `test_malformed_changed_source_ids_observation_is_unknown_before_sorting` covers both classifier sides; a non-tuple changed-ID collection and a tuple containing any non-exact-string member, including the reproduced integer/string mixture, return `canonical-observation-incomplete` without sorting or raising.
    - GREEN: every rejected case returns `unknown`, empty drifted paths, empty changed IDs, and no progress candidate; valid clean, drifted, and checkbox-only examples remain unchanged.
    - REFACTOR: the existing `_is_complete_observation` remains the sole canonical completeness conjunction and no exception-based sanitizer is added.
  </behavior>
  <action>First add the two concrete public-classifier tests `test_malformed_progress_observation_is_unknown_before_comparison` and `test_malformed_changed_source_ids_observation_is_unknown_before_sorting`. Each uses a small fixed parameter table, the existing `_assert_unknown` helper, and both `expected` and `observed` positions. Run only these two node IDs to establish RED, then commit the failing tests as `test(03-07): expose malformed progress and changed-id gaps`. Extend the existing completeness seam so all three counters are exact integers rather than booleans, are non-negative, equal the tuple length/done/not-done counts, and sum consistently; require an exact tuple of `NormalizedTask` members and exact `str`, `str`, and `bool` task fields. Validate every changed-ID member as an exact string before the existing unique/sorted comparison. Commit the passing implementation as `feat(03-07): validate nested progress and changed ids`. Run the separately listed clean/checkbox regression backstop only after the new malformed tests are GREEN. Perform a behavior-preserving review after GREEN; if consolidation changes production code, commit it separately as `refactor(03-07): consolidate canonical progress validation`, otherwise record that no refactor commit was warranted. Keep the existing sole checkbox-normalization property family unchanged and do not add private-validator tests.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_drift.py::test_malformed_progress_observation_is_unknown_before_comparison tests/test_handoff_lifecycle_drift.py::test_malformed_changed_source_ids_observation_is_unknown_before_sorting -q</automated>
    <regression-backstop>uv run pytest tests/test_handoff_lifecycle_drift.py::test_fixed_complete_equal_observations_are_clean tests/test_handoff_lifecycle_drift.py::test_checkbox_only_progress_is_clean_and_separate -q</regression-backstop>
  </verify>
  <acceptance_criteria>
    - Every fixed malformed Progress counter, task container/member/field, and changed-ID member case returns the exact public unknown shape without an exception.
    - Tests exercise both expected and observed positions and would fail if validation occurred after comparison or sorting.
    - The two new malformed-test node IDs alone are red before implementation and green afterward; pre-existing clean/checkbox tests are a separate regression backstop and cannot mask a missing malformed test.
    - Valid complete observations preserve their prior classification and progress behavior.
    - `tests/test_handoff_lifecycle_drift.py` still contains exactly the existing checkbox-normalization property family and no new `@given` family.
  </acceptance_criteria>
  <done>Progress and changed source-ID nested values are fully runtime-validated before comparison or sorting at the public classifier seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse complete SourceIdentityState validation through the public lifecycle gate</name>
  <read_first>
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_drift.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py, tests/test_handoff_lifecycle_drift.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: `test_malformed_source_state_observation_is_unknown_before_dereference` uses fixed expected-side and observed-side classifier cases to reject wrong counter types, non-tuple active/tombstone collections, `None` or wrong-class members, and malformed active/tombstone fields.
    - RED: parent, ID, path, fingerprint, uniqueness, aggregate-bound, and next-counter invariants continue to use the existing source-state rules rather than a copied subset.
    - RED: `test_malformed_canonical_nested_state_public_gate_is_wholly_unknown` injects malformed canonical observations through `FakeBoundary.source_result=Success(SourceCommitObservation(...))` and calls `gate_lifecycle_operation`; its fixed matrix includes every malformed Progress counter/tasks/task-member/task-field family, changed-ID container/member family, and SourceIdentityState counter/container/member/field/invariant family.
    - RED: every public-gate matrix row returns issue code `canonical-observation-incomplete`, state `UNKNOWN`, `admitted=False`, `drifted_artifact_paths=()`, `changed_source_item_ids=()`, `progress_update_candidate=None`, `revalidation_targets=()`, `replanning_targets=()`, `next_action_codes=()`, and `decision_identity=None`.
    - GREEN: valid source states continue to reconcile, classify, and pass the public gate with the same complete decision behavior.
    - REFACTOR: internal reconciliation and canonical classification call the same safe source-state authority.
  </behavior>
  <action>Add `test_malformed_source_state_observation_is_unknown_before_dereference` for the expected/observed classifier sides and `test_malformed_canonical_nested_state_public_gate_is_wholly_unknown` for the boundary-injected gate path before production changes; commit them as `test(03-07): expose malformed source-state admission gap`. Keep these seams distinct: classifier rows pass malformed `Success(CanonicalSourceObservation)` as expected or observed, while gate rows place that malformed observation inside a successful `SourceCommitObservation` returned by `FakeBoundary` and assert only the public `gate_lifecycle_operation` decision. The gate matrix must include all malformed Progress and changed-ID families established by Task 1 as well as every SourceIdentityState family, and must assert the full unknown/non-admitted/empty/no-identity shape for each row. In `source_identity.py`, add `validate_source_identity_state(value: object) -> Result[SourceIdentityState]` as the safe reusable entry point over the existing source-state invariant logic. Make the underlying validation order require the outer state before fields, exact counter types before numeric comparisons, exact tuple containers before iteration, and the correct active/tombstone member class before any member attribute; then retain the existing ID/category/path/heading/fingerprint/parent/uniqueness/bounds/counter checks. Route existing reconciliation validation and `lifecycle_drift._is_complete_observation` through this same authority; the classifier must project any validation failure as `canonical-observation-incomplete`, not leak a partial state or a granular reusable decision. Commit GREEN as `feat(03-07): share safe source-state validation`. Review for duplicate validation authority after GREEN and commit a separate refactor only when production code changes. Do not alter lifecycle identity encoding, public decision fields, evidence JSON, package-root exports, or CLI behavior.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_drift.py::test_malformed_source_state_observation_is_unknown_before_dereference tests/test_handoff_lifecycle_gate.py::test_malformed_canonical_nested_state_public_gate_is_wholly_unknown -q</automated>
  </verify>
  <acceptance_criteria>
    - The new validator accepts `object`, never dereferences an unvalidated member, and returns `Failure` rather than raising for every fixed malformed family.
    - The public classifier produces `canonical-observation-incomplete` for both expected and observed malformed source state with empty evidence.
    - The public lifecycle gate matrix covers every malformed Progress, changed-ID, and SourceIdentityState family through a boundary-returned successful source observation, not by directly calling a private validator or only reusing classifier assertions.
    - Every gate row has `canonical-observation-incomplete`, `UNKNOWN`, `admitted=False`, empty artifact paths/source IDs/remediation/action tuples, no progress candidate, and no reusable identity.
    - Reconciliation and classification share the validator; no parallel partial SourceIdentityState checker is introduced.
  </acceptance_criteria>
  <done>Malformed nested SourceIdentityState evidence is rejected by one authoritative validator and cannot reach lifecycle identity encoding.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Risk threshold: any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller-constructed structured values → canonical classifier | Runtime values can violate annotations inside otherwise valid frozen dataclasses. |
| SourceIdentityState → reconciliation/classification | Malformed members can spoof stable source identity or terminate validation before a fail-closed result. |
| canonical decision → lifecycle identity encoder | Partial progress or source evidence must not be encoded as reusable admission identity. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-07-01 | Tampering | high | nested canonical structured input | mitigate | Exact container/member/field validation and complete invariants run before attribute access, comparison, set construction, or sorting. |
| T-03-07-02 | Elevation of Privilege | high | fail-open canonical classification | mitigate | Any malformed nested value converges on `canonical-observation-incomplete` with empty evidence and cannot be admitted. |
| T-03-07-03 | Denial of Service | high | classifier and identity termination | mitigate | Fixed public regressions cover the reproduced `AttributeError` and `TypeError` families; validators return structured non-success without broad exception recovery. |
| T-03-07-04 | Spoofing/Replay | high | stale lifecycle identity reuse | mitigate | Unknown decisions expose no identity, so malformed or partial source/progress state cannot mint or match a reusable prior decision. |
| T-03-07-SC | Tampering | low | package supply chain | accept | No package install or dependency update occurs. |

</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_drift.py::test_malformed_progress_observation_is_unknown_before_comparison tests/test_handoff_lifecycle_drift.py::test_malformed_changed_source_ids_observation_is_unknown_before_sorting tests/test_handoff_lifecycle_drift.py::test_malformed_source_state_observation_is_unknown_before_dereference -q`
- `uv run pytest tests/test_handoff_lifecycle_gate.py::test_malformed_canonical_nested_state_public_gate_is_wholly_unknown -q`
- `uv run pytest tests/test_handoff_lifecycle_drift.py::test_fixed_complete_equal_observations_are_clean tests/test_handoff_lifecycle_drift.py::test_checkbox_only_progress_is_clean_and_separate -q`
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py`
- `git diff --check`
- Confirm the existing checkbox-normalization property family count is unchanged.
</verification>

<success_criteria>
- All four GAP-1 missing bullets in `03-VERIFICATION.md` have fixed public-seam evidence.
- Expected-side and observed-side classifier matrices reject every malformed Progress, SourceIdentityState, and changed source-ID family before comparison or sorting.
- The distinct boundary-injected public-gate matrix rejects the same Progress, changed-ID, and SourceIdentityState families as `canonical-observation-incomplete`, `UNKNOWN`, non-admitted, empty evidence/actions/remediation, no progress candidate, and no identity.
- Valid clean, drifted, checkbox-only, stale-decision, and repository-binding lifecycle behavior remains green.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-07-SUMMARY.md` when done.
</output>
