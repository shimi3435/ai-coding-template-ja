---
phase: 03-lifecycle-drift-gate
plan: 11
type: tdd
wave: 8
depends_on:
  - 03-08
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
  - tests/test_handoff_execution_mapping.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: build_manifest_mappings and validate_mapping_readiness call validate_source_identity_state before iterating active/tombstone members."
    - "validate_mapping_readiness validates the complete ManifestMapping outer tuple, members, fields, nested tuples, order, uniqueness, bounds, UTF-8 scalars, and canonical paths before set/equality operations."
    - "build_manifest_mappings and validate_mapping_readiness obtain expected mappings from one pure _project_canonical_manifest_mappings helper, with no second projection authority."
    - "Malformed SourceIdentityState and ManifestMapping values return structured mapping failures without AttributeError, TypeError, partial mappings, or readiness."
    - "Fixed equivalence and source assertions prove the canonical helper is the only ManifestMapping construction projection while valid 49-item construction and plan/execute/verify/finalize readiness behavior remain unchanged."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      provides: "Shared source-state reuse, complete ManifestMapping runtime validation, and one canonical pure mapping projection"
    - path: tests/test_handoff_execution_mapping.py
      provides: "Fixed builder/readiness malformed invariant regressions plus canonical projection equivalence/source assertions"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "both mapping public APIs call validate_source_identity_state before member iteration"
      pattern: "validate_source_identity_state"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
      via: "_validate_manifest_mappings validates ManifestMapping values before expected-set equality"
      pattern: "_validate_manifest_mappings|ManifestMapping"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      via: "both public mapping APIs call _project_canonical_manifest_mappings after their existing validation prerequisites"
      pattern: "_project_canonical_manifest_mappings"
---

<objective>
Close mapping public-API runtime validation and duplicated projection-authority gaps for HND-03 / HARD-R2.

Purpose: Ensure malformed source identity and manifest mapping values fail closed before iteration, hashing, set construction, equality, path use, or readiness, while builder and readiness cannot diverge on canonical mapping projection.
Output: Shared validator reuse, one complete private ManifestMapping validator, one canonical pure projection helper, and fixed public builder/readiness regressions.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-07-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-08-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
@tests/test_handoff_execution_mapping.py

<interfaces>
Import and call the existing `validate_source_identity_state(value) -> Result[SourceIdentityState]`; do not duplicate its invariants. Add one private `_validate_manifest_mappings(value: object) -> Result[tuple[ManifestMapping, ...]]` in `execution_mapping.py`. It must return the original tuple on success and `mapping-set-invalid` on malformed runtime shape. Replace the duplicated builder/readiness projection with one private pure `_project_canonical_manifest_mappings(inventory: PlanningInventory) -> tuple[ManifestMapping, ...]`; both public APIs call it only after their existing validation prerequisites. Preserve public `build_manifest_mappings` and `validate_mapping_readiness` signatures, MappingReadiness/failure vocabulary, exact expected mapping equality, and existing canonical path conventions.
</interfaces>
</context>

## Artifacts this phase produces

- New private symbols: `execution_mapping._validate_manifest_mappings` and `execution_mapping._project_canonical_manifest_mappings`.
- No new module, public export, serializer, CLI operation, dependency, or evidence schema.
- New fixed nodes:
  - `tests/test_handoff_execution_mapping.py::test_builder_and_readiness_reject_malformed_source_identity_state`
  - `tests/test_handoff_execution_mapping.py::test_readiness_rejects_manifest_mapping_outer_container_and_member_families`
  - `tests/test_handoff_execution_mapping.py::test_readiness_rejects_manifest_mapping_field_tuple_order_uniqueness_and_path_families`
  - `tests/test_handoff_execution_mapping.py::test_builder_and_readiness_share_canonical_mapping_projection`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Later lifecycle operations rely on fail-closed readiness. | 03-11 | COVERED | Mapping APIs cannot throw on malformed nested values. |
| REQ | HND-03 / HARD-R2 | Stable mapping evidence must be complete before lifecycle admission. | 03-11 | COVERED | Public builder/readiness return structured failure only. |
| RESEARCH | single validator authority | Validate before dereference and avoid parallel partial authorities. | 03-11 | COVERED | Existing source validator is reused; one mapping validator is added. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | No synthetic context is created. |
| VERIFICATION | root gap 3 | SourceIdentityState and complete ManifestMapping validation in public APIs. | 03-11 | COVERED | All missing outer/container/member/field/nested invariants have fixed tests. |
| REVIEW | CR-05 | active=(None,) and source_id=[] raise. | 03-11 | COVERED | Exact counterexamples become RED cases. |
| REVIEW | WR-01 | Builder and readiness duplicate canonical mapping projection. | 03-11 | COVERED | One named pure helper becomes the sole projection authority, with fixed equivalence/source assertions. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse complete SourceIdentityState validation in both mapping APIs</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_execution_mapping.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py, tests/test_handoff_execution_mapping.py</files>
  <behavior>
    - RED: `SourceIdentityState(active=(None,))` returns `mapping-input-invalid` from builder and readiness rather than AttributeError.
    - RED: fixed outer, counter, collection, active/tombstone member, field, ordering, uniqueness, path, parent, fingerprint, count, and byte-bound families return structured failures at both public APIs.
    - GREEN: valid source state is the exact success value from `validate_source_identity_state` and only then is iterated.
  </behavior>
  <action>Commit RED as `test(03-11): expose unsafe mapping source state use`. Add a finite local malformed-source-state table matching the authoritative validator families; keep fixed examples and do not add Hypothesis. In GREEN, import `validate_source_identity_state` and call it at the beginning of `build_manifest_mappings` and `validate_mapping_readiness`, before active/tombstone comprehensions or any attribute access. Convert any validator `Failure` to the established public `mapping-input-invalid` result without leaking partial IDs or validator internals; use the returned original state on success. Commit GREEN as `feat(03-11): reuse complete source state validation in mapping APIs`. If production code changes during REFACTOR, make exactly one behavior-preserving commit named `refactor(03-11): remove obsolete source state prechecks`; if no refactor is warranted, create no refactor commit and record that decision explicitly in `03-11-SUMMARY.md`. Do not modify `source_identity.py`, create a second source validator, or broaden public exports.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_execution_mapping.py::test_builder_and_readiness_reject_malformed_source_identity_state -q</automated>
  </verify>
  <acceptance_criteria>
    - Both public APIs reject every fixed source-state family before member iteration.
    - The reproduced `active=(None,)` row returns `mapping-input-invalid` without exception.
    - No Failure exposes mappings or MappingReadiness values.
    - Exact RED/GREEN commits and the REFACTOR decision are recorded.
    - Any production-code REFACTOR commit uses exactly `refactor(03-11): remove obsolete source state prechecks`; otherwise `03-11-SUMMARY.md` records the no-refactor decision and no refactor commit exists.
  </acceptance_criteria>
  <done>Mapping construction and readiness consume only a completely validated SourceIdentityState.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate complete ManifestMapping values and centralize canonical projection</name>
  <read_first>
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
    - tests/test_handoff_execution_mapping.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py, tests/test_handoff_execution_mapping.py</files>
  <behavior>
    - RED: non-tuple/4097/member-wrong-class mappings and wrong scalar field types return `mapping-set-invalid`.
    - RED: each nested path/reference collection rejects non-tuple, 4097 items, non-string member, invalid UTF-8 scalar, unsorted order, duplicate, alias/noncanonical path, or invalid policy reference before set/equality.
    - RED: outer mappings reject unsorted source IDs, duplicate source IDs, malformed IDs, invalid phase/path consistency, aggregate-byte limit+1, and the reproduced list-valued source_id without TypeError.
    - RED: a fixed valid inventory proves builder output equals a literal canonical tuple and is accepted by readiness; a source assertion proves both public APIs call `_project_canonical_manifest_mappings` and no second `ManifestMapping` projection remains.
    - GREEN: `_project_canonical_manifest_mappings` is pure and is the sole construction authority used by builder and readiness after source coverage, policy, inventory, and mapping validation; all readiness horizons retain current results.
  </behavior>
  <action>Commit RED as `test(03-11): expose unsafe manifest mapping validation`. Split malformed fixed cases between the first two exact node IDs named above and exercise only `validate_mapping_readiness`. Add `test_builder_and_readiness_share_canonical_mapping_projection` with an independently written literal expected tuple for a fixed valid inventory, assert `build_manifest_mappings` returns that tuple, and assert `validate_mapping_readiness` accepts the same tuple at the applicable horizon. In the same node, use a narrow AST/source assertion to require calls to `_project_canonical_manifest_mappings` from both public API bodies and to require the `ManifestMapping` construction projection to exist only in that helper; this is the explicit WR-01 structural regression, not a new public seam. In GREEN, add `_validate_manifest_mappings(value: object)`. Validate in this order: exact outer tuple and `_MAX_ENTRIES`; exact `ManifestMapping` members; exact string scalar fields; exact bounded nested tuples and string members; strict UTF-8 scalars; source/phase ID syntax; canonical phase/plan/evidence path shapes; policy-reference syntax; per-tuple sorted unique order; outer source-ID sorted unique order; and `_MAX_BYTES` aggregate UTF-8 size. Return `mapping-set-invalid` on any failure. Call this validator before repository resolution, mapping ID sets, tombstone intersections, or expected-mapping equality, and use its original tuple success value. Add pure `_project_canonical_manifest_mappings(inventory)` by moving the existing shared phase lookup, assignment ordering, plan/evidence selection, and tuple construction into it without changing projection semantics. After source coverage and policy validation, builder returns this helper's tuple; after inventory and caller-mapping validation, readiness compares only against this helper's tuple. Delete `_expected_mappings` and all duplicated projection code. Commit GREEN as `feat(03-11): validate complete manifest mappings before readiness`. If production code changes during REFACTOR, make exactly one behavior-preserving commit named `refactor(03-11): consolidate manifest mapping validation`; if no refactor is warranted, create no refactor commit and record that decision explicitly in `03-11-SUMMARY.md`. Preserve all public signatures, issue codes, MappingReadiness fields, readiness horizons, and canonical path conventions; do not repair, sort, coerce, or infer malformed caller values.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_execution_mapping.py::test_readiness_rejects_manifest_mapping_outer_container_and_member_families tests/test_handoff_execution_mapping.py::test_readiness_rejects_manifest_mapping_field_tuple_order_uniqueness_and_path_families tests/test_handoff_execution_mapping.py::test_builder_and_readiness_share_canonical_mapping_projection -q</automated>
  </verify>
  <acceptance_criteria>
    - Every outer/container/member/field/nested/order/uniqueness/path row returns `mapping-set-invalid`.
    - The reproduced list-valued `source_id` row never reaches hashing or set construction.
    - Malformed mappings are not normalized or repaired and expose no partial readiness.
    - Valid 49-item mapping baseline and all readiness horizons remain green.
    - Builder and readiness both use `_project_canonical_manifest_mappings`; fixed literal equivalence and narrow source assertions prove no duplicated projection authority remains.
    - Public API signatures, existing issue codes, and MappingReadiness/failure vocabulary are unchanged.
    - Any production-code REFACTOR commit uses exactly `refactor(03-11): consolidate manifest mapping validation`; otherwise `03-11-SUMMARY.md` records the no-refactor decision and no refactor commit exists.
  </acceptance_criteria>
  <done>Only complete canonical ManifestMapping tuples reach readiness comparison and filesystem observation, and builder/readiness derive their canonical expected tuple from one pure projection helper.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller SourceIdentityState → mapping APIs | Malformed frozen values can bypass annotations and crash member iteration. |
| manifest mappings → readiness sets/equality/filesystem | Malformed nested values can crash, alias paths, or contribute partial readiness. |
| validated PlanningInventory → builder/readiness projection | Duplicated projection logic can make construction and admission disagree about canonical ownership. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-11-01 | Tampering/Denial of Service | high | SourceIdentityState consumers | mitigate | Reuse the shared Result validator before all iteration; fixed malformed families return structured failures. |
| T-03-11-02 | Tampering/Elevation of Privilege | high | ManifestMapping validation | mitigate | Validate exact outer/container/member/field/order/uniqueness/path/byte invariants before sets, equality, or filesystem use. |
| T-03-11-03 | Path Traversal | high | plan/evidence/phase paths | mitigate | Enforce existing canonical repository-relative path and phase ownership shapes; never normalize or repair caller paths. |
| T-03-11-04 | Tampering/Repudiation | high | builder/readiness mapping projection | mitigate | Route both consumers through one pure `_project_canonical_manifest_mappings` authority and lock equivalence plus source shape with fixed assertions. |
| T-03-11-SC | Tampering | low | package supply chain | accept | No dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_execution_mapping.py -q`
- `uv run pytest tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py tests/test_handoff_execution_mapping.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py tests/test_handoff_execution_mapping.py`
- `task check`
- `git diff --check`
- Confirm no new property family, dependency, root export, CLI, serializer, evidence-schema, optional-smoke, retry/repair/rollback/route-switch behavior exists.
</verification>

<success_criteria>
- Verification root gap 3 and REVIEW CR-05 are closed at builder/readiness public seams.
- Complete SourceIdentityState validation is shared, not duplicated.
- Complete ManifestMapping validation precedes all semantic and filesystem use.
- Builder and readiness share one pure canonical mapping projection, with fixed equivalence/source assertions preventing WR-01 regression.
- Fixed malformed families return structured failures without partial values or exceptions.
- Existing lifecycle/mapping behavior remains green and no high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-11-SUMMARY.md` with RED/GREEN/REFACTOR commits, fixed family results, canonical projection equivalence/source evidence, full checks, and threat dispositions.
</output>
