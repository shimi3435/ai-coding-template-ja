---
phase: 03-lifecycle-drift-gate
plan: 14
type: tdd
wave: 11
depends_on:
  - 03-12
  - 03-13
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: a stale lifecycle rejection returns a decision identity computed from the complete stale decision, never the clean decision identity that was rejected."
    - "Replaying the identity returned by a stale rejection against unchanged current evidence remains DRIFTED, non-admitted, and issue-coded lifecycle-decision-stale."
    - "The repeated stale decision identity is stable, lowercase 64-hex, and distinct from the prior clean identity."
    - "A current clean identity still admits only after a fresh observation, while malformed or incomplete prior identity paths retain their existing UNKNOWN and identity-free behavior."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      provides: "Stale decision construction whose identity binds the returned state, admission flag, and issue codes"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Fixed two-step public gate replay regression for a stale rejection identity"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "gate_lifecycle_operation constructs the complete stale decision before passing it to the existing _decision_identity encoder"
      pattern: "lifecycle-decision-stale|_decision_identity"
    - from: tests/test_handoff_lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      via: "two consecutive gate_lifecycle_operation calls replay only the public decision_identity"
      pattern: "gate_lifecycle_operation"
---

<objective>
Close the stale-rejection identity replay gap at the sole public lifecycle admission seam for HND-03 / HARD-R2.

Purpose: Ensure every reusable identity authenticates the exact decision returned to the caller, so a rejected stale decision can never publish the clean digest that admits its next replay.
Output: Complete stale-decision identity recomputation plus one fixed two-step public-seam regression.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-06-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve the public `gate_lifecycle_operation(..., prior_decision_identity: str | None = None) -> LifecycleGateDecision` signature, `LifecycleGateDecision` fields, `lifecycle-gate-decision-v1` typed length-prefixed SHA-256 encoder, lowercase 64-hex identity format, and `hmac.compare_digest` prior comparison. The stable stale issue code remains `lifecycle-decision-stale`; no approval token, cache, retry, or second admission seam is introduced.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, test file, public symbol, private helper, dataclass field, root export, CLI operation, dependency, or evidence schema.
- Modified existing production symbols:
  - `gate_lifecycle_operation`
  - existing `_decision_identity` call site for the stale return branch
- New fixed test node:
  - `tests/test_handoff_lifecycle_gate.py::test_stale_rejection_identity_cannot_be_replayed_into_admission`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on one fresh fail-closed drift decision. | 03-14, 03-15, 03-16 | COVERED | The three current verifier gaps are assigned one public-seam TDD plan each. |
| REQ | HND-03 / HARD-R2 | Approval-relevant evidence is bound to observed inputs and stale reuse cannot silently admit. | 03-14 | COVERED | The returned stale fields and reusable identity are made consistent and replay-safe. |
| RESEARCH | — | Preserve the sole public gate, typed encoder, exact issue codes, and fixed-example TDD evidence. | 03-14 | COVERED | Existing `_decision_identity` remains the only encoder authority. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Gap closure is grounded in ROADMAP, canonical OpenSpec, verification, review, and accumulated Phase 3 decisions. |
| VERIFICATION | gap 1 / REVIEW CR-01 | Stale rejection exposes a clean identity that admits its next replay. | 03-14 | COVERED | Two-step replay through `gate_lifecycle_operation` must remain rejected. |
| VERIFICATION | gap 2 / REVIEW CR-02 | Repository-root replacement can return detached source content. | 03-15 | COVERED | Assigned to the disjoint source-reader plan in the same wave. |
| VERIFICATION | gap 3 / REVIEW CR-03 | Public source readers raise for malformed containers/members. | 03-16 | COVERED | Assigned after 03-15 because both source-reader gaps modify the same files. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind stale rejection identity to the returned decision</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: obtain a clean public decision, change one identity-bound manifest input, submit the original clean identity, then submit the identity returned by that stale rejection without changing current evidence.
    - RED: the current implementation's second submission becomes CLEAN and admitted because the first stale return retained the freshly computed clean identity.
    - GREEN: both stale submissions are DRIFTED, `admitted is False`, and have exactly `("lifecycle-decision-stale",)` for the controlled manifest-byte change.
    - GREEN: both stale submissions expose the same lowercase 64-hex identity, and that identity differs from the original clean identity because it binds stale state, non-admission, and the stale issue code.
    - GREEN: reusing the actual current clean identity still returns CLEAN and admitted after a fresh observation; malformed and incomplete prior identity tests remain unchanged.
  </behavior>
  <action>RED: add only `test_stale_rejection_identity_cannot_be_replayed_into_admission`, reusing `_fixture`, the existing manifest-byte mutation, and `gate_lifecycle_operation`. Drive three public calls: initial clean, first stale using the initial identity after the bound input changes, and second stale using the first stale decision's returned identity with no further change. Assert the exact states, admission flags, issue tuple, identity shape, stable repeated-stale identity, and separation from the initial clean identity. Do not call `_decision_identity`, inspect encoder bytes, or mock an internal collaborator. Run the focused node and confirm RED fails specifically because the second replay is CLEAN/admitted, then commit `test(03-14): expose stale rejection identity replay`. GREEN: in the prior-identity mismatch branch of `gate_lifecycle_operation`, first construct the complete stale `LifecycleGateDecision` with `state=DRIFTED`, `admitted=False`, and the UTF-8-sorted `lifecycle-decision-stale` issue. Only after those fields are final, recompute `decision_identity` through the existing `_decision_identity(observation.value, stale_decision)` authority and return that value. Preserve the original complete observation, all remediation/progress fields, `manifest_sha256`, encoder version, exact validation, and constant-time comparison; do not clear the identity, retain the clean digest, create a second token, or alter clean/unknown behavior. Commit `feat(03-14): bind stale identity to stale decision`. After GREEN, review only the stale branch for duplication; create `refactor(03-14): clarify stale decision construction` only if behavior remains identical, otherwise record that no REFACTOR commit was needed. Keep changes minimal and do not modify evidence JSON, source readers, dependencies, canonical OpenSpec artifacts, retry, repair, rollback, route-switch, or final-completion state.</action>
  <acceptance_criteria>
    - The named fixed public-seam test fails before production edits because the returned stale identity admits the second replay.
    - After GREEN, both stale calls return `LifecycleGateState.DRIFTED`, `admitted=False`, and exactly `("lifecycle-decision-stale",)`.
    - The stale identity is present, lowercase 64-hex, stable across the two unchanged stale observations, and different from the initial clean identity.
    - The test calls only `gate_lifecycle_operation`; it does not test or reproduce the private encoder.
    - Existing current-reuse, cross-root replay, malformed identity, incomplete observation, five-operation matrix, and portable-evidence tests remain green.
    - RED and GREEN commits exist in order; the summary records the optional REFACTOR decision and states that phase completion remains with independent verification.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py::test_stale_rejection_identity_cannot_be_replayed_into_admission -q</automated>
  </verify>
  <done>A caller cannot turn a stale rejection's returned identity into admission on the next unchanged observation.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Security Assets

| Asset | Security property |
|---|---|
| Reusable lifecycle `decision_identity` | Authenticates the exact complete decision returned by the sole admission gate. |
| Canonical source inventory | Remains bound to the caller-visible validated repository root for the full observation. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| prior identity → `gate_lifecycle_operation` | Caller-controlled replay evidence crosses into the lifecycle authorization decision. |
| current observation → returned decision identity | The digest must bind final state, admission, issues, remediation, progress, repository, and manifest evidence. |
| repository path → source descriptor | A path can be renamed or substituted while source bytes are read; Plan 03-15 owns this disjoint mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-14-STALE | Spoofing/Elevation of Privilege | high | `gate_lifecycle_operation` stale branch | mitigate | Construct the stale decision before identity generation and prove two consecutive public replays remain stale and non-admitted. |
| T-03-14-ROOT | Tampering/Elevation of Privilege | high | `read_source_inventory` repository root | transfer | Plan 03-15 binds pre-open, descriptor, and post-read root identity; its fixed root-replacement node is a blocking phase verification dependency. |
| T-03-14-SC | Tampering | low | package supply chain | accept | No package install, dependency change, or schema operation occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_gate.py::test_stale_rejection_identity_cannot_be_replayed_into_admission -q`
- `uv run pytest tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- After Wave 12: run the three new gap families together, then `task check` and `git diff --check`; Phase 03 remains incomplete until `$gsd-verify-work` independently closes all three gaps.
- Confirm the diff changes only the declared source/test files, preserves one public gate and one typed encoder, and adds no property family, dependency, evidence-schema change, retry, repair, rollback, route switch, or later-phase behavior.
</verification>

<success_criteria>
- Verification gap 1 and REVIEW CR-01 are closed at the public lifecycle gate seam.
- A stale rejection identity represents the complete stale decision and cannot admit its own replay.
- Current clean identity reuse, repository separation, malformed identity, and incomplete observation semantics remain unchanged.
- No unrelated refactor, new identity authority, dependency, property family, or final-completion claim is added.
- The high stale-replay threat is mitigated here; the high root-substitution threat remains explicitly transferred to and blocked on Plan 03-15.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-14-SUMMARY.md` with RED/GREEN/REFACTOR evidence, exact three-call public outcomes, identity relations, focused/full verification results, threat dispositions, and an explicit statement that independent verification still owns Phase 03 completion.
</output>
