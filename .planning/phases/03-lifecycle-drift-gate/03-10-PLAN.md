---
phase: 03-lifecycle-drift-gate
plan: 10
type: tdd
wave: 9
depends_on:
  - 03-09
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: handoff.json is read only through a no-follow descriptor chain anchored at the validated repository descriptor."
    - "An intermediate manifest symlink, directory replacement, or parent identity change is unknown and never admitted."
    - "Expected and observed phase ID/path maps are each exactly equal to the validated PlanningInventory phase map in both directions."
    - "Inventory-only, graph-only, and same-extra-phase-in-both-graphs mismatches return lifecycle-phase-observation-incomplete with no decision identity."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      provides: "Repository-anchored manifest traversal and exact graph/inventory consistency"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Public-gate symlink/TOCTOU and bidirectional membership/path regressions"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: .planning/openspec/<change-id>/handoff.json
      via: "_read_manifest_bytes opens each component relative to the preceding no-follow directory descriptor"
      pattern: "_read_manifest_bytes|dir_fd|O_NOFOLLOW"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      via: "_validate_phase_graph compares both graph maps exactly with validate_planning_inventory output"
      pattern: "validate_planning_inventory|_validate_phase_graph"
---

<objective>
Close repository-anchor and phase-membership consistency gaps for HND-03 / HARD-R2.

Purpose: Prevent repository-external manifest substitution and undeclared phase admission before any lifecycle operation proceeds.
Output: Anchored manifest reads, exact bidirectional graph/inventory checks, and fixed public-gate regressions.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-08-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve `gate_lifecycle_operation` as the sole admission seam and existing manifest/phase issue codes. Change `_read_manifest_bytes` to receive the validated repository root and change ID rather than a prejoined path, and keep bounded read-once behavior. Modify `_validate_phase_graph`; do not add another phase classifier. Reuse `_same_identity`, `validate_planning_inventory`, and the established descriptor-relative patterns from `execution_mapping.py` / `source_identity.py`.
</interfaces>
</context>

## Artifacts this phase produces

- No new module, public symbol, serializer, root export, CLI operation, or evidence schema.
- Existing `_read_manifest_bytes` gains repository-descriptor ancestry validation.
- Existing `_validate_phase_graph` gains exact bidirectional ID/path equality.
- New fixed nodes:
  - `tests/test_handoff_lifecycle_gate.py::test_manifest_intermediate_symlink_is_unknown_and_never_admitted`
  - `tests/test_handoff_lifecycle_gate.py::test_manifest_parent_identity_change_is_unknown_and_never_admitted`
  - `tests/test_handoff_lifecycle_gate.py::test_phase_graph_and_inventory_membership_paths_must_match_exactly`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Same fresh fail-closed decision protects later operations. | 03-10 | COVERED | Manifest and phase prerequisites cannot be substituted or partial. |
| REQ | HND-03 / HARD-R2 | Manifest and phase state are complete and mutually consistent. | 03-10 | COVERED | Public gate rejects all fixed inconsistencies. |
| RESEARCH | ordered trust boundary | Validate repository and graph evidence before comparison/identity. | 03-10 | COVERED | Existing descriptor and graph validators are extended. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | No synthetic context is created. |
| VERIFICATION | root gap 2 | Anchored ancestry plus exact graph/inventory membership/path equality. | 03-10 | COVERED | Every missing behavior has a fixed public-gate node. |
| REVIEW | CR-01, CR-04 | Intermediate symlink and undeclared extra phase admit clean. | 03-10 | COVERED | Reproductions become RED tests. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anchor every manifest path component to the repository descriptor</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: replacing `.planning`, `openspec`, or the change directory with an intermediate symlink to a byte-identical external tree is unknown and non-admitted.
    - RED: replacing or renaming any opened parent during the bounded read returns an identity-change/read failure and no decision identity.
    - GREEN: repository root, every directory component, and final regular file are opened with no-follow flags relative to the preceding descriptor and revalidated after reading.
  </behavior>
  <action>Commit RED as `test(03-10): expose unanchored manifest ancestry`. Drive only `gate_lifecycle_operation`; use fixed symlink components and a deterministic monkeypatched identity-change hook patterned after existing inventory/source descriptor tests. In GREEN, change `_read_manifest_bytes` in place to accept `(root, change_id, limit)`. Open the validated root using `O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC`; open `.planning`, `openspec`, and `change_id` one component at a time with `dir_fd` and directory flags; open `handoff.json` with regular-file no-follow flags. Capture link-side stat and descriptor stat for every entry, validate file type and `_same_identity`, read at limit+1, then re-stat every component and root after the read before returning bytes. Close all descriptors in reverse order on every path. Map symlink/type failures to existing unreadable/read-failed outcomes and observed replacement to `lifecycle-manifest-identity-changed`; do not follow another route. Commit GREEN as `feat(03-10): anchor manifest reads to repository descriptors`. If production code changes during REFACTOR, make exactly one behavior-preserving commit named `refactor(03-10): consolidate manifest descriptor cleanup`; if no refactor is warranted, create no refactor commit and record that decision explicitly in `03-10-SUMMARY.md`. No retry, rollback, repair, route switching, or manifest mutation.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py::test_manifest_intermediate_symlink_is_unknown_and_never_admitted tests/test_handoff_lifecycle_gate.py::test_manifest_parent_identity_change_is_unknown_and_never_admitted -q</automated>
  </verify>
  <acceptance_criteria>
    - Intermediate symlinks at all three directory levels are rejected before manifest parsing.
    - Parent/root/final-file identity changes are rejected after bounded read and expose no partial evidence.
    - Descriptor cleanup occurs for success and all failure paths.
    - RED/GREEN commits use the exact messages; the summary records the REFACTOR decision.
    - Any production-code REFACTOR commit uses exactly `refactor(03-10): consolidate manifest descriptor cleanup`; otherwise `03-10-SUMMARY.md` records the no-refactor decision and no refactor commit exists.
  </acceptance_criteria>
  <done>Manifest bytes cannot escape the repository root or survive a component identity change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require exact bidirectional phase graph and inventory consistency</name>
  <read_first>
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - tests/test_handoff_lifecycle_gate.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: inventory-only phase, expected-graph-only phase, observed-graph-only phase, and path mismatch are phase-incomplete.
    - RED: the same extra phase added to both expected and observed graphs but absent from inventory is phase-incomplete, including FINALIZE.
    - GREEN: each validated graph's `{phase_id: phase_path}` map equals the validated inventory map exactly; subset checks are insufficient.
    - GREEN: valid graph tuple reordering remains identity-invariant.
  </behavior>
  <action>Commit RED as `test(03-10): expose partial graph inventory consistency`. Add one fixed parameter matrix at the public gate with exact `lifecycle-phase-observation-incomplete`, wholly unknown, and no-identity assertions. In GREEN, after raw graph and `validate_planning_inventory` validation, construct phase maps only from validated members and require `expected_map == inventory_map` and `observed_map == inventory_map`; this enforces IDs and canonical paths in both directions before normalization, mapping readiness, phase comparison, or identity. Preserve duplicate/DAG/order rules and existing issue projection. Commit GREEN as `feat(03-10): require exact graph inventory consistency`. If production code changes during REFACTOR, make exactly one behavior-preserving commit named `refactor(03-10): consolidate phase map comparison`; if no refactor is warranted, create no refactor commit and record that decision explicitly in `03-10-SUMMARY.md`. Do not reinterpret a missing declaration as ordinary drift or infer inventory entries from graph nodes. After focused GREEN/REFACTOR verification, run the project-wide `task check` as the final Wave 9 gate; because execution is wave-barriered, this validates the integrated output of both Wave 8 plans plus all 03-10 work.</action>
  <verify>
    <automated>bash -o errexit -c 'uv run pytest tests/test_handoff_lifecycle_gate.py::test_phase_graph_and_inventory_membership_paths_must_match_exactly tests/test_handoff_lifecycle_gate.py::test_identity_ignores_semantically_irrelevant_phase_tuple_order -q; task check'</automated>
  </verify>
  <acceptance_criteria>
    - Inventory-only, graph-only, same-extra-both-graphs, and path mismatch rows all return the exact phase-incomplete decision.
    - No rejected row reaches mapping readiness or identity generation.
    - Valid order-invariance and five-operation behavior remain green.
    - No new property family or second graph authority is introduced.
    - Any production-code REFACTOR commit uses exactly `refactor(03-10): consolidate phase map comparison`; otherwise `03-10-SUMMARY.md` records the no-refactor decision and no refactor commit exists.
    - The final project-wide `task check` runs after all Wave 9 changes and passes against the integrated Wave 8/9 state.
  </acceptance_criteria>
  <done>Only a phase graph exactly equal to the explicit validated inventory can contribute to admission.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| repository namespace → manifest bytes | Intermediate symlinks and TOCTOU changes can substitute repository-external authorization input. |
| phase boundary → PlanningInventory | One-way membership checks can omit readiness declarations for admitted phases. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-10-01 | Spoofing/Tampering | high | manifest ancestry | mitigate | Descriptor-relative no-follow traversal, exact type/identity checks, bounded read, and post-read chain revalidation. |
| T-03-10-02 | Tampering/TOCTOU | high | opened manifest components | mitigate | Retain descriptors, compare link and opened identities before/after, fail closed without retry or alternate path. |
| T-03-10-03 | Elevation of Privilege | high | graph/inventory membership | mitigate | Exact expected/observed/inventory ID-path equality precedes readiness and identity. |
| T-03-10-SC | Tampering | low | package supply chain | accept | No dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `git diff --check`
</verification>

<success_criteria>
- Verification root gap 2 and REVIEW CR-01/CR-04 are closed.
- Manifest ancestry is repository-bound and TOCTOU-revalidated without automatic recovery.
- Graph/inventory membership and paths are exact in both directions.
- All fixed rejected rows are unknown, non-admitted, empty, and identity-free.
- The final project-wide `task check` passes after both Wave 8 plans and all 03-10 Wave 9 work are complete.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-10-SUMMARY.md` with RED/GREEN/REFACTOR commits, descriptor-chain evidence, exact mismatch results, and threat dispositions.
</output>
