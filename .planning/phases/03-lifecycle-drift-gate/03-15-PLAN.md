---
phase: 03-lifecycle-drift-gate
plan: 15
type: tdd
wave: 11
depends_on:
  - 03-12
  - 03-13
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
  - tests/test_handoff_identity.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: read_source_inventory returns Success only when the caller-visible resolved repository path names the same directory identity before open, through the bounded read, and immediately before acceptance."
    - "Repository-root rename, symlink replacement, and different-directory replacement during os.read all return Failure with source-root-identity-changed and expose no SourceInventory."
    - "Detached old-root content is never accepted even when every retained child descriptor and source-file identity remains internally consistent."
    - "Valid repositories and existing child-entry race, symlink, size-limit, parse, and whole-operation failure behavior remain unchanged."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      provides: "Pre-open and post-read repository-root identity binding for canonical source inventory"
    - path: tests/test_handoff_identity.py
      provides: "Fixed public reader fault-injection regressions for root disappearance, symlink substitution, and directory substitution"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "read_source_inventory compares no-follow path stat identity with the retained repository descriptor before and after _read_inventory_from_repository_fd"
      pattern: "follow_symlinks=False|source-root-identity-changed"
    - from: tests/test_handoff_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "os.read boundary fault injection exercises only public read_source_inventory results"
      pattern: "read_source_inventory"
---

<objective>
Close repository-root substitution during canonical source observation for HND-03 / HARD-R2.

Purpose: Keep the complete source inventory bound to the repository path the caller is authorizing, rather than accepting content from a descriptor-valid directory that has been detached and replaced.
Output: Root identity validation spanning the full read plus fixed public-seam race regressions.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-10-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-13-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_identity.py

<interfaces>
Preserve `read_source_inventory(repository_root, source_paths, *, limits=...) -> Result[SourceInventory]`, descriptor-relative `O_NOFOLLOW` child traversal, bounded reads, whole-operation failure, and all existing source path/read issue codes. Add one stable root-race code, `source-root-identity-changed`, for disappearance or identity/type substitution after the initial resolved path is observed.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, test file, public function, private helper, root export, CLI operation, dependency, or schema artifact.
- New stable public issue-code value:
  - `source-root-identity-changed`
- Modified existing production symbols:
  - `read_source_inventory`
- New fixed test node:
  - `tests/test_handoff_identity.py::test_source_inventory_rejects_repository_root_replacement_during_read`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on one fresh fail-closed drift decision. | 03-14, 03-15, 03-16 | COVERED | Source bytes cannot become fresh evidence after their repository path is substituted. |
| REQ | HND-03 / HARD-R2 | Missing, unreadable, malformed, or incomplete canonical inspection stops progression. | 03-15 | COVERED | Root identity is checked across the entire public read. |
| RESEARCH | — | Authorization paths retain no-follow descriptors and compare link-side/opened identities before and after reads. | 03-15 | COVERED | Applies the established manifest ancestry pattern to the source-reader root. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Gap closure is grounded in ROADMAP, canonical OpenSpec, verification, review, and accumulated Phase 3 decisions. |
| VERIFICATION | gap 1 / REVIEW CR-01 | Stale rejection exposes a clean identity that admits its next replay. | 03-14 | COVERED | Same-wave plan owns disjoint lifecycle gate files. |
| VERIFICATION | gap 2 / REVIEW CR-02 | Repository-root replacement can return detached source content. | 03-15 | COVERED | Missing, symlink, and different-directory replacements get fixed public examples. |
| VERIFICATION | gap 3 / REVIEW CR-03 | Public source readers raise for malformed containers/members. | 03-16 | COVERED | Sequential plan owns the same reader files after this root-race fix. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revalidate repository-root identity before accepting source inventory</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_identity.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, tests/test_handoff_identity.py</files>
  <behavior>
    - RED: during the first source `os.read`, rename the repository directory so the retained descriptor still references the original trusted files, then leave the original path absent, replace it with a symlink, or replace it with a different directory.
    - RED: the current implementation returns Success containing detached trusted content for at least the replacement rows because it revalidates child entries but never the repository path.
    - GREEN: every root disappearance, symlink replacement, and different-directory replacement row returns Failure with exact issue code `source-root-identity-changed` and no `value`.
    - GREEN: the root path is no-follow statted before open, matched against the opened directory descriptor, then no-follow statted and matched again after all bounded source reads but before Success can escape and before the descriptor is closed.
    - GREEN: a stable repository still returns the same deterministic SourceInventory, and existing nested-entry identity changes retain their existing source-path-specific codes.
  </behavior>
  <action>RED: add the named parameterized fixed test with replacement rows `missing`, `symlink`, and `directory`. Reuse `_write_source` for trusted content, create distinct attacker content for replacement targets, and monkeypatch only the filesystem boundary `os.read` so the first actual source read renames the repository to a detached path and performs the selected substitution at the original resolved path. Call only `read_source_inventory`; assert the swap happened, require exact `Failure("source-root-identity-changed")`, and require that no inventory `value` or detached heading escapes. Run the focused node and confirm RED reproduces Success from detached content or another nonconforming result, then commit `test(03-15): expose source root replacement during read`. GREEN: in `read_source_inventory`, capture `os.stat(resolved_repository, follow_symlinks=False)` before opening the root, reject an initial symlink/non-directory through the existing initial root semantics, open with the existing no-follow directory flags, and compare `(st_dev, st_ino, stat.S_IFMT(st_mode))` with `os.fstat(repository_fd)` before traversal. After `_read_inventory_from_repository_fd` finishes and while `repository_fd` remains open, no-follow stat the same resolved path again and require the same descriptor identity and directory type before returning any Success. Map disappearance, symlink replacement, different inode/type, or post-read stat failure to `source-root-identity-changed`; preserve a pre-existing unreadable root as `source-root-unreadable`, a stable non-directory as `source-root-invalid`, child-entry codes, read bounds, parser codes, and close-failure behavior. Ensure the descriptor closes on every branch. Do not follow the replacement path, retry, reopen, accept detached content, read attacker content, or change source parsing/reconciliation. Commit `feat(03-15): bind source inventory to repository root`. After GREEN, review only repeated identity tuple construction; create `refactor(03-15): clarify source root identity checks` only if behavior remains identical, otherwise record no REFACTOR commit. Do not modify lifecycle gate identity, evidence JSON, malformed-container policy reserved for 03-16, dependencies, canonical OpenSpec artifacts, retry, repair, rollback, route switch, or final-completion state.</action>
  <acceptance_criteria>
    - The named public-seam test contains fixed missing, symlink, and different-directory replacement rows triggered during the source read.
    - RED proves detached-root content can currently escape as Success or another nonconforming public outcome.
    - GREEN returns exact `source-root-identity-changed` with no `value` for all three rows.
    - Root link-side and descriptor device/inode/type identities match before traversal and are rechecked after reading before Success, with the descriptor closed on all paths.
    - Stable-root inventory, existing parent/file substitution, symlink, size-limit, UTF-8, parse, alias, and later-failure tests remain green.
    - RED and GREEN commits exist in order; the summary records the optional REFACTOR decision and states that phase completion remains with independent verification.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_identity.py::test_source_inventory_rejects_repository_root_replacement_during_read -q</automated>
  </verify>
  <done>Canonical source inventory can no longer be accepted from a descriptor-valid directory detached from the caller-visible repository path.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Security Assets

| Asset | Security property |
|---|---|
| Canonical source inventory | Contains bytes from the same validated repository path throughout the observation. |
| Reusable lifecycle `decision_identity` | Cannot authorize detached or substituted source evidence and cannot replay a stale rejection into admission. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller repository path → retained root descriptor | The path namespace can change independently of an already opened directory. |
| retained root/child descriptors → `SourceInventory` Success | Descriptor-consistent bytes are insufficient unless the caller-visible root still has the same identity. |
| prior identity → lifecycle gate | Stale identity replay is a separate high-severity authorization threat owned by Plan 03-14. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-15-ROOT | Tampering/Elevation of Privilege | high | `read_source_inventory` root path and descriptor | mitigate | Compare no-follow pre-open, opened descriptor, and post-read path identities; fixed public fault injection requires structured rejection for disappearance, symlink, and directory substitution. |
| T-03-15-STALE | Spoofing/Elevation of Privilege | high | lifecycle stale decision identity | transfer | Plan 03-14 recomputes identity from the complete stale decision and proves its returned identity cannot admit a second replay. |
| T-03-15-SC | Tampering | low | package supply chain | accept | No package install, dependency change, or schema operation occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_identity.py::test_source_inventory_rejects_repository_root_replacement_during_read -q`
- `uv run pytest tests/test_handoff_identity.py -q`
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q --no-cov`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- After Wave 12: run the stale replay, root substitution, and malformed-reader nodes together, then `task check` and `git diff --check`; Phase 03 remains incomplete until `$gsd-verify-work` independently closes all three gaps.
- Confirm the diff changes only the declared source/test files and adds no property family, dependency, schema task, retry, repair, rollback, route switch, or later-phase behavior.
</verification>

<success_criteria>
- Verification gap 2 and REVIEW CR-02 are closed at the public `read_source_inventory` seam.
- Root disappearance, symlink replacement, and different-directory replacement cannot contribute SourceInventory or later authorization evidence.
- Valid and existing fail-closed source-reader behavior remains unchanged.
- No unrelated refactor, alternate reader, dependency, property family, or final-completion claim is added.
- The high root-substitution threat is mitigated here; the high stale-replay threat remains explicitly transferred to and blocked on Plan 03-14.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-15-SUMMARY.md` with RED/GREEN/REFACTOR evidence, the three-row root replacement matrix, exact issue outcomes, descriptor cleanup proof, focused/full verification results, threat dispositions, and an explicit statement that independent verification still owns Phase 03 completion.
</output>
