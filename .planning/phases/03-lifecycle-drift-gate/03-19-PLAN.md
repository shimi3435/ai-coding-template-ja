---
phase: 03-lifecycle-drift-gate
plan: 19
type: tdd
wave: 15
depends_on:
  - 03-18
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
  - tests/test_handoff_lifecycle_gate.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: expected and observed phase paths are independently validated as canonical UTF-8 NFC POSIX repository-relative paths before comparison or remediation."
    - "Backslash, NUL, and non-NFC phase paths produce identity-free UNKNOWN decisions with empty remediation through gate_lifecycle_operation."
    - "Valid phase additions, removals, path changes, dependency changes, and combinations retain deterministic DRIFTED classification."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      provides: "Complete canonical phase-path validation in _canonical_phase_path"
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Fixed public-gate regressions for malformed paths on both graph sides"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: gate_lifecycle_operation
      via: "_validate_phase_nodes calls _canonical_phase_path before graph normalization, comparison, remediation, or identity"
      pattern: "_canonical_phase_path|_validate_phase_nodes|_unknown_decision"
---

<objective>
Close the malformed phase-path gap in the shared lifecycle gate for HND-03 / HARD-R2.

Purpose: Prevent malformed source-pinned or current phase paths from receiving reusable drift identities or remediation.
Output: Complete canonical path validation and fixed public regressions on expected and observed graph inputs.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-17-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-18-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_lifecycle_gate.py

<interfaces>
Preserve `PhaseGraphObservation`, `PhaseNodeObservation`, `LifecycleGateDecision`, `gate_lifecycle_operation`, `_validate_phase_nodes`, the `lifecycle-gate-decision-v1` identity contract, valid-clean identity bytes, and existing valid graph-remediation behavior. Strengthen only `_canonical_phase_path`.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, public/private function, dataclass field, package export, dependency, schema, or evidence format.
- New fixed test node: `tests/test_handoff_lifecycle_gate.py::test_malformed_canonical_phase_paths_are_unknown_on_both_graph_sides`.

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One fresh fail-closed drift decision is safe for later lifecycle operations. | 03-19 | COVERED | Malformed graph paths cannot become reusable drift evidence. |
| REQ | HND-03 / HARD-R2 | Malformed phase observations are identity-free UNKNOWN. | 03-19 | COVERED | Fixed public examples cover both graph sides. |
| RESEARCH | — | Validate exact runtime/scalar/path form before normalization or comparison. | 03-19 | COVERED | Reuses the established ordered trust boundary. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Operator approved planning without it. |
| VERIFICATION | gap 1 / CR-01 | Backslash, NUL, and non-NFC phase paths currently mint DRIFTED identities. | 03-19 | COVERED | This plan owns only phase-path validation. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject malformed phase paths before graph semantics</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md (`HARD-R2`, complete phase graph drift)
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py (`_canonical_phase_path`, `_validate_phase_nodes`, `_validate_phase_graph`, `gate_lifecycle_operation`)
    - tests/test_handoff_lifecycle_gate.py (`_phase_nodes`, `_malformed_phase_nodes`, `test_malformed_phase_graph_is_unknown_without_raising`, complete graph-drift tests)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - RED: each of backslash, embedded NUL, and NFD/non-NFC path spellings on expected_nodes and observed_nodes returns UNKNOWN, admitted false, `lifecycle-phase-observation-incomplete`, empty changed/remediation/action tuples, and no decision identity.
    - GREEN: a canonical `.planning/phases/{phase_id}-{slug}` path remains accepted.
    - GREEN: the existing valid add/remove/path/dependency/simultaneous graph cases retain deterministic DRIFTED results and stable valid-clean identity compatibility.
  </behavior>
  <action>RED: add the named fixed public-gate parameter table with independent `side` and malformed-path cases. Mutate only the chosen graph side through the existing boundary fixture and assert the complete identity-free UNKNOWN projection, not only the issue code. Run the node and require it to fail on the current identity-bearing DRIFTED behavior; commit `test(03-19): expose malformed canonical phase paths`.

GREEN: change `_canonical_phase_path` to reject non-string/empty/absolute values, backslash, NUL, empty/dot/dot-dot components, and any component whose NFC normalization differs. Split on literal `/`, require exactly `(".planning", "phases", "{phase_id}-{nonempty-suffix}")`, and require reconstruction to equal the original value. Keep the check inside the shared `_validate_phase_nodes` path so expected and observed graphs receive identical validation before normalization, inventory comparison, `_phase_changes`, `_downstream_phases`, or `_decision_identity`. Do not alter valid graph projection, target classification, public fields, issue codes, identity framing, or the approved A-P-GRAPH/B-P-PATH-ROLE property families. Run the focused node, existing malformed graph table, complete graph-drift selection, full lifecycle-gate file, Ruff, and BasedPyright; commit `feat(03-19): validate canonical phase paths before drift`.

REFACTOR only if duplication is demonstrated; otherwise record no REFACTOR commit. Add no Hypothesis family, dependency, schema operation, retry, repair, rollback, route switch, or evidence repin.</action>
  <acceptance_criteria>
    - The named test covers three malformed path families on both expected and observed graph sides through `gate_lifecycle_operation`.
    - All six malformed rows return the exact identity-free UNKNOWN projection before comparison or remediation.
    - Canonical paths and all existing valid graph-drift behaviors remain green without changing public fields, identity discriminator, or valid-clean identity bytes.
    - Only the declared source and test files change; no property family, schema, dependency, or evidence artifact changes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py::test_malformed_canonical_phase_paths_are_unknown_on_both_graph_sides tests/test_handoff_lifecycle_gate.py::test_malformed_phase_graph_is_unknown_without_raising -q</automated>
  </verify>
  <done>Malformed phase paths on either graph side stop as identity-free UNKNOWN before any drift comparison or remediation, while valid graph behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| source-pinned/current graph → lifecycle gate | Caller-constructed phase path strings are untrusted authorization input. |
| validated graph → remediation/identity | Only canonical paths may contribute to a reusable decision. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-19-PATH | Tampering/Elevation of Privilege | high | `_canonical_phase_path` / `_validate_phase_nodes` | mitigate | Validate POSIX shape, forbidden bytes/components, and NFC on both graph sides before semantic work; fixed public tests require identity-free UNKNOWN. |
| T-03-19-REG | Tampering | high | valid graph drift projection | mitigate | Rerun complete fixed graph and approved property evidence to prevent malformed-input hardening from suppressing valid drift. |
| T-03-19-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_gate.py::test_malformed_canonical_phase_paths_are_unknown_on_both_graph_sides tests/test_handoff_lifecycle_gate.py::test_malformed_phase_graph_is_unknown_without_raising -q`
- `uv run pytest tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `git diff --check`
</verification>

<success_criteria>
- Verification gap 1 path branch and review CR-01 are closed at the public lifecycle seam.
- Malformed phase paths cannot produce identity, changed items, revalidation targets, replanning targets, or action codes.
- Valid fixed graph examples and the locked property-testing boundary remain green.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-19-SUMMARY.md` when done.
</output>
