---
phase: 03-lifecycle-drift-gate
plan: 06
type: tdd
wave: 5
depends_on:
  - "03-04"
  - "03-05"
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
  - tests/test_handoff_lifecycle_gate.py
  - tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
  - .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json
autonomous: true
requirements:
  - HND-03
gap_closure: true
must_haves:
  truths:
    - "HND-03 / HARD-R2: the shared public LifecycleGateDecision exposes deterministic drifted artifact paths and a separate checkbox progress update candidate."
    - "Unknown public decisions expose empty drifted paths and no progress candidate."
    - "Decision identity binds the validated repository real path, so byte-identical repositories cannot reuse each other's prior identity."
    - "Portable tracked and golden evidence exposes every newly public decision field without serializing a raw repository-dependent decision identity."
    - "Portable evidence records deterministic relation booleans proving same-root identity stability, cross-root identity separation, and foreign-root replay rejection."
    - "All seven Phase 3 verification counterexamples are green under focused tests and task check."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      provides: "Complete public lifecycle decision projection and repository-bound identity"
      exports:
        - LifecycleGateDecision
        - gate_lifecycle_operation
    - path: tests/test_handoff_lifecycle_gate.py
      provides: "Public decision-field, cross-repository replay, and portable relation-evidence regressions"
    - path: tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
      provides: "Independent portable literal golden with complete decision fields and repository-identity relation booleans"
    - path: .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json
      provides: "Tracked source-pinned portable lifecycle evidence with no raw repository-dependent identity"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: CanonicalSourceDriftDecision
      via: "`_decision_from_observation` projects paths and progress without recomputation"
      pattern: "drifted_artifact_paths|progress_update_candidate"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
      to: SourceCommitObservation.repository_root
      via: "`_decision_identity` length-prefix encodes the validated repository real path"
      pattern: "source_commit.repository_root"
    - from: tests/test_handoff_lifecycle_gate.py
      to: .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json
      via: "the versioned portable producer strips raw identities, emits repository-identity relation booleans, and compares exact bytes"
      pattern: "_repository_root_lifecycle_evidence|test_repository_root_lifecycle_evidence_matches_tracked_record"
---

<objective>
Complete the HND-03 / HARD-R2 public decision contract and make its reusable identity repository-scoped.

Purpose: Ensure callers receive every canonical remediation/progress output and cannot replay a decision across repository identities without leaking repository-dependent digests into portable reviewer artifacts.
Output: Two new immutable `LifecycleGateDecision` fields, repository-bound internal decision digests, public regressions, and portable relation-based deterministic evidence.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-03-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
@tests/test_handoff_lifecycle_gate.py
@tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
@.planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json

<interfaces>
Extend frozen `LifecycleGateDecision` with `drifted_artifact_paths: tuple[str, ...]` and `progress_update_candidate: Progress | None`. This plan consumes the validated/normalized observations delivered by Plans 03-04 and 03-05, then follows the remaining research order: project the lower decision without recomputation, bind every complete admission input including the validated repository real path, and only afterward compare a prior identity. Complete decisions copy these values from `CanonicalSourceDriftDecision`; unknown decisions use `()` and `None`. Keep the internal `lifecycle-gate-decision-v1` typed length-prefix encoder contract and add `SourceCommitObservation.repository_root` plus both public decision fields to its bound input set. Prior identity mismatch remains `lifecycle-decision-stale`. The portable reviewer schema becomes `lifecycle-evidence-v2` with producer `repository-portable-lifecycle-evidence-v2`: `_decision_view` must omit raw repository paths, `decision_identity`, and `prior_decision_identity`, represent identity availability only as a boolean where needed, and expose `same_root_identity_stable`, `cross_root_identities_distinct`, and `foreign_root_prior_identity_rejected` as deterministic relation booleans.
</interfaces>
</context>

## Artifacts this phase produces

- `src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py`
  - new public field: `LifecycleGateDecision.drifted_artifact_paths`
  - new public field: `LifecycleGateDecision.progress_update_candidate`
  - modified public identity behavior: `LifecycleGateDecision.decision_identity`
  - modified public behavior: `gate_lifecycle_operation`
- `tests/test_handoff_lifecycle_gate.py`
  - portable `_decision_view` serialization for both new fields without raw decision identities
  - public drift/progress/unknown field regressions
  - same-root stability, cross-root separation, and stale-replay relation regressions
- `tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json`
  - independent `lifecycle-evidence-v2` golden with decision fields and fixed repository-identity relation booleans
- `.planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json`
  - regenerated portable deterministic public-seam evidence with no raw repository-dependent identity
- `.planning/phases/03-lifecycle-drift-gate/03-06-SUMMARY.md`
  - complete gap-closure and full-check evidence

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on the same complete fresh decision. | 03-06 | COVERED | Public output and reusable identity are completed together. |
| REQ | HND-03 / HARD-R2 | Report changed artifacts/source items, progress, remediation, and stale-safe identity. | 03-06 | COVERED | Uses the existing public decision and evidence producer. |
| VERIFICATION | Gap 5 | LifecycleGateDecision drops artifact paths and progress candidate. | 03-06 | COVERED | Adds immutable fields, projection, unknown defaults, identity, and evidence. |
| VERIFICATION | Gap 6 | Decision identity omits repository identity. | 03-06 | COVERED | Adds validated real path to the internal digest, proves cross-repository replay rejection, and records only portable relation booleans in tracked/golden evidence. |
| REVIEW | Plan checker blocker | Raw repository-bound identities cannot be fixed golden values across different temporary roots. | 03-06 | COVERED | Bumps the evidence schema/producer and removes raw current/prior identities from portable serialization while preserving identity-based admission checks. |
| VERIFICATION | Gaps 1–4, 7 | Other reproduced fail-closed blockers. | 03-04, 03-05 | COVERED | This plan runs their focused suites and final project gate after both dependencies. |
| RESEARCH | G5 | Public projection copies exact paths/progress, gives unknown empty/null values, and binds both fields in identity/evidence. | 03-06 | COVERED | Reuses the frozen-decision and `_decision_from_observation` analogs without reclassification. |
| RESEARCH | G6 | Runtime identity binds the validated real root while portable evidence proves only stable relations. | 03-06 | COVERED | Keeps the existing typed encoder and constant-time prior comparison; no raw path/current/prior identity is portable. |
| VALIDATION | 03-06-01, 03-06-02 | Nyquist requires the named projection slice followed by both lifecycle suites and `task check`. | 03-06 | COVERED | Task commands match `03-VALIDATION.md` exactly; Wave 4 backstop runs before this plan's RED work. |
| PATTERNS | public decision and portable producer | Frozen projection, typed hashes, relation evidence, protected inputs, and one operation matrix remain intact. | 03-06 | COVERED | Existing analogs guide serialization but do not create a second production authority. |
| CONTEXT | — | No Phase 3 CONTEXT.md exists. | — | EXCLUDED | Continuation without context was explicitly approved. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project complete source drift and progress evidence onto the public decision</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - .planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_drift.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_gate.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/progress.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py</files>
  <behavior>
    - A non-checkbox canonical artifact change reports its exact UTF-8-sorted path on `LifecycleGateDecision.drifted_artifact_paths`.
    - A checkbox-only tasks change remains clean/admitted and exposes the current immutable `Progress` as `progress_update_candidate`.
    - Unknown decisions expose `drifted_artifact_paths == ()` and `progress_update_candidate is None`.
    - The two fields are included in the internal decision identity and portable public decision view; the view does not expose the raw repository-dependent identity.
  </behavior>
  <action>Before RED, run the Wave 4 dependency backstop from `03-VALIDATION.md`: `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q`; stop if either independent dependency is not green. RED: extend the existing canonical drift, checkbox-only progress, and incomplete-decision public tests to assert the two missing fields; the tests must fail because `LifecycleGateDecision` lacks them. GREEN: after the dependencies' raw validation and normalization, add frozen fields `drifted_artifact_paths` and `progress_update_candidate`, copy the already classified `observation.source_decision` values in `_decision_from_observation`, and set empty/null values in `_unknown_decision`; sort paths with the existing UTF-8 ordering helper but do not reclassify source bytes or synthesize progress. Extend the internal `_decision_identity` decision-domain encoding so both fields are identity-bound after projection. Extend `_decision_view` with public paths/progress values while excluding raw repository paths and repository-dependent digest values; identity availability may be represented only by a boolean. PATTERNS' projection and serializer analogs are partial implementation guides, not authorization to omit any existing decision field. REFACTOR: centralize only repeated projection/serialization discovered by these cases and retain the sole production admission seam.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py -q -k "canonical_source_has_exact_remediation or checkbox_progress_public_decision or incomplete_dimension"</automated>
  </verify>
  <acceptance_criteria>
    - Public canonical drift returns the exact changed artifact path together with existing source IDs and remediation targets.
    - Public checkbox-only clean returns a non-null progress candidate without reporting artifact/source drift.
    - Every unknown construction path has empty paths, no progress candidate, no identity, and `admitted=False`.
    - No caller must inspect `CanonicalSourceDriftDecision` privately to recover these values.
  </acceptance_criteria>
  <done>The shared public decision preserves all canonical drift and checkbox-progress evidence required by HARD-R2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind repository identity and publish portable relation evidence</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
    - .planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
    - .planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py
    - tests/test_handoff_lifecycle_gate.py
    - tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json
    - .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json
    - .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
    - .planning/phases/03-lifecycle-drift-gate/03-03-SUMMARY.md
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py, tests/test_handoff_lifecycle_gate.py, tests/fixtures/openspec_gsd_handoff/lifecycle/expected-lifecycle-evidence.json, .planning/phases/03-lifecycle-drift-gate/03-LIFECYCLE-EVIDENCE.json</files>
  <behavior>
    - Byte-identical fixtures under two different resolved repository roots produce different lowercase 64-hex decision identities.
    - Repository A's identity supplied as `prior_decision_identity` in repository B is rejected as stale and never admitted.
    - Repeated observation in the same repository with unchanged inputs preserves the identity and remains admitted.
    - Tracked evidence and independent golden contain both public fields for every decision row but contain neither raw current identity nor raw prior identity.
    - The versioned evidence records `same_root_identity_stable=true`, `cross_root_identities_distinct=true`, and `foreign_root_prior_identity_rejected=true`.
  </behavior>
  <action>RED: add public tests using two byte-identical `_fixture` repositories. Assert a lowercase 64-hex identity is stable across repeated observations of the same resolved root, differs for the other resolved root, and causes `lifecycle-decision-stale` with no admission when repository A's transient identity is supplied to repository B. Replace the existing fixed digest literal assertion with these shape and relation assertions; do not pin any repository-dependent digest value. GREEN: complete the research order by adding exact tag `source_commit.repository_root` to `_decision_identity` using the already validated real path from `SourceCommitObservation`, then retaining the existing prior-identity validation and `hmac.compare_digest` step. Keep `lifecycle-gate-decision-v1`, typed framing, and lowercase SHA-256. Change `_decision_view` so portable rows omit repository-root values, `decision_identity`, and `prior_decision_identity`, using a boolean identity-presence field only where the evidence contract distinguishes reusable from unknown decisions. Bump the evidence schema to `lifecycle-evidence-v2` and producer to `repository-portable-lifecycle-evidence-v2`. Have `_repository_root_lifecycle_evidence` compute identities transiently from same-root repetitions and two distinct temporary repository roots, then serialize only `same_root_identity_stable`, `cross_root_identities_distinct`, and `foreign_root_prior_identity_rejected`; the last relation is true only when repository B returns `lifecycle-decision-stale`, `DRIFTED`, and `admitted=False`. Run all G1-G7 focused regressions and the two lifecycle files green before editing either evidence JSON. Repin `03-LIFECYCLE-EVIDENCE.json` last from the public test-side producer, then independently update `expected-lifecycle-evidence.json` with both public decision fields and the three relation booleans. Preserve source-pinned provenance, protected-input checks, the literal golden's prohibition on calling or reproducing the private encoder, and byte equality across different temporary root layouts. REFACTOR: keep optional real OpenSpec/GSD/host smoke uninvoked and outside normal CI; only its isolation test may run.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_lifecycle_gate.py -q -k "repository_root_identity or repository_root_lifecycle_evidence"</automated>
  </verify>
  <acceptance_criteria>
    - Cross-repository identities differ and cross-root prior identity reuse returns `lifecycle-decision-stale`, `DRIFTED`, and `admitted=False`.
    - Same-repository unchanged reuse remains admitted after a fresh observation and retains the same lowercase 64-hex identity.
    - Every serialized decision row contains both new public fields; unknown rows use empty/null values, and no serialized row contains `decision_identity` or `prior_decision_identity`.
    - Tracked evidence and golden use `lifecycle-evidence-v2` / `repository-portable-lifecycle-evidence-v2` and contain all three repository-identity relation booleans set to true.
    - Producer runs rooted at different temporary paths are byte-identical to the tracked record, and parsed tracked evidence equals the independent literal golden.
    - `task check` passes all gap regressions and existing project checks without invoking optional real-tool smoke.
  </acceptance_criteria>
  <done>Decision reuse is repository-scoped while tracked and golden reviewer evidence remains portable and proves the identity contract without pinning raw repository-dependent digests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical source decision → public lifecycle decision | Dropped paths or progress can prevent correct revalidation while still presenting a complete-looking decision. |
| repository A identity → repository B gate | Content-identical repositories are distinct admission contexts and must not share reusable identities. |
| public lifecycle decision → tracked/golden evidence | Serialization can omit new blocking fields or leak repository-dependent digest/path values that cannot be portable golden data. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06-01 | Repudiation/Tampering | high | `LifecycleGateDecision` projection | mitigate | Copy classified paths/progress into frozen public fields, bind them in identity, and prove unknown exposes no partial values. |
| T-03-06-02 | Spoofing/Elevation of Privilege/Tampering/Information Disclosure | high | repository-scoped identity and portable lifecycle evidence | mitigate | Encode the validated repository real path, reject a foreign digest with constant-time comparison, omit raw roots/current/prior identities from portable rows, and compare the three relations plus independent golden. |
| T-03-06-SC | Tampering | low | package supply chain | accept | No package install or dependency change is in scope. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/lifecycle_gate.py tests/test_handoff_lifecycle_gate.py`
- `task check`
- `uv run pytest tests/test_taskfile.py::test_handoff_smoke_is_explicit_and_isolated_from_normal_check -q`
- `git diff --check`
- Confirm canonical OpenSpec artifacts, tracked handoff, optional smoke record, Taskfile, package exports, and CLI files are unchanged; do not execute the optional real-tool smoke.
</verification>

<success_criteria>
- Verification gaps 5 and 6 are closed at the public lifecycle gate and evidence seams.
- Plans 03-04 through 03-06 collectively close all seven reproduced blockers with no deferred item.
- HND-03 / HARD-R2 now provides complete fail-closed public decisions and repository-scoped stale protection.
- Deterministic evidence is repinned under the portable v2 schema, contains no raw repository-dependent identity, proves all three repository-identity relations, and passes normal CI.
- No canonical specification text, mutation authority, retry, rollback, repair, route switch, or new dependency is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-06-SUMMARY.md` with RED/GREEN/REFACTOR evidence, new public fields, same-root/cross-root/replay relation results, portable evidence hashes, confirmation that no raw repository-dependent identity was serialized, and full `task check` output.
</output>
