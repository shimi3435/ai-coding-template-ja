---
phase: 03-lifecycle-drift-gate
plan: 13
type: tdd
wave: 10
depends_on:
  - 03-10
  - 03-11
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
  - tests/test_handoff_identity.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: reconcile_source_items validates the SourceInventory outer value, items container, and every SourceObservation member and field before dereference, len, iteration, comparison, hashing, or path use."
    - "Scenario and explicit-match parent locators are exact SourceParentLocator values with exact string fields before any nested attribute or dictionary-key use."
    - "Malformed explicit_matches containers and members return source-explicit-match-invalid before len or iteration."
    - "Every malformed public reconciliation row returns Failure without AttributeError, TypeError, partial SourceReconciliation state, or allocation progress."
    - "Fixed public-seam examples cover outer, container, member, nested-parent, and explicit-match families; no new property family is added."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      provides: "Validation-before-use for all public source reconciliation aggregate inputs"
    - path: tests/test_handoff_identity.py
      provides: "Fixed reconcile_source_items runtime-shape regressions with stable structured failure codes"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "reconcile_source_items routes inventory and explicit matches through the existing validators before sorting, hashing, path validation, or allocation"
      pattern: "_validate_inventory|_validate_explicit_matches|reconcile_source_items"
    - from: tests/test_handoff_identity.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "fixed tests call only the public reconcile_source_items Result seam"
      pattern: "reconcile_source_items"
---

<objective>
Close malformed structured-input safety at the public source reconciliation seam for HND-03 / HARD-R2.

Purpose: Ensure caller-constructed inventories, nested parent locators, and explicit matches fail closed as stable structured failures before Python runtime operations can raise or expose partial allocation state.
Output: Ordered validation in the existing reconciliation validators plus fixed public-seam regressions for every reproduced runtime-shape family.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-RESEARCH.md
@.planning/phases/03-lifecycle-drift-gate/03-PATTERNS.md
@.planning/phases/03-lifecycle-drift-gate/03-07-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-11-SUMMARY.md
@src/ai_coding_template_ja/openspec_gsd_handoff/models.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_identity.py

<interfaces>
Preserve the public `reconcile_source_items(inventory, previous_state, *, explicit_matches=()) -> Result[SourceReconciliation]` signature and its successful allocation/reconciliation semantics. Keep `source-inventory-invalid` for malformed inventory runtime shape, `source-parent-unresolved` for a well-shaped scenario whose parent is semantically absent or unresolved, and `source-explicit-match-invalid` for malformed explicit-match shape. Existing semantic ambiguity, collision, limit, Unicode, and prior-state issue codes remain unchanged.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, public symbol, private helper, root export, CLI operation, dependency, or evidence schema.
- Existing `_validate_inventory` and `_validate_explicit_matches` gain complete runtime-shape checks before use.
- New fixed test nodes:
  - `tests/test_handoff_identity.py::test_reconcile_rejects_malformed_inventory_runtime_shapes`
  - `tests/test_handoff_identity.py::test_reconcile_rejects_malformed_parent_locator_runtime_shapes`
  - `tests/test_handoff_identity.py::test_reconcile_rejects_malformed_explicit_match_runtime_shapes`

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation relies on the same fresh fail-closed drift decision. | 03-12, 03-13 | COVERED | 03-13 makes the source-observation dependency return structured non-success for malformed aggregates. |
| REQ | HND-03 / HARD-R2 | Incomplete source inspection is unknown/non-success and stops without exceptions. | 03-12, 03-13 | COVERED | Both remaining verifier gaps are assigned without scope expansion. |
| RESEARCH | ordered trust boundary | Validate outer, container, member, field, and invariant before semantic operations. | 03-13 | COVERED | The existing Result vocabulary and validator locations are retained. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | The explicit gap-closure invocation authorizes planning from verification and existing decisions. |
| VERIFICATION | remaining gap 1 / REVIEW CR-01 | Non-empty expected changed IDs must not become clean, admitted, or reusable. | 03-12 | COVERED | Independent same-wave plan with disjoint files. |
| VERIFICATION | remaining gap 2 / REVIEW CR-02 | Malformed inventory, parent, and explicit-match values must return Failure. | 03-13 | COVERED | All named public-seam families receive fixed examples. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate reconciliation aggregates before every runtime use</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/models.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - tests/test_handoff_identity.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, tests/test_handoff_identity.py</files>
  <behavior>
    - RED: `inventory=object()` and a SourceInventory whose `items` is not an exact tuple return `source-inventory-invalid` instead of AttributeError or unsafe iteration.
    - RED: an invalid inventory member, invalid SourceObservation scalar field, string/object scenario parent locator, or parent locator with a non-string field returns `source-inventory-invalid` before path, normalization, comparison, hashing, or dictionary-key use.
    - RED: `explicit_matches=None`, an unsupported/non-sequence container, string/bytes, invalid member, invalid scalar field, malformed parent locator, or malformed source ID returns `source-explicit-match-invalid` before len, iteration, regex, path, comparison, or hashing.
    - GREEN: a well-shaped scenario with no resolvable parent retains `source-parent-unresolved`, and existing ambiguity/collision/limit/Unicode codes retain their current semantics.
    - GREEN: valid empty and populated inventory/explicit-match reconciliation results remain unchanged and deterministic.
  </behavior>
  <action>Write the three named fixed public-seam tests first using `cast(Any, ...)`, frozen-dataclass replacement, and the existing `_empty_source_state`, requirement, and scenario fixtures. Parameterize finite outer/container/member/field/nested-parent families; call only `reconcile_source_items`, require `Failure`, assert the exact stable issue code, and assert no `value` or partial state is exposed. Run the focused command and confirm RED reproduces AttributeError/TypeError or the wrong structured outcome, then commit `test(03-13): expose malformed reconciliation aggregate inputs`. In GREEN, order `_validate_inventory` as exact `SourceInventory` outer check, exact `items` tuple check, bound check, exact `SourceObservation` member check, exact category/scalar field checks, then path/normalization/content invariants. For scenario observations, require an exact `SourceParentLocator` and exact `source_path`/`normalized_heading` strings before any nested attribute, path, string, equality, identity-tuple, set, sort, byte-count, or hashing use; preserve the existing semantic unresolved-parent result for a correctly shaped missing parent. Order `_validate_explicit_matches` as accepted non-string `Sequence` container check before `len`, bound check, exact `ExplicitSourceMatch` member and scalar-field checks, then exact optional parent-locator and nested-string checks before `_explicit_match_category`, regex, inventory-key comparison, dictionary lookup, or hashing. Convert every malformed inventory family to `source-inventory-invalid` and every malformed explicit-match family to `source-explicit-match-invalid` through the existing `_SourceInputError`/`Failure` path. Do not use a broad exception handler, coerce or repair values, allocate before validation completes, duplicate `validate_source_identity_state`, or change successful reconciliation ordering. Commit `feat(03-13): validate reconciliation inputs before use`. After GREEN, review only validator duplication and create `refactor(03-13): consolidate reconciliation shape checks` only if behavior remains identical; otherwise record no refactor in the summary. Do not add property tests, a second public validator, dependencies, filesystem behavior, retry, repair, rollback, or later-phase ownership logic.</action>
  <acceptance_criteria>
    - All outer/container/member/scalar/nested-parent inventory rows return `Failure` with issue code `source-inventory-invalid` without AttributeError or TypeError.
    - All malformed explicit-match container/member/scalar/parent/source-ID rows return `Failure` with issue code `source-explicit-match-invalid` without AttributeError, TypeError, or partial matches.
    - Every parent field is runtime-validated before path or string operations and before `SourceParentLocator` participates in a hashed key.
    - Existing well-shaped `source-parent-unresolved`, ambiguity, collision, limit, Unicode, valid allocation, rename-match, and deterministic-order tests remain green.
    - RED fails on the reproduced public inputs, GREEN passes after ordered validation, and the summary records the optional REFACTOR decision.
    - Tests remain fixed examples; the existing checkbox normalization seam remains the only Phase 3 Hypothesis family.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_identity.py::test_reconcile_rejects_malformed_inventory_runtime_shapes tests/test_handoff_identity.py::test_reconcile_rejects_malformed_parent_locator_runtime_shapes tests/test_handoff_identity.py::test_reconcile_rejects_malformed_explicit_match_runtime_shapes -q</automated>
  </verify>
  <done>Malformed reconciliation aggregates always become stable structured failures before runtime use and never expose partial allocation state.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller inventory aggregate → reconciliation validator | Python annotations and frozen dataclasses do not enforce outer/container/member/field runtime shape. |
| nested parent and explicit-match values → path/hash/comparison/allocation logic | Malformed nested values can raise, alias keys, or allow partial allocation work before failure. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-13-01 | Tampering/Denial of Service | high | `_validate_inventory` | mitigate | Validate exact outer, tuple, member, scalar, and nested-parent shapes before every len, iteration, path, sort, set, byte, or hash operation; fixed public tests require structured failure. |
| T-03-13-02 | Tampering/Elevation of Privilege | high | `_validate_explicit_matches` | mitigate | Validate the accepted sequence container and every match/parent field before semantic lookup or hashing; malformed values expose no match or allocation state. |
| T-03-13-03 | Repudiation | medium | public `reconcile_source_items` failure result | mitigate | Preserve stable issue codes and require every fixed malformed row to return `Failure` without a partial `value`. |
| T-03-13-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_identity.py::test_reconcile_rejects_malformed_inventory_runtime_shapes tests/test_handoff_identity.py::test_reconcile_rejects_malformed_parent_locator_runtime_shapes tests/test_handoff_identity.py::test_reconcile_rejects_malformed_explicit_match_runtime_shapes -q`
- `uv run pytest tests/test_handoff_identity.py -q`
- `uv run pytest tests/test_handoff_lifecycle_drift.py tests/test_handoff_lifecycle_gate.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- `task check`
- `git diff --check`
- Confirm the diff changes only the declared source/test files, adds no property family or dependency, and leaves lifecycle gate, mapping, evidence artifacts, canonical OpenSpec files, and Phase 4-6 surfaces untouched.
</verification>

<success_criteria>
- Verification remaining gap 2 and REVIEW CR-02 are closed at the public `reconcile_source_items` seam.
- Malformed outer, container, member, field, nested-parent, and explicit-match families return stable structured failures without exceptions or partial state.
- Existing valid reconciliation behavior and established semantic issue codes remain unchanged.
- No broad exception sanitizer, unrelated refactor, new public authority, dependency, property family, or later-phase behavior is added.
- All high-severity scoped threats are mitigated.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-13-SUMMARY.md` with RED/GREEN/REFACTOR evidence, the malformed-family failure matrix, focused/full verification results, and threat dispositions.
</output>
