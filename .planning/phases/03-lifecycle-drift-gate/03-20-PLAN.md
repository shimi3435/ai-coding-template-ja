---
phase: 03-lifecycle-drift-gate
plan: 20
type: tdd
wave: 15
depends_on:
  - 03-18
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
  - tests/test_handoff_identity.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "HND-03 / HARD-R2: both public source inventory readers validate the exact SourceIdentityLimits outer type and every field before attribute access."
    - "Wrong outer values, subclasses, booleans, floats, zero, and negative fields return Failure with source-limits-invalid and never raise or begin filesystem work."
    - "Exact positive SourceIdentityLimits values and existing item/file/total limit boundaries remain unchanged."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      provides: "Validation-before-dereference for SourceIdentityLimits"
    - path: tests/test_handoff_identity.py
      provides: "Fixed public-reader malformed limit regressions"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      to: source_inventory_from_bytes
      via: "_valid_limits rejects malformed outer/field values before source aggregate work"
      pattern: "_valid_limits|source-limits-invalid|source_inventory_from_bytes"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      to: read_source_inventory
      via: "_valid_limits rejects malformed limits before repository/path/filesystem work"
      pattern: "_valid_limits|source-limits-invalid|read_source_inventory"
---

<objective>
Close malformed SourceIdentityLimits handling at both public source readers for HND-03 / HARD-R2.

Purpose: Convert malformed caller-supplied bounds into stable structured failure before Python attribute access or filesystem observation.
Output: One exact limits validator and fixed public-reader regressions.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-16-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_identity.py

<interfaces>
Preserve `SourceIdentityLimits`, `source_inventory_from_bytes`, `read_source_inventory`, accepted exact positive limits, established source aggregate/path/content issue-code precedence, and valid deterministic SourceInventory values. Strengthen only `_valid_limits`.
</interfaces>
</context>

## Artifacts this phase produces

- No new production module, function, dataclass field, package export, dependency, schema, or issue-code value.
- New fixed test node: `tests/test_handoff_identity.py::test_public_source_readers_reject_malformed_limits`.

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Later lifecycle operations receive fail-closed source observations. | 03-20 | COVERED | Malformed limits cannot escape as exceptions. |
| REQ | HND-03 / HARD-R2 | Malformed/over-limit source observation stops without partial green evidence. | 03-20 | COVERED | Both public readers share the exact validator. |
| RESEARCH | — | Validate outer objects and exact positive integer bound fields before dereference. | 03-20 | COVERED | Matches existing gate/refresh limit patterns. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | Operator approved planning without it. |
| VERIFICATION | gap 1 / WR-01 | Malformed SourceIdentityLimits raises AttributeError. | 03-20 | COVERED | This plan owns only limits validation. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate SourceIdentityLimits before both public readers dereference it</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-VERIFICATION.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py (`SourceIdentityLimits`, `_valid_limits`, `source_inventory_from_bytes`, `read_source_inventory`)
    - tests/test_handoff_identity.py (`test_source_inventory_from_bytes_rejects_malformed_file_inputs`, `test_read_source_inventory_rejects_malformed_path_inputs`, `test_inventory_enforces_item_file_and_total_limit_plus_one`)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py, tests/test_handoff_identity.py</files>
  <behavior>
    - RED: `None`, `object()`, and a `SourceIdentityLimits` subclass return `source-limits-invalid` from each public reader without AttributeError.
    - RED: bool, float, zero, and negative values in each of `max_items`, `bytes_per_file`, and `bytes_total` return the same structured failure from each reader.
    - GREEN: malformed-limit rows expose no value and the filesystem-backed reader performs no repository/path/filesystem operation.
    - GREEN: exact positive fields, existing limit values, and limit+1 source item/file/total outcomes retain their current results.
  </behavior>
  <action>RED: add the named fixed parameter table over reader seam and malformed limit case. Construct wrong outer values with `cast(Any, ...)`; define a local subclass only for the exact-type rejection row. For field rows, use `replace(SourceIdentityLimits(), field=value)` with bool, float, zero, and negative values. Instrument `read_source_inventory` at its repository/filesystem boundary and require zero calls for every invalid limit. Assert exact `Failure("source-limits-invalid")` with no value and no exception. Run the node and require failure on current attribute dereference or subclass acceptance; commit `test(03-20): expose malformed source identity limits`.

GREEN: change `_valid_limits` to accept `object`, first require `type(limits) is SourceIdentityLimits`, and only then read `max_items`, `bytes_per_file`, and `bytes_total`; require `type(field) is int and field > 0` for every field. Keep both public readers' existing early `source-limits-invalid` branch and all later aggregate, canonical path, byte, item, file, total, parsing, root, and reconciliation codes unchanged. Do not catch AttributeError, coerce fields, accept subclasses, or create a second validator. Run the focused node, both Plan 03-16 aggregate tests, exact limit/limit+1 tests, full identity file, Ruff, and BasedPyright; commit `feat(03-20): validate source identity limits before use`.

REFACTOR only if duplication is demonstrated; otherwise record no REFACTOR commit. Add no property family, dependency, schema operation, filesystem retry, repair, rollback, or route switch.</action>
  <acceptance_criteria>
    - The named public-reader table covers wrong outer values, a subclass, and all four invalid numeric kinds for every limits field at both readers.
    - Every invalid row returns exact `source-limits-invalid`, exposes no value, raises no exception, and performs no filesystem work.
    - Exact positive limits and existing aggregate/path/content/limit boundary results remain unchanged.
    - One `_valid_limits` authority serves both readers; only the declared source/test files change.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_identity.py::test_public_source_readers_reject_malformed_limits tests/test_handoff_identity.py::test_inventory_enforces_item_file_and_total_limit_plus_one -q</automated>
  </verify>
  <done>Both public source readers reject malformed SourceIdentityLimits as stable structured failure before dereference or filesystem work.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller limits → public source readers | Python annotations and frozen dataclasses do not validate runtime outer/field shape. |
| validated bounds → allocation/filesystem | Only exact positive bounds may authorize source work. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-20-LIMIT | Tampering/Denial of Service | high | `_valid_limits`, both public readers | mitigate | Exact outer type plus exact positive integers are checked before dereference or I/O; fixed public tests cover every invalid family. |
| T-03-20-BOUND | Denial of Service | high | accepted source bounds | mitigate | Rerun exact accepted and limit+1 tests so validation does not weaken resource ceilings. |
| T-03-20-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_identity.py::test_public_source_readers_reject_malformed_limits tests/test_handoff_identity.py::test_inventory_enforces_item_file_and_total_limit_plus_one -q`
- `uv run pytest tests/test_handoff_identity.py -q`
- `uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- `uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py tests/test_handoff_identity.py`
- `git diff --check`
</verification>

<success_criteria>
- Verification gap 1 limits branch and review WR-01 are closed at both public source reader seams.
- Malformed limits never escape as Python exceptions or begin source/filesystem observation.
- Established exact limits, limit+1 behavior, and structured issue-code precedence remain green.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-20-SUMMARY.md` when done.
</output>
