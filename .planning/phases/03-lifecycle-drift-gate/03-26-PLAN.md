---
phase: 03-lifecycle-drift-gate
plan: 26
type: tdd
wave: 19
depends_on:
  - 03-25
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
  - tests/test_handoff_migration.py
autonomous: true
gap_closure: true
requirements:
  - HND-03
must_haves:
  truths:
    - "A valid SourceIdentityState subclass whose truth value is false remains the exact previous_source_items input to public preview_manifest_migration."
    - "A reintroduced requirement/scenario locator reserved by supplied tombstones returns the existing source-tombstone-identity-collision Failure instead of a successful preview that reuses REQ-000001 and SCN-000001 as created IDs."
    - "The collision result exposes no candidate or apply path, and the manifest target plus complete isolated repository tree remain byte-for-byte unchanged."
    - "Only literal None selects _EMPTY_SOURCE_ITEMS; no new issue code, wrong-runtime-type contract, dependency, schema, persistence protocol, or unrelated refactor is introduced."
    - "Plan 03-26 hands completed behavior to Plan 03-23 for fresh independent review, security audit, and all-26-plan verification without editing current canonical exit artifacts itself."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      provides: "None-only default selection for previous source identity state at public migration preview"
    - path: tests/test_handoff_migration.py
      provides: "Fixed public regression for falsey valid tombstone/counter state and reserved-ID collision"
  key_links:
    - from: tests/test_handoff_migration.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      via: "public preview_manifest_migration call with a validated falsey SourceIdentityState subclass containing requirement/scenario tombstones and counters"
      pattern: "preview_manifest_migration|SourceIdentityState|source-tombstone-identity-collision|__bool__"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
      via: "the exact supplied state is passed to reconcile_source_items, whose existing tombstone collision authority rejects locator reuse"
      pattern: "reconcile_source_items|previous_source_items"
---

<objective>
Close the fresh Phase 3 review blocker by preserving caller-supplied valid previous source identity state regardless of truthiness.

Purpose: Prevent migration preview from discarding tombstones/counters, reusing reserved source IDs, and producing approvable Success evidence from an empty replacement state.
Output: One fixed public-seam RED-to-GREEN regression, one None-only state-selection fix, and complete focused/static/project verification.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
@.agents/skills/self-review/SKILL.md
@.agents/skills/verify-change/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lifecycle-drift-gate/03-REVIEW.md
@.planning/phases/03-lifecycle-drift-gate/03-25-PLAN.md
@.planning/phases/03-lifecycle-drift-gate/03-25-SUMMARY.md
@.planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md
@.planning/phases/03-lifecycle-drift-gate/03-VALIDATION.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@tests/test_handoff_migration.py
@tests/test_handoff_identity.py
@tests/test_handoff_manifest_refresh.py

<interfaces>
The confirmed test seam is public `preview_manifest_migration`. Its typed `previous_source_items` input is `SourceIdentityState | None`: every supplied supported state, including a subclass whose `__bool__` returns `False`, is reconciliation authority; only literal `None` selects `_EMPTY_SOURCE_ITEMS`.

The regression state contains no active items, preserves `next_requirement_id == 2` and `next_scenario_id == 2`, and contains tombstones for `REQ-000001` and `SCN-000001` at the same canonical requirement/scenario locators found in the current source. `validate_source_identity_state` must return `Success` for the subclass before preview is invoked. Passing that exact state to existing `reconcile_source_items` must return `Failure(ClassifiedIssue(IssueCategory.INPUT, "source-tombstone-identity-collision", KnownState.MANIFEST_ABSENT))` before candidate serialization or any apply/persistence boundary.

No new wrong-runtime-type behavior is specified. The production edit changes only the existing default-selection expression; existing reconciliation validation and issue taxonomy remain authoritative.
</interfaces>
</context>

## Artifacts this plan produces

- One narrowly named falsey `SourceIdentityState` test subclass and one public preview regression using exact requirement/scenario tombstones and next counters.
- One `previous_source_items is None` selection expression in `preview_manifest_migration`; no helper or second state authority.
- No canonical OpenSpec, REQUIREMENTS, current `03-REVIEW.md`, completed plan/summary, dependency, schema, issue-code taxonomy, approval/apply, CAS-like persistence, retry, rollback, repair, or route-switch edit.

<source_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every later lifecycle operation can rely on the same fresh, fail-closed drift decision. | 03-26 then 03-23 | COVERED | Migration preview cannot erase identity history before final independent exit evidence. |
| REQ | HND-03 / HARD-R2 | Unsafe or incomplete lifecycle evidence stops progression before an approvable candidate is exposed. | 03-26 then 03-23 | COVERED | The public migration preview preserves prior stable identity state and existing structured failure. |
| RESEARCH | — | Reuse existing public/result/reconciliation authorities, fixed filesystem examples, no new dependency, and focused/static/project gates. | 03-26 | COVERED | One source expression and one migration test file are the only implementation surfaces. |
| CONTEXT | — | No Phase 03 CONTEXT.md exists. | — | EXCLUDED | The approved gap brief supplies the locked scope; no deferred CONTEXT decision exists. |
| REVIEW | CR-01 | Falsey valid previous source state is replaced with empty state, losing tombstones/counters and reusing reserved IDs. | 03-26 | COVERED | Public RED-to-GREEN evidence proves exact existing collision failure and protected-tree preservation. |
| PLANNING BRIEF | UD-01 | Use TDD in Wave 19 after completed Plan 03-25; Plan 03-23 moves to Wave 20. | 03-26 then 03-23 | COVERED | Dependencies are explicit and no completed history is reopened. |
| PLANNING BRIEF | UD-02 | Select `_EMPTY_SOURCE_ITEMS` only for literal None in one production location. | 03-26 | COVERED | No alternate helper, validator, or state projection is added. |
| PLANNING BRIEF | UD-03 | Preserve existing collision taxonomy and do not add wrong-type, dependency, schema, CAS, or refactor scope. | 03-26 | COVERED | The supplied valid subclass exercises the existing typed contract exactly. |
| PLANNING BRIEF | UD-04 | Keep canonical/current/completed artifacts unchanged during implementation and leave clean exit evidence to 03-23. | 03-26 then 03-23 | COVERED | Plan execution owns only migration source/test plus its summary. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve falsey valid previous source state at migration preview</name>
  <read_first>
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/tdd/tests.md
    - .agents/skills/tdd/mocking.md
    - .planning/phases/03-lifecycle-drift-gate/03-REVIEW.md (current CR-01 only; retain the file unchanged)
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py (`_EMPTY_SOURCE_ITEMS`, `preview_manifest_migration`, reconciliation/candidate ordering)
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py (`SourceIdentityState`, `SourceTombstone`, `validate_source_identity_state`, `reconcile_source_items` tombstone collision)
    - tests/test_handoff_migration.py (source constants, `_tree_bytes`, `_write_repository`, `_inputs`, `_preview`, migration preview failure examples)
    - tests/test_handoff_identity.py (`test_reconcile_rejects_reintroduced_tombstone_locator_without_reallocation`)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py, tests/test_handoff_migration.py</files>
  <behavior>
    - RED: public `preview_manifest_migration` receives a `SourceIdentityState` subclass whose `__bool__` returns `False`, whose counters are both 2, and whose requirement/scenario tombstones reserve `REQ-000001` and `SCN-000001`; `validate_source_identity_state` first proves that exact object is valid.
    - RED: under the old fallback, preview returns `Success`, reports the two reserved IDs as `created`, and drops the supplied tombstones because reconciliation receives `_EMPTY_SOURCE_ITEMS`.
    - GREEN: preview returns the existing exact `source-tombstone-identity-collision` INPUT/MANIFEST_ABSENT Failure with no value, while target bytes, complete repository tree, and staging absence remain exact pre-call state.
    - GREEN control: omitted or literal-None previous state retains the existing deterministic empty-state migration preview behavior.
  </behavior>
  <action>RED: import the existing `SourceTombstone` and `validate_source_identity_state` authorities, add a narrowly named `FalseySourceIdentityState` subclass with only `__bool__ -> False`, and add public regression `test_preview_preserves_falsey_previous_source_state_and_rejects_tombstone_reuse`. Construct exact requirement and scenario tombstones for the existing `SOURCE` headings and path, preserve the scenario parent ID, set both next counters to 2, and keep active empty. Assert the validator returns `Success` with the exact supplied object before invoking public preview. Snapshot target bytes and `_tree_bytes(repository)`, call `_preview(previous_source_items=state)`, and assert the existing collision Failure category/code/known-state, absence of a value/candidate, unchanged target/tree, and no staging residue. Run only this node and require RED for the documented old result: `Success`, empty tombstones, and `created` reuse of `REQ-000001` / `SCN-000001`. If it is unexpectedly green or fails for setup/validation reasons, stop and diagnose rather than weakening the public assertions.

GREEN: replace only the previous-state default selection so `_EMPTY_SOURCE_ITEMS` is chosen when `previous_source_items is None`; otherwise pass the exact supplied object to `reconcile_source_items`. Do not add a second validator, new issue code, wrong-type branch, helper abstraction, candidate/apply behavior, or source-identity change. Run the focused node plus the existing deterministic/without-mutation preview controls. Commit RED and GREEN separately under the execute-plan TDD protocol; add no REFACTOR commit unless review identifies a concrete behavior-preserving defect.</action>
  <acceptance_criteria>
    - The exact falsey subclass is accepted by `validate_source_identity_state`, retains counters 2/2 plus both reserved tombstones, and reaches reconciliation through public migration preview.
    - Public preview returns `Failure` with `IssueCategory.INPUT`, code `source-tombstone-identity-collision`, and `KnownState.MANIFEST_ABSENT`; it has no candidate/value and does not report reserved IDs as created.
    - Target bytes, all repository file bytes, and staging absence exactly match the pre-call snapshot.
    - Production selects `_EMPTY_SOURCE_ITEMS` only for literal None and otherwise retains the supplied state; existing omitted/None preview behavior remains green.
    - No new public issue code, wrong-type contract, dependency, schema, persistence, apply, or unrelated production behavior appears.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_migration.py::test_preview_preserves_falsey_previous_source_state_and_rejects_tombstone_reuse -q &amp;&amp; uv run pytest tests/test_handoff_migration.py -q -k "preview and (deterministic or without_mutation or falsey_previous_source_state)"</automated>
  </verify>
  <done>Public migration preview preserves the supplied valid tombstone/counter state regardless of truthiness and rejects reserved identity reuse before exposing candidate or apply evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Verify identity-history preservation and hand off fresh exit review</name>
  <read_first>
    - .agents/skills/self-review/SKILL.md
    - .agents/skills/verify-change/SKILL.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py (the exact Task 1 diff)
    - tests/test_handoff_migration.py (the exact Task 1 diff and complete migration suite)
    - tests/test_handoff_identity.py (`test_reconcile_rejects_reintroduced_tombstone_locator_without_reallocation`)
    - tests/test_handoff_manifest_refresh.py (`test_refresh_preview_lists_created_updated_and_tombstoned_changes`, `test_apply_uses_falsey_supplied_operations_without_default_fallback`)
    - .planning/phases/03-lifecycle-drift-gate/03-23-PLAN.md (fresh all-26-plan exit handoff)
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py, tests/test_handoff_migration.py</files>
  <behavior>
    - Complete migration tests preserve deterministic preview, approval binding, apply failure evidence, shared locking, and existing falsey operations-adapter behavior after the previous-state fix.
    - Source-identity and refresh analogs preserve tombstone collision/no-reallocation semantics, semantic created/updated/tombstoned evidence, and caller-owned falsey adapter boundaries.
    - Static/type/project gates and public behavior verification demonstrate that the change is one state-selection expression plus its regression and that protected authorities remain unchanged.
    - Fresh review/security/verifier conclusions remain pending and are consumed only by Plan 03-23 after this plan completes.
  </behavior>
  <action>Run the focused public regression, the complete migration suite, the source-identity reintroduced-tombstone collision node, the refresh created/updated/tombstoned preview node, and the refresh falsey-adapter analog. Run targeted Ruff and BasedPyright for the two owned implementation/test files, then `task check` and `git diff --check`.

Apply `self-review` to both the uncommitted/staged diff and the phase base diff. Confirm the production change is exactly one None-only state-selection expression; the test proves public behavior with an independently valid expected result; no truthiness fallback, reserved-ID reuse, new issue taxonomy, wrong-type contract, helper, dependency, schema, approval/apply/persistence change, or unrelated refactor appears. Fix only obvious defects and rerun all affected checks; report judgment calls without changing scope.

Apply `verify-change`: rerun the focused node individually and inspect its public `Failure` shape plus target/tree/staging preservation, then run the complete test and project gates. Record RED/GREEN commits, exact collision evidence, analog results, static/type/project results, self-review findings, public behavior verification, and any unverified item in `03-26-SUMMARY.md`. Preserve canonical OpenSpec, REQUIREMENTS, current `03-REVIEW.md`, completed plan/summary artifacts, and OpenSpec task 3.1 unchanged. Do not claim clean review/security/verifier evidence, advance HND-03, complete Phase 3, or unblock Phase 4; hand those decisions to Plan 03-23.</action>
  <acceptance_criteria>
    - The focused node and complete migration suite pass, including existing operations-adapter and persistence guards.
    - The named source-identity and refresh analogs pass without changing their production authorities or property-test scope.
    - Targeted Ruff and BasedPyright, `task check`, and `git diff --check` pass.
    - Self-review finds no unresolved obvious defect or scope creep; public behavior verification observes exact collision Failure and protected filesystem state through `preview_manifest_migration`.
    - `03-26-SUMMARY.md` records separate RED/GREEN evidence and complete verification, while canonical/current/completed authority artifacts remain unchanged and 03-23 retains fresh exit ownership.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_migration.py::test_preview_preserves_falsey_previous_source_state_and_rejects_tombstone_reuse -q &amp;&amp; uv run pytest tests/test_handoff_migration.py -q &amp;&amp; uv run pytest tests/test_handoff_identity.py::test_reconcile_rejects_reintroduced_tombstone_locator_without_reallocation tests/test_handoff_manifest_refresh.py::test_refresh_preview_lists_created_updated_and_tombstoned_changes tests/test_handoff_manifest_refresh.py::test_apply_uses_falsey_supplied_operations_without_default_fallback -q &amp;&amp; uv run ruff check src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py tests/test_handoff_migration.py &amp;&amp; uv run basedpyright src/ai_coding_template_ja/openspec_gsd_handoff/manifest_migration.py tests/test_handoff_migration.py &amp;&amp; task check &amp;&amp; git diff --check</automated>
  </verify>
  <done>The falsey previous-state regression and all identity/refresh/static/project backstops are green, protected authorities are unchanged, and fresh all-26-plan exit evidence is ready for Plan 03-23.</done>
</task>

</tasks>

<threat_model>
ASVS baseline: Level 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller-supplied source state → migration preview | A supported prior identity state carries reserved IDs and must not be replaced because its truth value is false. |
| previous source state → reconciliation | Exact counters, active items, and tombstones determine whether a locator may allocate, update, or fail as reserved. |
| reconciliation failure → candidate/apply boundary | A tombstone collision must stop before an approvable candidate or filesystem mutation evidence exists. |
| completed gap → fresh exit roles | 03-26 proves behavior but cannot author its own clean review, security audit, verifier result, or HND-03 completion. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-03-26-STATE | Spoofing/Tampering | high | `previous_source_items` selection | mitigate | Select the empty default only for None and validate/exercise an exact falsey supported state through public preview. |
| T-03-26-REUSE | Tampering/Elevation of Privilege | high | source ID reconciliation | mitigate | Preserve tombstones/counters and require existing collision Failure rather than created reuse of reserved requirement/scenario IDs. |
| T-03-26-WRITE | Tampering | high | candidate/apply/persistence boundary | mitigate | Require Failure without value plus exact target/tree/staging preservation before any candidate or apply effect. |
| T-03-26-CONTRACT | Repudiation | medium | typed previous-state contract | accept | The typed public contract remains SourceIdentityState-or-None and existing reconciliation owns runtime validation/taxonomy; no new branch or issue code is added. |
| T-03-26-REVIEW | Repudiation | high | Phase 3 exit evidence | mitigate | Preserve the issues_found review and require fresh Plan 03-23 review/security/verifier coverage after 03-26. |
| T-03-26-SC | Tampering | low | package supply chain | accept | No package install or dependency change occurs. |
</threat_model>

<verification>
- RED must fail on the old empty-state substitution with observable successful reserved-ID reuse, not on malformed state setup or a different exception.
- GREEN must preserve the exact validated falsey state into reconciliation and return the existing structured tombstone collision Failure.
- Public behavior evidence must prove no candidate/value, exact target/tree bytes, and no staging residue.
- Regression evidence must include the complete migration suite and named source-identity/refresh analogs.
- Static/project evidence must include targeted Ruff, targeted BasedPyright, `task check`, self-review, public behavior verification, and `git diff --check`.
- Authority integrity must preserve canonical OpenSpec, REQUIREMENTS, current `03-REVIEW.md`, completed plan/summary history, and OpenSpec task 3.1 during execution.
- Plan 03-23 remains the only owner of later clean review, zero-open-threat security, Complete HND-03 traceability, and passed all-26-plan verification.
</verification>

<success_criteria>
- Current CR-01 is closed through a genuine public RED then GREEN fixed regression.
- The implementation uses explicit None selection at the single previous-state fallback and retains every supplied valid state regardless of truthiness.
- Existing `source-tombstone-identity-collision` evidence stops reserved-ID reuse without a new wrong-type contract, issue code, dependency, schema, CAS-like persistence, or unrelated refactor.
- Focused, complete migration, source-identity/refresh analog, static, project, diff, self-review, and public behavior checks pass.
- After execution, Phase 3 remains incomplete at 25/26 executed until Plan 03-23 produces fresh independent exit evidence across all 26 plans.
</success_criteria>

<output>
Create `.planning/phases/03-lifecycle-drift-gate/03-26-SUMMARY.md` when done, including RED old-success/reserved-ID-reuse evidence, GREEN exact collision/preservation evidence, no-new-contract rationale, full verification, self-review/public behavior results, protected-authority integrity, and handoff to Plan 03-23.
</output>
