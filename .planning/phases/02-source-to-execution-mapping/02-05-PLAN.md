---
phase: 02-source-to-execution-mapping
plan: 05
type: tdd
wave: 5
depends_on:
  - 02-04
files_modified:
  - tests/test_handoff_manifest_refresh.py
  - tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
  - .planning/phases/02-source-to-execution-mapping/02-REFRESH-PREVIEW.json
autonomous: true
requirements:
  - HND-02
must_haves:
  truths:
    - "D-01/D-03: the current canonical pin 4d8b5b173927ed518d39dee18a29b0271628afbd produces the exact 42-to-49 reconciliation, 49-of-49 mapping, and current-tree artifact/source fingerprints without changing assignments or policy references."
    - "D-02: the tracked started-v2 manifest remains SHA-256 554690a1eee6e632eaf7c4fce3517cba69ff38eb8a06a1873b7a5e6822e59914 while a complete new read-only refresh preview is regenerated only through preview_manifest_refresh."
    - "D-04: readiness is treated as a point-in-time observation; the existing public-seam test proves that a subsequent readiness call after external evidence removal returns non-ready."
    - "The two stale focused failures become green without production, public API, or CLI changes, and no new property family or atomic-snapshot test is introduced."
    - "OpenSpec task 2.2, the tracked target, canonical artifacts, and historical Plans/Summaries 02-01 through 02-04 remain unchanged."
  artifacts:
    - path: tests/test_handoff_manifest_refresh.py
      provides: "Current canonical SOURCE_COMMIT and exact fixed-candidate/read-only evidence assertions"
    - path: tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
      provides: "Independently reviewable current-pin golden candidate, bytes, hashes, and fingerprints"
    - path: .planning/phases/02-source-to-execution-mapping/02-REFRESH-PREVIEW.json
      provides: "Complete current-pin tracked read-only preview evidence for later display and separate approval"
  key_links:
    - from: tests/test_handoff_manifest_refresh.py
      to: openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
      via: "SOURCE_COMMIT plus exact canonical artifact hashes and source fingerprints"
      pattern: "SOURCE_COMMIT|updated_fingerprints"
    - from: tests/test_handoff_manifest_refresh.py
      to: tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
      via: "literal old-target, reconciliation, candidate value/bytes, and candidate hash comparisons"
      pattern: "EXPECTED\\[|candidate_sha256|candidate_bytes_utf8"
    - from: .planning/phases/02-source-to-execution-mapping/02-REFRESH-PREVIEW.json
      to: .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
      via: "read-only target observation and immutable preview identity with empty mutation/staging evidence"
      pattern: "read-only-preview-only|mutation_operations|preview_sha256"
---

<objective>
Correct the stale Phase 2 golden/evidence records against canonical source authority `4d8b5b173927ed518d39dee18a29b0271628afbd` without changing production readiness behavior.

Purpose: turn the already-observed two focused stale-evidence failures into exact current-pin evidence while preserving D-01 through D-04, the tracked started-v2 preimage, and the separate fresh-approval boundary.
Output: a repinned fixed-candidate test, an independently reviewable exact golden fixture, a completely regenerated tracked read-only preview, and executor-authored `02-05-SUMMARY.md`.
No new public symbols are produced.
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
@.agents/skills/tdd/SKILL.md
@.agents/skills/verify-change/SKILL.md
@.agents/skills/self-review/SKILL.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-source-to-execution-mapping/02-CONTEXT.md
@.planning/phases/02-source-to-execution-mapping/02-RESEARCH.md
@.planning/phases/02-source-to-execution-mapping/02-PATTERNS.md
@.planning/phases/02-source-to-execution-mapping/02-REVIEW.md
@.planning/phases/02-source-to-execution-mapping/02-REVIEW-FIX.md
@.planning/phases/02-source-to-execution-mapping/02-04-PLAN.md
@.planning/phases/02-source-to-execution-mapping/02-04-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@tests/test_handoff_manifest_refresh.py
@tests/test_handoff_execution_mapping.py
@tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
@.planning/phases/02-source-to-execution-mapping/02-REFRESH-PREVIEW.json

<interfaces>
Existing public seam used for tracked evidence:
`preview_manifest_refresh(repository_root, target_path, *, current_source_commit, current_artifacts, current_progress, source_paths, planning_inventory, policy_registry, explicit_matches=(), limits=DEFAULT_REFRESH_LIMITS, operations=None) -> Result[ManifestRefreshPreview]`.

Existing public seam for D-04 observation reuse:
`validate_mapping_readiness(repository_root, source_items, mappings, planning_inventory, *, operation, target_phase_id=None) -> Result[MappingReadiness]`.

Existing fixed example `tests/test_handoff_execution_mapping.py::test_verify_requires_every_selected_source_and_plan_evidence` first observes ready, removes one declared evidence path externally, calls `validate_mapping_readiness` again, and requires `ready=False` with `MappingIssue("mapping-path-missing", ...)`. Cite and run it; do not duplicate it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repin the exact fixed candidate and reuse the point-in-time readiness example</name>
  <read_first>
    - .planning/phases/02-source-to-execution-mapping/02-CONTEXT.md
    - .planning/phases/02-source-to-execution-mapping/02-PATTERNS.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - tests/test_handoff_manifest_refresh.py
    - tests/test_handoff_execution_mapping.py
    - tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
  </read_first>
  <files>tests/test_handoff_manifest_refresh.py, tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json</files>
  <behavior>
    - RED evidence is the existing `uv run pytest tests/test_handoff_manifest_refresh.py -q` result: exactly 2 failed and 34 passed, with failures limited to repository-root evidence equality and `REQ-000001` candidate fingerprint equality.
    - GREEN fixed-candidate evidence uses source commit `4d8b5b173927ed518d39dee18a29b0271628afbd`, design SHA-256 `3561792edfe750f5815fad72ff2e133888848b2733e770e2b6f66f87c413e783`, spec SHA-256 `7d076d2a946a8e8f3346f48ae80d4fbeb8ae0fb9ea6d20ccf19e01847edfd784`, `REQ-000001` candidate fingerprint `01f6b1f8dd9985a6f6fac5648358e3e7d01bd26da56a592d610cbb9ca51505bc`, and unchanged `SCN-000018` candidate fingerprint `46843242f7ab1f230919ca8ff5915859cd6aee9e243714ec63d3aa9ae4a2c025`.
    - GREEN candidate evidence has candidate SHA-256 `6cc9bcf4caa3f9f839742f6d86660a8039c2370cf5cf7d054ba04199e3775fc5` and exact candidate object/UTF-8 bytes with source commit `4d8b5b173927ed518d39dee18a29b0271628afbd`.
    - GREEN D-01 reconciliation remains exactly 42 previous active, 49 candidate active, 7 created, 2 updated, 0 tombstoned, counters 7/44, and 49/49 mappings; OpenSpec progress remains 4/12 with task 2.2 false.
    - GREEN D-04 evidence is the existing `test_verify_requires_every_selected_source_and_plan_evidence`; no adversarial atomic-snapshot test or new property family is added.
  </behavior>
  <action>Preserve the existing two-failure run as RED evidence. Change only the `SOURCE_COMMIT` constant and exact current canonical expectations in `tests/test_handoff_manifest_refresh.py`. Update the complete literal golden in `expected-refresh-preview.json` for the new source pin: current design/spec hashes, `REQ-000001` fingerprint, candidate manifest source commit, candidate object, exact candidate UTF-8 bytes, candidate hash, and preview identity fields that derive from those values. Keep the target preimage, proposal/tasks hashes, progress checkbox states, created/updated IDs, counters, 49 mappings, policy references, capabilities, ownership, and lifecycle unchanged per D-01/D-03. Use the public preview result to obtain the candidate for comparison, but review the checked-in golden as an independent literal rather than weakening assertions or deriving expected values inside the test. Run the existing D-04 missing-path example and cite it in `02-05-SUMMARY.md`; do not add a duplicate concurrency/atomicity test. If the public reconciliation seam reports anything other than the locked 42/49/7/2/0, counters 7/44, and 49/49 mapping result, stop and report the mismatch instead of changing expectations.</action>
  <acceptance_criteria>
    - `SOURCE_COMMIT` and every changed golden field are traceable to `4d8b5b173927ed518d39dee18a29b0271628afbd`.
    - The pinned fixed-candidate example passes with exact literal candidate bytes/hash and the existing D-04 subsequent-readiness example passes unchanged.
    - No production file, mapping assignment fixture, policy registry, canonical OpenSpec artifact, or historical plan/summary is modified.
    - OpenSpec task 2.2 remains unchecked and no new property test or public symbol exists.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_refresh.py::test_pinned_started_v2_builds_exact_complete_read_only_candidate tests/test_handoff_execution_mapping.py::test_verify_requires_every_selected_source_and_plan_evidence -q</automated>
  </verify>
  <done>The current-pin fixed candidate is exact and independently reviewable, while the already-existing public seam proves that external drift is rejected by the next readiness observation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate the complete tracked read-only preview and run all boundary gates</name>
  <read_first>
    - .planning/phases/02-source-to-execution-mapping/02-CONTEXT.md
    - .planning/phases/02-source-to-execution-mapping/02-PATTERNS.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_refresh.py
    - tests/test_handoff_manifest_refresh.py
    - tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json
    - docs/agents/adaptive-change-execution.references.json
    - tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
    - .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md
    - .planning/phases/02-source-to-execution-mapping/02-REFRESH-PREVIEW.json
  </read_first>
  <files>.planning/phases/02-source-to-execution-mapping/02-REFRESH-PREVIEW.json</files>
  <behavior>
    - GREEN evidence is rebuilt as one complete deterministic unit by the existing `_repository_root_evidence` producer, whose only lifecycle operation is public `preview_manifest_refresh` with `MutationRecordingRefreshOperations`.
    - GREEN evidence has source commit `4d8b5b173927ed518d39dee18a29b0271628afbd`, candidate SHA-256 `6cc9bcf4caa3f9f839742f6d86660a8039c2370cf5cf7d054ba04199e3775fc5`, preview SHA-256 `90b52efd98d6718796548151ea9c808dfd1e14484bcacd2f847b09ea71054bea`, and complete evidence-file SHA-256 `661b63be39bacb882c53ade5e9919ae7fea661f852b7e47fb53188a29348138a`.
    - GREEN protected evidence keeps target SHA-256 `554690a1eee6e632eaf7c4fce3517cba69ff38eb8a06a1873b7a5e6822e59914`, tasks SHA-256 `cf4a9dc56afc15b98a008cff686989bd446215c95b3962ea3efd5a4f9eb30220`, `apply_invoked=false`, `mutation_operations=[]`, and empty staging observations before and after.
    - GREEN reconciliation and mapping evidence remains exactly 42/49/7/2/0, counters 7/44, and active/mapped 49/49.
    - GREEN CLI remains exactly `inspect`, `prepare`, and `mark-started`; focused Phase 2, Phase 1/v1 regression, strict OpenSpec, project OpenSpec, and full project checks all pass.
  </behavior>
  <action>Before generation, prove the tracked target/tasks hashes and absence of staging files. Load the existing test evidence producer with `runpy.run_path("tests/test_handoff_manifest_refresh.py")`, call `_repository_root_evidence()` exactly once, and replace the entire `02-REFRESH-PREVIEW.json` with those returned bytes; never patch selected hashes into the stale file. That producer must reach the tracked repository only through public `preview_manifest_refresh`, record zero mutation operations, strict-serialize the full machine preview, and recheck target/tasks/staging invariants. Do not invoke the refresh apply seam for the tracked target under any name or approval value. Strict-reparse the evidence and embedded candidate, verify the exact hashes/counts above, then rerun the producer and require byte equality. If any public-seam value differs from the locked reconciliation/mapping contract, stop without inferring a replacement expectation. Run every command in `<verification>`, perform self-review of the three-file corrective diff, and write `02-05-SUMMARY.md` with RED/GREEN evidence, exact hashes/counts, commands/results, the reused D-04 test citation, and all protected-surface proofs. The summary must state that actual tracked apply, OpenSpec task 2.2, fresh code review, independent phase verification, and the phase-verification claim are not executor outputs. Record that the complete new preview must be displayed later and receive a distinct new explicit user approval naming its exact preview hash before any tracked apply can be considered.</action>
  <acceptance_criteria>
    - The tracked preview file is the exact complete output of the public read-only preview path and has SHA-256 `661b63be39bacb882c53ade5e9919ae7fea661f852b7e47fb53188a29348138a`.
    - The tracked target and OpenSpec tasks are byte-identical, no staging file exists, and mutation operations remain empty.
    - All focused, regression, strict OpenSpec, project, CLI, and protected-surface gates pass without production/API/CLI changes.
    - `02-05-SUMMARY.md` reports execution evidence but does not check task 2.2, claim Phase 2 verification, or authorize/apply the preview.
    - Fresh code review and independent Phase 2 verification remain explicit orchestrator gates after execution.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_handoff_manifest_refresh.py tests/test_handoff_execution_mapping.py tests/test_handoff_policy_reference.py -q</automated>
  </verify>
  <done>The exact current-pin preview is regenerated read-only, the full required validation matrix is green, and the tracked apply boundary remains unexecuted pending later display and distinct approval.</done>
</task>

</tasks>

<prohibitions>
- Do not modify production source, package exports, `__main__.py`, or the public CLI.
- Do not call `apply_manifest_refresh` for `.planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json`; do not create, stage, replace, or clean up a tracked-target candidate.
- Do not change the tracked target, `openspec/.../tasks.md`, task 2.2, canonical OpenSpec artifacts, assignment/policy fixtures, ROADMAP, STATE, REVIEW, VERIFICATION, or Plans/Summaries 02-01 through 02-04.
- Do not add an atomic-snapshot/adversarial concurrency test, a new property family, retry, rollback, repair, route switching, or source implementation changes.
- Do not run a schema push/apply command or install/update dependencies.
- Do not treat plan execution, preview generation, or a prior approval as Phase 2 verification or mutation authorization.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Risk |
|---|---|
| canonical source pin → golden/evidence | A stale or substituted source pin can make review evidence describe the wrong source. |
| preview producer → tracked target | A read-only correction could accidentally invoke mutation or leave staging. |
| preview identity → later approval | A preview/target substitution could reuse approval for different bytes or repository state. |
| corrective diff → protected artifacts | Broad regeneration could mutate OpenSpec tasks, historical evidence, production, or the tracked manifest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-13 | Spoofing/Tampering | `SOURCE_COMMIT`, canonical hashes, golden fixture | mitigate | Pin `4d8b5b1...`, keep literal independent expectations, and fail on any reconciliation/count mismatch. |
| T-02-14 | Elevation of Privilege/Tampering | tracked refresh boundary | mitigate | Use `preview_manifest_refresh` only, record empty mutation operations/staging, and prove target/tasks byte invariance. |
| T-02-15 | Tampering/Repudiation | preview hash, target path, repository root | mitigate | Regenerate the complete approval-bound unit, strict-reparse candidate/evidence, and require a later distinct approval for the displayed exact preview. |
| T-02-16 | Tampering | production, canonical, task, historical planning artifacts | mitigate | Three-file allowlist plus protected `git diff`/SHA gates; no schema push applies and no dependency install occurs. |
| T-02-SC | Tampering | package supply chain | accept | No package-manager install or dependency change is required. |
</threat_model>

<verification>
1. Preserve RED evidence: `uv run pytest tests/test_handoff_manifest_refresh.py -q` reports exactly 2 failed and 34 passed before edits.
2. Run Phase 2 focused verification: `uv run pytest tests/test_handoff_manifest_refresh.py tests/test_handoff_execution_mapping.py tests/test_handoff_policy_reference.py -q`.
3. Run Phase 1/v1 regression: `uv run pytest tests/test_handoff_identity.py tests/test_handoff_manifest_v2.py tests/test_handoff_migration.py tests/test_handoff_manifest.py tests/test_handoff_cli.py -q`.
4. Run the exact CLI surface example and direct help: `uv run pytest tests/test_handoff_cli.py::test_module_help_exposes_exact_operations -q` and `uv run python -m ai_coding_template_ja.openspec_gsd_handoff --help`; only `inspect`, `prepare`, and `mark-started` are exposed.
5. Run strict canonical validation: `openspec validate harden-openspec-gsd-handoff-lifecycle --strict`.
6. Run project OpenSpec validation: `task openspec:validate`.
7. Run full project validation: `task check`.
8. Prove protected hashes with `sha256sum`: tracked handoff is `554690a1eee6e632eaf7c4fce3517cba69ff38eb8a06a1873b7a5e6822e59914`, OpenSpec tasks is `cf4a9dc56afc15b98a008cff686989bd446215c95b3962ea3efd5a4f9eb30220`, design is `3561792edfe750f5815fad72ff2e133888848b2733e770e2b6f66f87c413e783`, and spec is `7d076d2a946a8e8f3346f48ae80d4fbeb8ae0fb9ea6d20ccf19e01847edfd784`.
9. Run `git diff --check` and `git diff --exit-code --` for production modules, package/CLI surfaces, tracked handoff, OpenSpec tasks/canonical artifacts, mapping/policy fixtures, ROADMAP, STATE, REVIEW, historical Plans/Summaries 02-01 through 02-04; only the three corrective files and executor-created `02-05-SUMMARY.md` may differ.
10. Confirm no `.handoff.*.tmp` exists beside the tracked target and strict JSON/candidate reparse succeeds.
</verification>

<success_criteria>
- HND-02 corrective evidence is exact at source authority `4d8b5b173927ed518d39dee18a29b0271628afbd`.
- D-01 mapping/reconciliation, D-02 separate approval-bound refresh, D-03 exact current-tree fingerprints, and D-04 point-in-time readiness semantics are preserved without production changes.
- The two stale focused failures are green and every required regression/validation/protected-surface gate passes.
- Actual tracked apply and OpenSpec task 2.2 remain unexecuted; fresh review and independent verifier remain downstream gates.
- The new complete preview is not approval. It must be displayed later and receive a distinct new explicit approval before tracked mutation.
</success_criteria>

## Artifacts this phase produces

| Artifact | Role |
|---|---|
| `tests/test_handoff_manifest_refresh.py` | Repinned exact current-canonical test inputs and assertions; no public symbol added. |
| `tests/fixtures/openspec_gsd_handoff/manifest/expected-refresh-preview.json` | Exact independently reviewable current-pin golden. |
| `.planning/phases/02-source-to-execution-mapping/02-REFRESH-PREVIEW.json` | Complete regenerated read-only preview evidence for later display/approval. |
| `.planning/phases/02-source-to-execution-mapping/02-05-SUMMARY.md` | Executor evidence handoff; explicitly not phase verification or mutation approval. |

No new production module, API, CLI operation, schema, or public symbol is produced.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 2 | Complete reviewable source-to-phase, plan, and evidence mapping | 02-01..02-05 | COVERED | 02-05 corrects current-pin derived evidence without rewriting historical execution. |
| REQ | HND-02 / HARD-R1 | Opaque Phase 2 source-to-execution mapping handle | 02-05 | COVERED | Exact golden, read-only preview, and validation gates close the stale-evidence gap. |
| RESEARCH | current source repin | `4d8b5b1...` exact golden/evidence refresh, production unchanged | 02-05 Task 1-2 | COVERED | Stops on reconciliation/count mismatch. |
| RESEARCH | validation architecture | focused Phase 2, Phase 1/v1, strict OpenSpec, project and protected-surface gates | 02-05 Task 2 | COVERED | Fresh review/verifier remain downstream. |
| CONTEXT | D-01 | full 49-ID assignment and operation-specific readiness | 02-05 Task 1-2 | COVERED | Assignment fixture unchanged; exact 49/49 evidence retained. |
| CONTEXT | D-02 | separate started-v2 refresh and distinct approval | 02-05 Task 2 | COVERED | Preview only; tracked apply is unreachable. |
| CONTEXT | D-03 | exact current-tree policy/source fingerprints | 02-05 Task 1-2 | COVERED | Golden and preview bind current canonical hashes without policy registry changes. |
| CONTEXT | D-04 | point-in-time readiness observation | 02-05 Task 1 | COVERED | Reuses the existing subsequent-call missing-path public-seam test; no atomic-snapshot test added. |

Deferred Phase 3-6 behavior, automatic recovery, mandatory real-tool smoke, and tracked apply are explicit exclusions rather than gaps.

<output>
Create `.planning/phases/02-source-to-execution-mapping/02-05-SUMMARY.md`. Include the baseline 2-failed/34-passed RED evidence, exact current-pin candidate/preview/evidence hashes, reconciliation and 49/49 coverage, the cited existing D-04 public-seam example, every validation command/result, protected byte/diff/staging evidence, and a prominent unverified boundary: no tracked apply, no OpenSpec task 2.2 update, no phase-verification claim, and fresh code review plus independent verifier still required. State that the complete preview must be displayed later and receive a distinct new explicit user approval before any tracked apply.
</output>
