---
phase: 02-source-to-execution-mapping
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
  - tests/test_handoff_policy_reference.py
  - tests/fixtures/openspec_gsd_handoff/policy/valid-policy.md
  - tests/fixtures/openspec_gsd_handoff/policy/duplicate-heading.md
  - tests/fixtures/openspec_gsd_handoff/policy/unclosed-fence.md
  - docs/agents/adaptive-change-execution.references.json
autonomous: true
requirements:
  - HND-02
must_haves:
  truths:
    - "D-03: current-tree policy anchors are observed with the exact adaptive-policy-section-v1 contract and no historical Git dependency."
    - "A complete strict registry validates unique stable IDs, canonical paths, exact headings, and observed fingerprints before any mapping can consume it."
    - "Malformed, duplicate, missing, escaped, aliased, symlinked, fenced, or over-limit policy input returns structured whole-operation non-success."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
      provides: "Immutable policy records, bounded section observations, strict registry codec, and current-tree validation"
    - path: docs/agents/adaptive-change-execution.references.json
      provides: "Mechanical stable IDs, current-tree anchors, hashes, and non-normative provenance without copied policy prose"
    - path: tests/test_handoff_policy_reference.py
      provides: "Fixed public-seam examples for exact policy observation and registry rejection"
  key_links:
    - from: docs/agents/adaptive-change-execution.references.json
      to: docs/agents/workflow.md
      via: "canonical path, normalized ATX heading, and adaptive-policy-section-v1 fingerprint"
      pattern: "adaptive-policy-section-v1"
    - from: tests/test_handoff_policy_reference.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
      via: "public read/observe/validate operations"
      pattern: "read_policy_reference_registry|observe_policy_sections|validate_policy_references"
---

<objective>
D-03に従い、current treeだけで検査できるpolicy reference registryとexact section observerをTDDで作る。

Purpose: HND-02 mappingがpolicy本文や到達不能なGit blobを複製せず、stable IDから現在のcanonical anchorへfail-closedに到達できるようにする。
Output: `policy_reference.py`、固定adversarial fixtures、current-tree reference record、focused tests。
Source authority: canonical OpenSpec artifacts pinned at `fbe7f714f734d714480583ab90f41ec0d2077f50`。
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@docs/agents/workflow.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-source-to-execution-mapping/02-CONTEXT.md
@.planning/phases/02-source-to-execution-mapping/02-RESEARCH.md
@.planning/phases/02-source-to-execution-mapping/02-VALIDATION.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/models.py
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py

<interfaces>
Use `Result[T] = Success[T] | Failure` and immutable values. Create module-level `read_policy_reference_registry(repository_root, registry_path, *, limits=...)`, `observe_policy_sections(repository_root, registry, *, limits=...)`, and `validate_policy_references(registry, observations, referenced_ids)`. The section fingerprint frames the version tag, canonical path, normalized heading, and normalized body with unsigned 8-byte big-endian lengths before SHA-256.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Observe exact bounded policy sections from fixed Markdown examples</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - .planning/phases/02-source-to-execution-mapping/02-CONTEXT.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/models.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py, tests/test_handoff_policy_reference.py, tests/fixtures/openspec_gsd_handoff/policy/valid-policy.md, tests/fixtures/openspec_gsd_handoff/policy/duplicate-heading.md, tests/fixtures/openspec_gsd_handoff/policy/unclosed-fence.md</files>
  <behavior>
    - RED: an independently calculated fixed example yields exact raw/canonical path, normalized heading/body, body byte length, lowercase section SHA-256, and exactly one terminal LF.
    - RED: CRLF/CR/LF and NFC-equivalent forms preserve the observation; only line-end horizontal whitespace is removed, while leading indentation and internal/initial blank lines remain.
    - RED: duplicate normalized headings, unclosed fences, invalid UTF-8, missing headings, path traversal, symlink escape, Unicode/case alias, non-regular files, and limit+1 input return `Failure` with no partial observations.
    - GREEN: headings inside closed fences do not open or terminate a section; the next fence-external heading of the same or higher level terminates it.
  </behavior>
  <action>Per D-03, implement the exact `adaptive-policy-section-v1` scanner as a separate policy seam. Reuse only small Phase-1 mechanical predicates when their contracts are byte-identical; do not expose or test private helpers. Read each file once with limit+1, validate lexical components before anchored no-follow access, and return a complete immutable observation tuple or one structured failure. Do not use broad whitespace stripping, Git history, OpenSpec/GSD tools, or a property suite.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_policy_reference.py -q</automated>
  </verify>
  <done>The fixed examples prove exact section bytes/fingerprints and every incomplete observation fails as a whole operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate the strict current-tree reference registry</name>
  <read_first>
    - src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
    - tests/test_handoff_policy_reference.py
    - docs/agents/workflow.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py, tests/test_handoff_policy_reference.py, docs/agents/adaptive-change-execution.references.json</files>
  <behavior>
    - RED: the design-fixed ACE stable namespace parses as exact bounded records, has unique IDs, and every referenced ID resolves to one observed current-tree anchor with matching path, heading, body length, and SHA-256.
    - RED: unknown fields, wrong primitive types/version, duplicate IDs, duplicate/aliased anchors, unknown referenced IDs, missing current-tree anchors, or hash/length mismatch return structured non-success.
    - GREEN: historical provenance is retained only as non-normative metadata and removing Git history or `docs/template/` does not change validation of the required `docs/agents/workflow.md` anchors.
  </behavior>
  <action>Write RED registry/validation examples first, then implement frozen `PolicyReference`, `PolicyReferenceRegistry`, and strict JSON decoding with duplicate-key rejection, scalar validation, deterministic ordering, 4096-record and 8 MiB bounds. Add the complete stable ID namespace and exact current-tree anchors fixed by canonical Gate C; store only mechanical identifiers/anchors/hashes and no policy requirement or scenario prose. D-01 mappings will consume IDs only in Plan 02, while D-02 refresh remains untouched here.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_policy_reference.py tests/test_handoff_identity.py -q</automated>
  </verify>
  <done>The tracked registry validates entirely against current-tree bytes and rejects every incomplete or ambiguous reference set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| registry JSON → stable references | Malformed or duplicate records can redirect mapping evidence. |
| repository path/Markdown → section observation | Alias, symlink, encoding, fence, and size ambiguity can spoof a canonical anchor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Spoofing/Tampering | policy registry and paths | mitigate | Exact fields, unique IDs/anchors, NFC/case alias rejection, anchored no-follow reads, and current-tree hash comparison. |
| T-02-02 | Tampering | section normalizer | mitigate | Versioned framing, strict heading/fence grammar, independent literals, and whole-operation failure. |
| T-02-03 | Denial of Service | registry/section reads | mitigate | 8 MiB limit+1 and 4096-record bounds without truncated success. |
| T-02-SC | Tampering | package supply chain | accept | No package install or dependency update occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_policy_reference.py tests/test_handoff_identity.py -q`
- `uv run pytest tests/test_handoff_manifest_v2.py tests/test_handoff_migration.py -q`
- `git diff --check`
- Confirm `git diff -- openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json` is empty.
</verification>

<success_criteria>
- D-03 is implemented exactly with fixed examples, not a new property family.
- Normal CI needs neither historical Git blobs nor optional OpenSpec/GSD tools.
- Root package exports and the `inspect` / `prepare` / `mark-started` CLI remain unchanged.
- Canonical OpenSpec tasks and the tracked handoff manifest remain byte-identical.
</success_criteria>

<output>
Create `.planning/phases/02-source-to-execution-mapping/02-01-SUMMARY.md` with RED/GREEN commits, registry schema, fixed fingerprints, tests, and unchanged-manifest evidence.
</output>
