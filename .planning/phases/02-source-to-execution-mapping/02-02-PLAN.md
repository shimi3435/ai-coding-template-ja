---
phase: 02-source-to-execution-mapping
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
  - tests/test_handoff_execution_mapping.py
  - tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
autonomous: true
requirements:
  - HND-02
must_haves:
  truths:
    - "D-01: every active source ID receives exactly one caller-declared phase assignment before operation readiness is evaluated."
    - "Plan, execute, verify, and finalize evaluate only their exact bounded horizon; empty future plan/evidence inventories never satisfy an active horizon."
    - "The fixed change inventory covers the expected 49 active IDs without heuristic inference, tombstone references, cross-change paths, duplicates, or phase/path conflicts."
  artifacts:
    - path: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      provides: "Explicit planning inventory, deterministic ManifestMapping builder, operation enum, and structured readiness result"
    - path: tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
      provides: "Reviewable exact 49-ID source-to-phase declarations for the pinned change"
    - path: tests/test_handoff_execution_mapping.py
      provides: "Fixed baseline and all four operation-horizon examples"
  key_links:
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
      via: "validated explicit declarations converted to exact ManifestMapping values"
      pattern: "ManifestMapping"
    - from: src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
      to: src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
      via: "mapping policy IDs resolved against the complete validated registry"
      pattern: "validate_policy_references"
---

<objective>
D-01に従い、明示的でboundedなplanning inventoryからcomplete mapping baselineを作り、operationごとのreadinessを固定例で検査する。

Purpose: HND-02のsource-to-phase / plan / evidence対応を、headingやproseから推測せず、同じchangeに属するcanonical declarationsとしてreview可能にする。
Output: `execution_mapping.py`、49-ID assignment fixture、fixed positive/negative readiness tests。
Source authority: canonical OpenSpec artifacts pinned at `fbe7f714f734d714480583ab90f41ec0d2077f50`。
</objective>

<execution_context>
@/home/shimi3435/.codex/gsd-core/workflows/execute-plan.md
@/home/shimi3435/.codex/gsd-core/templates/summary.md
@/home/shimi3435/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-source-to-execution-mapping/02-CONTEXT.md
@.planning/phases/02-source-to-execution-mapping/02-RESEARCH.md
@.planning/phases/02-source-to-execution-mapping/02-VALIDATION.md
@.planning/phases/02-source-to-execution-mapping/02-01-SUMMARY.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
@openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
@src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
@src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
@src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py

<interfaces>
Create immutable `MappingOperation` (`plan`, `execute`, `verify`, `finalize`), `PhaseAssignment`, bounded phase/plan/evidence declarations, `PlanningInventory`, `MappingIssue`, and `MappingReadiness`. Expose `build_manifest_mappings(source_items, planning_inventory, policy_registry)` and `validate_mapping_readiness(repository_root, source_items, mappings, planning_inventory, *, operation, target_phase_id=None)`. Both return whole-operation `Success` or `Failure`/structured non-success and never infer declarations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build one complete explicit mapping baseline</name>
  <read_first>
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/design.md
    - openspec/changes/harden-openspec-gsd-handoff-lifecycle/specs/openspec-gsd-handoff-lifecycle-hardening/spec.md
    - src/ai_coding_template_ja/openspec_gsd_handoff/source_identity.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/manifest_v2.py
    - src/ai_coding_template_ja/openspec_gsd_handoff/policy_reference.py
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py, tests/test_handoff_execution_mapping.py, tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json</files>
  <behavior>
    - RED: the pinned 49-active-item source state plus the hand-reviewed assignment fixture produces exactly 49 sorted `ManifestMapping` values and preserves multiple source IDs sharing one phase.
    - RED: missing/extra/duplicate source declarations, tombstone or unknown IDs, inconsistent phase ID/path pairs, path aliases, cross-change inventory, malformed policy IDs, and collections over 4096 return one complete structured non-success with no partial mappings.
    - GREEN: future phases retain explicit canonical `phase_path` declarations while their plan/evidence tuples may be empty outside the requested horizon.
  </behavior>
  <action>Write the independent 49-ID assignment fixture first from the pinned canonical source state and ROADMAP phase ownership; include every ID once and store only IDs/paths/policy IDs, never canonical requirement/scenario/acceptance prose. Per D-01, implement strict immutable inventory values and a deterministic builder that validates the whole active/tombstone set, complete assignment coverage, one change ID, canonical POSIX NFC paths, phase ID/path bijection, policy references through Plan 01, ordering, aliases, and bounds before returning any `ManifestMapping`. Do not infer membership from headings, phase names, source paths, fingerprints, or similarity.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_execution_mapping.py -q</automated>
  </verify>
  <done>The fixture and builder yield one exact 49-entry mapping baseline and every incomplete/conflicting declaration fails before a partial graph escapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the four exact operation readiness horizons</name>
  <read_first>
    - src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py
    - tests/test_handoff_execution_mapping.py
    - tests/fixtures/openspec_gsd_handoff/mapping/hardening-phase-assignments.json
    - .planning/phases/02-source-to-execution-mapping/02-CONTEXT.md
  </read_first>
  <files>src/ai_coding_template_ja/openspec_gsd_handoff/execution_mapping.py, tests/test_handoff_execution_mapping.py</files>
  <behavior>
    - RED plan: all active IDs are assigned and the selected phase has one existing canonical phase path; plan/evidence files are not yet required.
    - RED execute: selected-phase mappings and phase path exist and every declared execution plan path exists and belongs to that phase/change; evidence is not yet required.
    - RED verify: the selected phase has all declared plans and all required source/plan evidence paths; a single empty, absent, aliased, escaped, or cross-change path is non-ready.
    - RED finalize: all active sources, phases, plans, and required evidence across the inventory exist and belong to the same change; any future empty declaration is non-ready.
    - GREEN: no operation returns partial green for unknown operation/phase, tombstone reference, conflict, unreadable path, non-regular file, symlink, alias, or limit+1 observation.
  </behavior>
  <action>Implement one readiness evaluator over the explicit inventory and exact mapping set. Apply D-01's horizon table literally and validate only the requested horizon without treating future emptiness as success. Use anchored repository-relative observations and stable structured issue codes; inventory declares which plan/evidence paths are required, so the validator does not scan broadly or infer ownership. D-03 policy IDs remain prerequisite evidence; D-02 refresh publication is not performed in this plan. Use fixed examples only—no mapping property family.</action>
  <verify>
    <automated>uv run pytest tests/test_handoff_execution_mapping.py tests/test_handoff_policy_reference.py -q</automated>
  </verify>
  <done>Each operation distinguishes ready/non-ready at its exact horizon and a future empty path can never satisfy an active request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| explicit inventory → manifest mappings | Duplicate, cross-change, or heuristic declarations can attach evidence to the wrong source. |
| repository paths → readiness result | Missing, aliased, escaped, or symlinked phase/plan/evidence paths can produce false green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04 | Spoofing/Tampering | source/phase assignment | mitigate | Complete active-ID coverage, one-to-one source entries, phase/path bijection, validated policy IDs, and no inference. |
| T-02-05 | Tampering | path membership | mitigate | Canonical POSIX NFC paths, change-scoped explicit inventory, anchored no-follow observations, alias rejection. |
| T-02-06 | Denial of Service | mapping/readiness inventory | mitigate | 4096-entry and 8 MiB bounds with no partial readiness. |
| T-02-SC | Tampering | package supply chain | accept | No install or new dependency. |
</threat_model>

<verification>
- `uv run pytest tests/test_handoff_execution_mapping.py tests/test_handoff_policy_reference.py -q`
- `uv run pytest tests/test_handoff_identity.py tests/test_handoff_manifest_v2.py tests/test_handoff_migration.py -q`
- Confirm fixture has exactly 49 unique source IDs and zero tombstone IDs through the public parser, not a text-count grep.
- Confirm `git diff -- openspec/changes/harden-openspec-gsd-handoff-lifecycle/tasks.md .planning/openspec/harden-openspec-gsd-handoff-lifecycle/handoff.json` is empty.
</verification>

<success_criteria>
- D-01 is implemented as full assignment followed by exact operation-specific readiness.
- D-03 references are resolved through the validated registry rather than copied policy prose.
- Fixed examples are the primary evidence; no Phase-2 mapping property suite exists.
- Root package exports, CLI operations, canonical OpenSpec tasks, and tracked handoff bytes remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-source-to-execution-mapping/02-02-SUMMARY.md` with RED/GREEN commits, exact fixture cardinality, readiness matrix, tests, and unchanged-manifest evidence.
</output>
