# Tasks

- [x] 1. `docs/agents/workflow.md`: 「初めての change」quickstart 節を追加する（proposal → spec delta → tasks → 実装 → validate → pre-merge close の順序・必須 6 項目・既存節へのリンクで束ね本文を二重化しない・「OpenSpec engine のアクセス形態と Markdown fallback」節の直後に配置）
- [x] 2. `docs/guide.md` §4: quickstart 節への 1 行参照を追加する（手順本文なし・ファイルリンク＋節名文中言及）
- [x] 3. `task check` と `task openspec:validate` と verify-change（追記した導線・リンク先の実在を実ファイルで確認）を実施する
