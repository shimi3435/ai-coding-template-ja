# Tasks: dependabot による GitHub Actions 更新自動化

- [ ] 1. `.github/dependabot.yml` を作成する（github-actions / directory "/" / weekly）。
- [ ] 2. `task check` と `openspec validate add-dependabot` が green であることを確認する。
- [ ] 3. 完了報告に「GitHub 上での dependabot PR 生成は未検証（マージ後の初回実行で
       確認）」を明記する。
- [ ] 4. close: マージ前の最終コミットで本 change ディレクトリを削除する（main に
       change ディレクトリを載せない・経緯は PR とブランチ履歴が保持）。
