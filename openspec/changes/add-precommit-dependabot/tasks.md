# Tasks

- [x] 1. .github/dependabot.yml に pre-commit ecosystem エントリ（directory "/" ・weekly）を追加し、ヘッダコメントを実態へ更新する
- [x] 2. .pre-commit-config.yaml の tag pin コメントから「dependabot 監視外」を除き、監視有効の現状へ書き換える
- [x] 3. `task check`・`task openspec:validate` を green にする（ecosystem 認識の実機確認はマージ後・未検証と PR に明記）
